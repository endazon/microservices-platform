---
paths:
  - "**/*"
---

# トレーサビリティ（追跡可能性）の規約

計画書と実装を相互に追跡できるよう、実装の起点となる計画書の ID を以下に残す。コミット・PR・コードを編集する際は本ルールに従う。

## 起点 ID の種別

- `FR-xx`: 機能要求（計画リポ `02_requirements/`）
- `NFR`: 非機能要件
- `UC-xx`: ユースケース（`03_usecases/`）
- `SC-xx`: 画面（`05_screens/`）
- `ADR-xxxx`: 計画ADR（計画リポ `07_adr/`）
- `IADR-xxxx`: 実装ADR（本リポ `docs/adr/`。計画ADR とは別系統）

上記の裸（無修飾）の ID は、**本リポジトリが主に実装するプロジェクトの計画書の名前空間**を指す。
本リポジトリではそれが **MSP（microservices-platform）** であり、ID レンジは
`FR-01..22` / `UC-01..11` / `SC-01..21` / `ADR-0001..0045`（**欠番なし**）である
（**走査基準: planning `2cf0795`**。**［2026-08-09 / planning#304 の裁定］pin が `31a69c9` → `2cf0795`（planning#305）へ進んだので引き直した。`FR` / `UC` / `SC` / `ADR` の 4 種は動いていない**——`FR-01..22`（22 件）／`UC-01..11`（11 件）／`SC-01..21`（21 件）／`ADR` 45 件で欠番なし、`Proposed` な 6 件と `Superseded` な `ADR-0003` も同一である。
> **［2026-08-09 追記 / planning#304 の裁定］★ 5 種目が生まれた。`NFR` に採番が付いた。**
>
> planning#295 が**非機能要件へ `NFR-01`〜`NFR-27` の ID 列を付与した**。**従前 NFR は「区分名」しか持たず、
> 実装側は起点 ID を一律 `NFR` としか書けなかった**（計画側の実測: 直近 100 コミット中 50 件が起点 `NFR` のみで、
> 規約整備なのか性能要件なのかを機械集計できない状態であった）。
>
> - **書式検査は既に通る。** `check-commit-messages.js` の `ID_PATTERN` は `NFR(?:-\w+)?` であり、
>   採番付きも受理する（実測）。**コードの変更は要らない。**
> - **ただし実在性は検査されない。** レンジの実在性検査（#579）の対象は `FR` / `UC` / `SC` の 3 種だけで、
>   **`NFR-99` は素通りする**（同スクリプト。実測）。**ここは書き手が守る。**
> - **既存参照（一律 `NFR`）の遡及書き換えはしない。新規参照から採番付きを使う**（計画側の指示）。
>   本ファイル自身の既存の `NFR` 表記も書き換えない。

（**以下は前回の追随記録である。**［2026-08-09 / #628］pin が `90f5251` → `31a69c9`（planning#300）へ進んだときも 4 種を引き直したが、4 種とも動いていなかった——`FR-01..22`（22 件）／`UC-01..11`（11 件）／`SC-01..21`（21 件）／`ADR` 45 件で欠番なし、`Proposed` な 6 件（下記の列挙）も同一である。**不変であることも実測の結果である。**
**［2026-08-08 / フェーズ末クロス監査］その 1 つ前も同じ結果だった** —— pin が `891b199` → `d9c2014`（#620）→ `90f5251`（#624）と 2 度進んだので 4 種すべてを引き直したが、`FR` / `UC` / `SC` / `ADR` のいずれも動いていなかった。
なお **#624 は pin を進めながらこの引き直しを行わなかった**（#620 は行っていた）。齟齬は生じなかったが手続としては抜けており、**同型 1 回目なので記録に留める**（`CLAUDE.md`「検査器・規約の追加は同型の事故が 2 回起きたら」）。
以下は前回の追随記録である——planning `891b199` = PR planning#284 時点。**［2026-08-07 / #599］レンジは planning#200 時点以来はじめて動いた**——
`FR-22`（通知）・`ADR-0044`（LLM 利用実績の計測粒度と単価表。ADR-0006 の部分改定）・
`ADR-0045`（メール送信基盤。Google のメールサーバへの SMTP リレー）の 3 件が新設された。
**従前ここには「レンジは planning#200 時点から変わっていない」と書いてあり、それは `3e58b97` までは正しかった。**
`UC` / `SC` は変化していない）。
**`ADR-0035`（GraphRAG 検索戦略）は 2026-08-04 に起案され、欠番ではなくなった**。
**`Proposed` な計画 ADR は planning `891b199` 時点で 6 件**である（全 ADR の `status:` を実測。
`Accepted` でも `Proposed` でもないのは `ADR-0003` の `Superseded` 1 件だけ。**［2026-08-07 / #599］`891b199` で 45 件すべてを数え直したが、この 6 件の列挙は変わらない** ——新設された `ADR-0044` / `ADR-0045` はいずれも `Accepted` である。**変えないことも実測の結果である**）——
`ADR-0023`（エッジ TLS 証明書の自動化）／`ADR-0038`（analysis 用途のモデル割当）／
`ADR-0039`（SC-18 のグラフ描画ライブラリ）／`ADR-0040`（k8s 管理 UI の選定・planning#193）／
`ADR-0041`（Result 型の外部ライブラリ）／`ADR-0042`（運用管理 UI の本番導入）。
（**［2026-08-07 / #586］従前は 0040〜0042 の 3 件しか挙げておらず不完全だった**ため、
実測で置き換えた。列挙を足すときは推測せず `grep -m1 '^status:'` で数え直すこと。）
**`ADR-0043`（権限内属性値の照会と存在秘匿の整合・planning#197〜199 の裁定）は `Accepted`** であり、
実装の着手条件を満たす（#540 が実装する。同 ADR の制限を必ず守ること）。

> **［2026-08-07 追記 / #586］`ADR-0033`〜`ADR-0037` は 5 件とも `Accepted` へ移った**
> （planning `3e58b97` = PR planning#244〔裁定依頼 planning#237〕。実測して確認。前の追記が述べていた `ADR-0035` の保留は、
> 計画側が「稼働後の再実測」を同 ADR 決定 4 の**検証項目**へ切り出すことで解消した）。
> **ただし [[IADR-0119]] の保留が解けたのは FR-17 / FR-18 だけである。**
> 同 IADR 決定 2 は FR ごとに条件を分けており、**FR-19 / FR-20 には ADR 以外の条件
> （Wiki.js の個人スコープ可視性の前提検証。計画側が「この依存は解消していない」と明記）が、
> FR-21 には要求そのものの `fixed` 化**がそれぞれ残っている。
> したがって **#450（FR-17/18）は着手可能・#451（FR-19/20）は保留継続**である。
> 詳細は [[IADR-0119]] の 2026-08-07 追補を見ること。
> **`ADR-0039`（SC-18 の描画ライブラリ）は `Proposed` のまま**である（着手条件には含まれない）。
>
> > **［2026-08-07 追記 / #599］上記のうち「#451 は保留継続」は、同日中に部分解除された。**
> > planning#250 の裁定（質問票 第15回 Q5・Q6）が `891b199` へ着地し、
> > **`ADR-0037` に「着手可否の注記」が新設された**。
> > **FR-19 / FR-20 は、SC-19 の「本文を編集（Wiki.js）」導線ただ 1 つを除いて着手してよい**
> > （INDEX 決定 33）。同 ADR の**決定 1〜20 はいずれも覆らず**、SC-20（Obsidian 連携設定）の全体も覆らない。
> > 覆り得るのは前提として引く「個人資料の本文編集手段を Wiki.js とする」裁定（planning#74）だけである。
> > **前提検証（Wiki.js の個人スコープ可視性制御）は #449 から切り出して先に単独で実施する**と計画が定めた
> > ——#449（大玉）の完了を待つと planning#237 で解いたゲートが別の形で復活するためである。
> > **範囲の正は計画 `ADR-0037` の「着手可否の注記」である**（実装側の条件は [[IADR-0142]] 決定 1・2）。**ここへ転記しない** —— 転記すると計画側が動いたとき一斉に腐る。
>
> **［2026-08-06 追記 / #560・上記により更新済み］**`ADR-0035` に ABAC 属性組み合わせ数の実測が
> 反映されたが状態は `Proposed` のまま、という記述は 2026-08-07 の遷移で古くなった。
> ただし**「実測が反映された」ことと「着手条件を満たした」ことは別である**という指摘自体は
> 有効であり、同じ型の誤りが**「ADR が全部 `Accepted` になった」＝「FR-17〜21 が全部着手可能」**
> という形で再発しやすい。**着手条件は FR 単位で読むこと。**

> **このレンジが実際に効く場所**（PR #550 のレビュー指摘・2026-08-06 に実測して確定）:
> **計画 ADR の実在性検査は、どのワークフローからも submodule を populate した状態で実行されない。**
> `check-commit-messages.js` を走らせるのは `pr-title.yml` と `ci.yml` の `commit-messages` だけで、
> どちらも `actions/checkout` に `submodules` を付けない（`loadExistingPlanAdrIds()` が null を返し
> skip される）。唯一 submodule を populate する `doc-links-planning.yml` は
> **`check-doc-links.js --require-planning` しか実行しない**（別スクリプト・別対象）。
> したがって**このレンジが効くのは、ローカル実行と、本ファイルを読んで作業する AI だけ**である。
> **CI は計画 ADR の実在性を守っていない。** 実効させたいなら、`check-commit-messages.js` を走らせる
> ジョブへ `submodules` ＋ `token` を付ける必要がある（下記「ADR / IADR の実在性」の注も参照）。
**`Proposed` でも ID としては実在する**——参照の実在性検査（`check-commit-messages.js` 等）は
状態を見ないため、レンジには含める。実装の着手条件（`Accepted` 化）とは別の話である
（[[IADR-0119]] 決定 2）。計画側で ID が増減したら本節を追随させる
（レンジは監査 / `trace-check` の突合基準であり、古いままだと新 ID の参照を「存在しない ID」と誤検出する）。

## 複数プロジェクトを跨ぐ場合の ID 修飾

1 つの実装リポジトリが複数プロジェクトの計画書を参照する場合（基盤リポに別プロジェクトを
submodule で取り込む等）、**計画 ID はプロジェクトごとに独立採番のため衝突する**。裸の ID で
書くと、機械的な突合（監査・`trace-check`）が「計画書に存在しない ID の参照」「誤帰属」を
誤検出する（例: 双方に存在し意味の異なる `FR-17` / `SC-01`）。

- **書式**: `<PROJ>/<ID>`。例 `AST/FR-17`・`AST/SC-01`。`<PROJ>` は短縮プロジェクト名。
- **適用箇所**: コード内コメント・IADR / 仕様書の本文・テスト名、およびコミット / PR 件名の説明文。
- **コミット / PR 件名のスコープ `()`**: `check-commit-messages.js` の実在性検査は本リポジトリの
  名前空間しか解決できない。よって**スコープには自プロジェクトが所有する ID を置き**、他プロジェクトの
  ID は説明文で修飾して参照する。例:
  `feat(IADR-0072): AST/SC-02 監視銘柄の /bff/monitor プロキシを登録`
- **機械検査**（#576）: `scripts/check-plan-id-qualification.js`（`--self-test` あり）が
  **空白区切り**（`AST FR-17`）を検出する。対象は**追跡下の全ファイル**（拡張子で絞らない）で、
  submodule・`CHANGELOG.md`（生成物）・`docs/specs/`・`feedback/`（書いた時点の記録）は除く。
  **`check-cross-repo-refs.js` とは対象が違う** —— あちらは **issue / PR 番号**（`AST#24`）、
  こちらは **計画 ID / ADR ID**（`AST/FR-17`）である。同じ `AST` で始まるので混同しないこと。
  **検出しないもの**: 「AST 文脈で裸の ID」（`AST リスク設定（SC-02）`）は近傍規則になり
  **偽陽性が避けられない**ため検査しない —— 本ファイル自身が「AST の `FR-17`」と
  **誤帰属そのものを説明する地の文**を持つ。列挙の後続 ID（`AST/SC-02/SC-03` の `SC-03`）も同様。
  **どちらも人と AI が守る。**
- **監査 / trace-check の扱い**: `<PROJ>/<ID>` は外部プロジェクトへの参照であり、自名前空間に対する
  「参照切れ」ではない。監査は修飾付き ID を突合対象から**除外**する
  （`.claude/agents/traceability-auditor.md` に同じ規約を書くこと）。
- **既存履歴は不変**: 過去コミット件名の裸 ID は書き換えない（force-push 禁止）。CHANGELOG 生成時の
  `changelog-overrides.json`（`remap`・後述）または今後の編集で生成物 / 本文のみ是正する。

### 本リポジトリでの名前空間（固有設定）

本リポジトリは複数ユニット（`src/platform` / `src/knowledge`）と、submodule で取り込む別プロジェクト
（`src/ai-stock-trading`）を含む。上記規約の可変部分を次のとおり確定する。

- **計画 ID の `<PROJ>`**: ai-stock-trading = `AST`。例 `AST/FR-17`・`AST/SC-01`・`AST/UC-06`。
  実際に AST の `FR-17`（当時 MSP は FR-15 まで）と AST の `SC-01`（MSP の SC-01＝検索/チャットと衝突）で
  誤検出が発生した。
  **AST 側が自前で採番しているレンジは `FR-01..20` / `UC-01..07` / `SC-01..03`**（submodule pin `655e2ed`
  時点で実測）。AST の文書に他の `SC` 番号も現れるが、いずれも MSP 画面への**参照**であって AST の採番ではない
  — `05_screens/01_screens.md` の `SC-13` / `SC-16`、`07_adr/ADR-0012_mcp-exposure-policy.md` の `SC-11`。
  計画大改定（PR planning#144）で MSP のレンジが `FR-01..21` / `UC-01..11` / `SC-01..21` へ広がった結果、
  重なり方は次のとおり変化した。**修飾の欠落がどちらの誤りとして現れるかが変わる**ため区別する。
  - **`FR-16`〜`FR-20`**: 従来は AST にしか無く、裸で書けば「計画書に存在しない ID」＝**参照切れ**として目立った。
    MSP 側に同番号が生まれたことで**双方に存在し意味の異なる ID** へ変わり、修飾の欠落は**誤帰属**
    （別プロジェクトの要求へ静かに紐づく）として現れる。検査で捕まらないのはこの型である。
  - **`FR-01`〜`FR-15` / `UC-01`〜`UC-07` / `SC-01`〜`SC-03`**: 改定前から双方に存在する（従来どおり誤帰属）。
  - **`FR-21` / `FR-22` / `UC-08`〜`UC-11` / `SC-04`〜`SC-21`**: MSP にしか無い（**`FR-22` は［2026-08-07 / #599］で追加**。AST の採番は `FR-01..20` なので衝突しない）。**`SC-18`〜`SC-21` は AST 側に対応 ID が
    存在しないため衝突しない**（将来 AST が採番を伸ばせば上の「誤帰属」型へ移る）。

  裸の ID は必ず MSP を指す。
- **issue / PR 番号の修飾**: **短縮形に寄せる**（フルパス形式と混在させない）。
  `AST#NNN`（ai-stock-trading）・`planning#NNN`（project-planning）。GitHub の自動リンクを効かせたい
  箇所に限りフルパス形式（`endazon/ai-stock-trading#NNN`）を用いてよい。
  **列挙形でも各番号を修飾する**（先頭だけの修飾は後続の番号を本リポジトリへ誤リンクさせる）。
  誤: `planning#146 / #149 / #160`。正: `planning#146 / planning#149 / planning#160`。
  - **［#507 の裁定］Markdown の明示リンクもテキストは短縮形へ揃える**
    （`[planning#197](https://github.com/endazon/project-planning/issues/197)`）。URL 側が自動リンクを
    担うのでテキストを短縮形にしても機能は落ちず、表記を 1 つに保てる。**`project-planning#NNN` /
    `ai-stock-trading#NNN`（リポジトリ名の裸書き）は短縮形でもフルパス形式でもない第 3 の表記であり、
    使わない。**
  - **修飾語と番号の間に空白を入れない。** 誤: `AST #24` / `planning PR #144`。正: `AST#24` /
    `PR planning#144`。空白が入ると `AST#24` を探す機械的突合に掛からず、自動リンクが効く面
    （issue / PR / コミットメッセージ本文）では番号が本リポジトリへ張り付く。
    **`MSP #266` のように自リポジトリを指す修飾語の直後は裸のままでよい**（番号が本リポジトリを
    指すのは正しい）。
  - **［#590］フルパス形式の owner は `endazon` である。** 本組織が持つリポジトリ
    （`project-planning` / `ai-stock-trading` / `microservices-platform`）をフルパス形式で書くときの
    owner は `endazon` ただ 1 つであり、他の綴りは誤りである
    （**実測値はここに書かない** —— 走査基準つきの数は
    [作業仕様書 §母集合](../../docs/specs/20260807_issue-590_fullpath-owner-check.md) を正とする。
    数を条文へ埋めると、自己試験へ正例を足しただけで黙って古くなる。**#590 が実際にそれを踏んだ**）。
    **この型だけは `.md` でも実害が出る** —— 上記のとおり
    `.md` で自動リンクになるのはフルパス形式だけなので、**owner を誤ると死んだリンクが描画される**
    （型 1〜3 は `.md` では表記ゆれに留まる）。**第三者リポジトリへの参照はこの限りではない**
    （`anthropics/claude-code-action#723` は owner が `endazon` でないのが正しい）。
  - **機械検査**: `scripts/check-cross-repo-refs.js`（`--self-test` あり）が上記の各型
    （長い表記・列挙形の修飾漏れ・空白区切り・**フルパス形式の owner 誤り**）を検出する
    （**型の件数は書かない** —— 型を足したとき片方が黙って古くなるため。正は同スクリプト冒頭の列挙）。
    対象は追跡下の `*.md` と、
    `scripts/check-commit-messages.js` 経由の**コミット件名 / 本文 / PR タイトル**。
    規約に書くだけでは再発する（PR #561 は**本ファイルを編集する PR でありながら**同じ違反を犯し、
    書式しか見ない検査を green で通過した）。[[IADR-0140]]。
  - **どこで自動リンクが効くかを取り違えないこと**（実測。[[IADR-0140]] 決定 1）。
    **`.md` では裸の `#NNN` も短縮形の修飾も自動リンクにならない**——`.md` でリンクになるのは
    フルパス形式だけである。したがって `.md` での違反は「表記ゆれ（機械的突合の不安定）」であり、
    **誤リンクという実害が出るのは issue / PR / コミットメッセージの本文**である。
  - **意図的に誤例を書くときはインラインコードかコードフェンスに入れる**（上の「誤:」の行がその形）。
    literal な引用は表記規約の対象外であり、検査器も対象外にする（自動リンクが効く面でも
    コード中は平文で描画される）。

## 残す箇所と書式

- **ブランチ名**: `<種別>/<起点ID>-<概要のケバブケース>`。例 `feat/FR-012-login-validation`。
- **コミットメッセージ**: 先頭に `<種別>(<起点ID>): <要約>`。例 `feat(FR-012): ログイン画面のバリデーションを実装`。
  - 種別: `feat` / `fix` / `refactor` / `test` / `docs` / `chore` 等。
  - 1 コミット = 1 論理変更。複数 ID にまたがる場合は `feat(FR-012,UC-03): ...` のように併記する。
- **コード内コメント**: 計画書由来の実装箇所に ID を残す。例 `// FR-012, UC-03: 入力バリデーション`。
- **テスト**: テスト名またはコメントに起点 ID を残す。
- **PR**: PR テンプレートの該当欄に、実装した FR/UC・関連 ADR・受け入れ基準のチェックを記入する。

### Superseded / Deprecated な ADR を引用するときの書式（#580）

計画 ADR は Supersede されても**実装が同時に移行するとは限らない**（例: `ADR-0003`（MassTransit +
RabbitMQ）は `Superseded by ADR-0027`（Wolverine）だが、コードは未移行で MassTransit のままである）。
このとき **ID を後継へ付け替えてはならない**——付け替えると「この実装は後継の決定に従っている」という
偽の主張になり、実装の由来（当時なぜそう作ったか）と、移行がどこまで済んでいるかの記録が同時に消える。
**旧 ID を残したまま、後継を併記する。**

| 置き場所 | 書式 |
| --- | --- |
| frontmatter の ID リスト（`related_ids` / `related_adrs`）・`plan_refs` | **旧 ID を残し、後継 ID を項目として併記する**。ID に説明を混ぜない（`- ADR-0003（Superseded by ADR-0027）` と書くと ID として解析できず、監査 / `trace-check` の突合を壊す） |
| 散文・コード / 設定ファイルのコメント | `ADR-0003（Superseded by ADR-0027）` のように、ID に隣接する括弧内へ `Superseded by <後継 ID>` を書く |

実際に移行したときは、この決まった文字列を後継 ID へ一括置換する（先に付け替えると、移行の実施と
記録の一致を後から検証できなくなる）。

**注記には起票 ID を添える**（#580 / クロス監査 G-b）。ADR / 仕様書の本文へ後から注記を差し込むと、
原文と見分けが付かず**いつ誰が足したのか本文から辿れなくなる**。`ADR-0003（MassTransit。Superseded
by ADR-0027・注記は #580）` のように**注記そのものへ起票 ID を書き**、あわせて frontmatter の
`updated:` を前進させる。決定を変える追記なら ID ではなく**日付つきの追記ブロック**にする
（`［YYYY-MM-DD 追記 / #NNN］…`。#570 / #577 / #582 が採っている形）。
**適用先は下記「母集合」の内側すべてである**——`docs/adr/` だけでなく `docs/functional/` `docs/data/`
`docs/tech/` `docs/tests/` 等の live な文書も、**コード・設定ファイルのコメントも同じに扱う**
（ADR 本体だけに適用しない）。ただし**対象は「後から差し込んだ注記」に限る**——ファイルを新規作成
した時点の原文（例: `src/knowledge/backend/Tests/Knowledge.IntegrationTests/Deployment/NetworkIsolationTests.cs`
冒頭の `// IADR-0017（Superseded by IADR-0026）`。ファイル追加コミットから在る記述）は注記ではないので、
遡って起票 ID を足す必要はない（足しても「いつ誰が差し込んだか」の情報は増えない）。ただし **frontmatter を持たないファイル（コード・設定）は注記 ID
だけでよい**——`updated:` は前進させる先が無いので求めない。**コードを対象外にしない理由**:
母集合を切る基準は「**書いた時点の記録か否か**」であり（下記のとおり `docs/specs/` 等はそれで外れる）、
コードはその基準に当たらない。「`git blame` で辿れるから注記 ID は要らない」は `.md` にも等しく
当てはまるので、コードだけを外す根拠にならない。

**後継 ID は旧 ID の隣に置く**（#580 / クロス監査 G-c）。frontmatter の ID リストで `ADR-0003` の
直後に `ADR-0027` を置くと番号順が崩れるが、**これは意図的**である——「この旧 ID の後継はこれ」と
いう対応が読み手に伝わることを、番号順より優先する。機械照合は順序非依存なので実害はない。

**この書式を適用する母集合は「live な権威文書とコード」に限る**——**確定済み（過去 PR の）**
`docs/specs/`（作業 / PR 単位の一時点記録）・`feedback/`（計画リポへ送った内容の写し）・
`docs/superpowers/`（保管された旧計画）は**書いた時点の記録**であり、後から注記を足すのは記録の
改竄にあたるので**書き換えない**。**作業中の PR 自身の作業仕様書は別**で、その PR の中で内容を
更新してよい（まだ一時点の記録として確定していないため）。

> **機械検査は置いていない**（#580 の測定）。計画 ADR の `status` を読むには planning submodule が
> 必要だが、**PR で起動する決定的な検査ジョブ**（`ci.yml` の `doc-links` / `scripts-tests` /
> `commit-messages` 等、`pr-title.yml`）は**どれも submodule を populate しない**ため、検査を作っても
> 常に skip され緑のまま素通りする。よって本規約は人と AI が守るものであり、CI は守っていない。
>
> **例外は 2 本あるが、いずれもゲートではない**（#580 の測定・実測日 2026-08-07）。どちらも
> `PLANNING_REPO_TOKEN` を使って `git submodule update --init --recursive` を実行する。
>
> | ワークフロー | トリガ | PR 文脈で起動するか |
> | --- | --- | --- |
> | `claude-code-review.yml` | `on: pull_request`（`opened` / `synchronize`） | する |
> | `claude-coding.yml` | `issue_comment` / `pull_request_review_comment` / `pull_request_review` / `issues` | する（PR へのコメント・レビューで起動する） |
>
> ただしどちらも **AI 実行系であってマージを止める決定的ゲートではない**（前者は AI レビュー、
> 後者は `@claude` メンションでの対話実装）ので、これらに検査を載せても「PR で planning を読む
> **検査**」にはならない。「**PR で planning は絶対に取れない**」と読み違えないこと——取れるジョブは
> 在るが、ゲートではない、が正しい。

## 是正・追随の母集合の取り方（[[IADR-0141]] 決定 1）

表記の是正・ID の追随・条文の更新はいずれも「**どこまでが対象か**」を先に決める作業である。
**母集合の取り違えは、この規約で最も繰り返し起きている事故**であり（下表の「破れた実例」はすべて
2026-08-07 までの実測）、**着手時に実装側が自分で引き、結果と除外理由を作業仕様書へ書く。**

| # | 規則 | 破れた実例 |
| --- | --- | --- |
| 1 | **誤りの側から引く**（正しい表記だけを検索語にしない） | #541（正しい表示名だけを検索語にし、誤った表示名を構造的に取りこぼした）／#507（母集合 88 件に第 4 の表記 95 件が丸ごと欠けていた） |
| 2 | **あり得る形をすべて列挙してから引く** | #583（`AST #24` 型が着手時の走査式から丸ごと漏れた） |
| 3 | **拡張子で絞らない**（パスの除外だけで取る） | #570（`--include` で `.sh` / `.js` を落とし、compose だけ直すと赤が入れ替わるだけだった） |
| 4 | **行フィルタで絞らない。パスから引く** | #593（走査の末尾に `grep -i "feedback\|FR-08"` を継いだため、該当行が両語を含まない `docs/api/openapi.yaml` が落ちた） |
| 5 | **軸を 1 本で終わらせない** | #593（監査の 8 件に対し、2 軸目で +4、3 軸目でさらに +7 が出た） |
| 6 | **引いた結果と、除外したものとその理由を作業仕様書に書く** | —（[[IADR-0141]] で新設。事故は引き漏らしだけでなく「黙って除外した」ことでも起きる） |
| 7 | **「追随する文書」を記憶で挙げない。誤りの側の文字列で全文書を走査してから挙げる** | #642（訂正を 3 箇所へ入れて 2 箇所取り残し、うち 1 つは同ファイル冒頭と矛盾）／#645（`openapi.yaml`・[[IADR-0044]]・[[IADR-0041]]・`security.md` の 4 件が丸ごと漏れた。**#628 は同じ追随をしていた**） |

- **規則 7 は「同型 2 回」で足した**（`CLAUDE.md`「検査器・規約の追加は同型の事故が 2 回起きたら」）。
  **機械検査は置かない** —— 「認可を変えた」ことを機械が知るには意味の理解が要り、
  文字列一致では偽陽性が避けられない（本ファイルが `AST` の近傍規則で既に採っている言い方と同じ）。
  **走査の型は決まっている**: 変えた事実を**否定する側の語**（例: 認可を admin 限定へ狭めたなら
  `admin/operator` / `管理者・運用者`）で `docs/` と `*.yaml` を全数引き、
  **確定済みの `docs/specs/` と履歴の地の文だけを理由付きで除外する。**
- **issue 本文の「反映先」リストは母集合ではない。** #594 では引き直しで反映先が 3 件増えた
  （`.claude/commands/` 2 本と [[IADR-0139]] 決定 4）。
- **他人の数えを検証せず転記しない。** 監査の一覧も、前の PR の表も、引き直しの起点であって結論ではない。
- **機械検査は無い。** 本節が守られているかを見るスクリプトは置いていない（[[IADR-0141]]「検出しないこと」）。
  作業仕様書に「母集合」の節があるかを検査するものも無い。

## 守ること

- 起点 ID を持たない大きな変更を作らない（雑多な変更は理由を明記する）。
- 計画書に存在しない ID を参照しない（誤記・廃止に注意）。
- ADR の制約に反する実装をしない。逸脱が必要なら新 ADR の起票を提案する。
- **是正・追随に着手する前に、上記「母集合の取り方」に従って対象範囲を自分で引く。**

## コミットメッセージの機械チェック（CI・再発防止）

PR で追加されるコミット（`base..HEAD`）の件名を `scripts/check-commit-messages.js` が検査し、規約
`種別(起点ID): 要約` に違反していれば CI を失敗させる。既存履歴は書き換えず、再発防止のみを目的とする。

- **許可する種別**: `feat` / `fix` / `perf` / `refactor` / `docs` / `test` / `build` / `ci` / `style` / `chore`。
- **起点 ID の書式**: `FR-\d+` / `NFR` / `UC-\d+` / `SC-\d+` / `ADR-\d{3,4}` / `IADR-\d{3,4}` / `P0`〜`P3`。
  複数 ID はカンマ区切りで併記。スコープ `()` は省略可。
- **末尾の PR 番号**: 半角スペース + `(#123)` はスカッシュマージ既定件名として許容。
- **FR / UC / SC の実在性**（#579）: スコープの `FR-xx` / `UC-xx` / `SC-xx` が上記「起点 ID の種別」節の
  計画レンジに実在することを検査する。レンジのパーサは `check-test-traceability.js` のものを再利用する
  （**同じ事実を 2 本のパーサで持たない**）。
  **この検査が無い間、`feat(SC-99)` / `feat(FR-77)` / `feat(UC-88)` はいずれも exit 0 で受理され、
  恒久履歴へ載れる状態だった**（実測）。**着地済みの全件を走査した結果、違反は 0 件**なので、
  本検査は是正ではなく**予防**である。
- **ADR / IADR の実在性**: 書式に加え、スコープの `IADR-xxxx` は本リポ `docs/adr/` に、`ADR-xxxx` は
  planning submodule の `projects/<name>/07_adr/` に該当ファイルが実在することを検査する
  （PR コミットと PR タイトルの両方）。読めない環境（submodule 未 populate 等）では該当検査を
  skip する（`check-doc-links.js` と同じ扱い。ローカル環境差で CI を落とさない）。
  **注**: PR の CI が submodule を取得しない構成の場合、実効的に働くのは IADR 検査のみである。
  計画 ADR 検査まで CI で効かせたい場合は、submodule を取得するジョブ（例: 夜間の planning 系
  ジョブ）に結線すること。

### 採番衝突時の改番手順

並行実装では IADR 番号の採番衝突が起こり得る。**番号は先にマージした側が確保する（先着尊重）**。
後発は次の空き番号へ改番し、欠番を作らない。改番時は以下を**すべて**追随させる。

1. ファイル名（`docs/adr/IADR-xxxx_*.md`）と本文中の自番号
2. 索引（`docs/adr/README.md`）・関連仕様書（`docs/specs/` の `related_ids` / 本文）
3. コード内コメント・テスト名の起点 ID
4. **PR タイトル**（= スカッシュ後にコミット件名として統合ブランチへ載る。ここだけが人手の追随に
   依存するため最も漏れやすい。ブランチ名は既 push なら変更不要だが、タイトルは必ず直す）

### クロスリポジトリの issue / PR 番号の修飾

計画 ID と同様に、**issue / PR 番号にも名前空間衝突**がある。裸の `#NNN` は GitHub 上で
**本リポジトリの** issue / PR へ自動リンクされるため、他リポジトリの番号を裸で書くと無関係な
issue / PR へ誤リンクする（コミット footer の `Refs #NNN` で実際に発生する）。

**規約**: 他リポジトリの issue / PR 番号は必ず修飾する（例: `<短縮リポ名>#NNN`）。適用箇所は
コード内コメント・IADR / 仕様書の本文・コミット件名 / footer・PR 本文。GitHub の自動リンクを
効かせたい箇所ではフルパス形式（`<owner>/<repo>#NNN`）を用いてもよい。裸の `#NNN` は常に
本リポジトリの issue / PR を意味する。**プロジェクト内で短縮形とフルパス形式のどちらに
寄せるかを最初に決め、混在させない**（機械的突合の揺れを避けるため）。

#### 関連番号を添える注記 `〔〕`

PR 番号とその裁定依頼 issue 番号のように、**同じ他リポジトリの番号を 2 つ並べる**ときは
`〔〕` で添える（#586 が採用）。

```
正: planning `3e58b97` = PR planning#244〔裁定依頼 planning#237〕
誤: planning `3e58b97` = PR planning#244〔裁定依頼 #237〕
```

**`〔〕` の中の番号は、先頭の番号と同じ他リポジトリを指す。** したがって中を裸の `#NNN` に
すると誤リンクの型（型 2 ＝ 先頭だけ修飾）になるため、`check-cross-repo-refs.js` が検出する。

- **`（）` は同じ扱いにしない。** 日本語の地の文で従属節を開く一般的な記号であり、
  その直後の裸 `#NNN` が**本リポジトリを指すのは正しい**（例: `planning#197（#502 由来）`）。
- **先頭の番号を裸にする形**（`PR #244〔裁定依頼 planning#237〕`）は**機械では検出できない**。
  裸の `#244` は規約上「本リポジトリの PR」を意味する正しい表記であり、
  意味の取り違えは構文から判定できない。**書き手が責任を持つ。**

### PR タイトル（スカッシュ後件名）の検査（再発防止）

スカッシュマージで develop に載る件名は「**PR タイトル + 半角スペース + `(#番号)`**」であり、PR コミット
（`base..HEAD`）の範囲に含まれないため、上記のコミット件名チェックだけではすり抜ける
（ブランチ名由来の規約外件名がそのまま統合ブランチへ載る事象が実際に起きている）。これを防ぐため `pr-title.yml` が PR タイトルを規約
`種別(起点ID): 要約` に照合し、違反ならマージ前に CI を失敗させる。

- 検査ロジックは `check-commit-messages.js` の単一件名モード（`--title` / `--author` 引数、
  `PR_TITLE` / `PR_AUTHOR` 環境変数）で、`validateSubject` を再利用する（規約の単一情報源）。
- `pull_request` の `opened/edited/reopened/synchronize` で起動し、タイトル後編集も再検査する。
- **除外は「作成者の名前」で行う**（#524）。ワークフローは `PR_AUTHOR`
  （`github.event.pull_request.user.login`）を渡すだけで、`dependabot[bot]` 等かどうかの判定は
  `check-commit-messages.js` の `BOT_AUTHORS`（`isBotAuthorName`）が担う。Revert・`[skip ci]` も
  同スクリプトが除外する。
  - **ジョブ条件で `user.type != 'Bot'` を使ってはならない。** それは dependabot だけでなく
    **GitHub App が人の代わりに作成した PR（`claude[bot]`）まで除外**し、この検査が `skipped` になる
    （PR #523 で実測）。スカッシュ後件名は develop に恒久的に残り、`base..HEAD` 検査にも含まれず、
    force push 禁止で事後修正もできないため、規約違反の件名が入る経路が空く。
    `scripts/scripts.repo.test.js` が全ワークフローを走査してこの判定の再混入を止める。

#### ★ スカッシュ件名を書き直すときは、スコープの ID を 1 つも落とさない（#579）

**上記の検査は「PR タイトル ＝ スカッシュ後件名」を前提にしているが、その前提は破れる。**
マージダイアログ（GitHub UI・API の `commit_title`）で件名を**書き直せる**からである。

**実際に破れた**（#568・`bc7bc8e`）。

| | 文字列 |
| --- | --- |
| PR タイトル（`pr-title.yml` は green） | `feat(FR-12,SC-07): ConversionJobDto にデッドレター標識と試行上限を追加する` |
| **develop に載った件名** | `feat(FR-12): 変換ジョブのデッドレター標識と試行上限を契約へ追加する (#568)` |

**`SC-07` が消えている。** 同 PR は SC-07 の画面仕様書・テスト仕様書・実装を実際に改変しており、
**恒久履歴とそこから生成される `CHANGELOG.md` から SC-07 が辿れなくなった**
（force push 禁止のため件名は事後修正できない。`changelog-overrides.json` の `remap` で生成物のみ是正した）。

**要約は書き直してよい。スコープの ID は落としてはならない。**
複数 ID なら**併記のまま**運ぶ（`feat(FR-12,SC-07): ...`）。

- **機械検査**: `scripts/check-landed-subjects.js`（`--self-test` あり）が、**実際に載った件名**を
  規約へ照合する（`scripts.repo.test.js` 経由で `ci.yml` の `scripts-tests` に載る）。
  **既存履歴は `scripts/landed-subject-baseline.json` のラチェットで据え置き、新規混入だけを落とす。**
- **ただし、この事故そのものは機械で検出しない**（**「できない」ではない**。下記）。落ちた後の件名 `feat(FR-12): ...` は
  **それ自体が完全に規約適合**であり、「落ちた」と判定するには PR タイトルとの突合が要る。
  **PR タイトルはリポジトリの中に無い。** 検査が捕まえるのは**書式違反**と**実在しない起点 ID** の
  2 型だけで、どちらも**恒久履歴に入ってから**気づく事後検知である。
  **ここは書き手（マージを実行する人・AI）が守る。**
  - **［2026-08-08 追記 / フェーズ末クロス監査］「できない」は言い過ぎだった。**
    **変更パス由来の FR/UC/SC ID がスコープに在るか**は、PR タイトルなしで判定できる。
    実測（着地件名 319 件・**走査基準: develop `2cd8508` 時点**）: **26 件ヒットし、`bc7bc8e`（実際の事故）を含む**。
    **採らない理由は「できない」ではなく「偽陽性 25/26 で方向が合わない」**である
    —— pin 更新や横断的な是正 PR が、触ったファイル名の ID を大量に拾う。
    **これは本ファイルが `AST` の近傍規則で既に採っている言い方と同じである**
    （「近傍規則になり偽陽性が避けられないため検査しない」）。[[IADR-0145]] 決定 3 の追記。
- **最も確実なのは、件名を書き直さないことである。** 書き直す必要があるなら、
  **先に PR タイトルを直してからマージする**（そうすれば `pr-title.yml` が検査する文字列と
  着地する文字列が一致する）。

> **［2026-08-08 追記 / フェーズ末クロス監査］★ 対になる規則: 実体を伴わない ID をスコープへ足さない。**
>
> 上の条文は「**落とすな**」しか書いておらず、**逆向きの誤り（足す）を止めていなかった。**
> **その穴を、この節を書いた PR 自身（#612）が踏んだ。**
>
> | | |
> | --- | --- |
> | 着地件名 | `feat(NFR,FR-12,SC-07,IADR-0145): スカッシュ着地件名を検査し、FR/UC/SC の実在性を機械化する (#612)` |
> | 実際に触ったもの | `scripts/` の検査器と `docs/adr/` `docs/specs/` `.claude/rules/`。**FR-12 / SC-07 の成果物は 1 つも触っていない**（実測） |
> | 両 ID が現れる唯一の場所 | `scripts/changelog-overrides.json` の **`bc7bc8e` 用 remap の値** |
>
> **他コミットの記録を是正する作業で、是正対象の ID を自分のスコープへ引き写してはならない。**
> 起点 ID は**自分の作業**のものであり、是正対象は**本文で参照する**。
> 正: `feat(NFR,IADR-0145): …（#568 の FR-12,SC-07 の記録も remap で是正）`。
>
> **先例は既にあった** —— #596（`78c9753`）は同じく remap で他コミットの記録を是正する作業だが、
> 対象の scope（`NFR,IADR-0116`）を継承せず `fix(NFR,IADR-0140)` を名乗っている。**#612 だけが破った。**
>
> **実害**: `CHANGELOG.md` で **FR-12 / SC-07 を辿る読み手に、変換ジョブと無関係な検査器の PR が
> 「新機能」として二重計上される**（`bc7bc8e` の正当な行と並んで出ることを実測）。
> `changelog-overrides.json` の `b421761` が「**新たな誤帰属を避ける**」として禁じている型と同じである。
>
> **機械では捕まらない。** `check-landed-subjects.js` が見るのは**実在性**であり、
> `FR-12` / `SC-07` はどちらも実在するので通る（実測: 着地件名 319 件で違反 0。**走査基準: develop `2cd8508` 時点**）。
> **落とした ID も足した ID も、機械は同じ理由（PR タイトルが無い・実体との突合が要る）で見られない。**
> **ここは書き手が守る。**

**事前防止と事後補正の使い分け**: `pr-title.yml` は**マージ前**の予防（規約外件名を止める）。
万一すり抜けて develop に規約外件名が入った場合は、履歴不変の原則（force push 禁止）に従い、
下記 CHANGELOG 生成時の `changelog-overrides.json`（`action: "remap"`）で**生成物のみ**是正する。
前者が第一防衛線、後者が最終手段という役割分担。

### 検査対象から除外する自動コミット

- **自動コミットの著者**: `dependabot[bot]` / `renovate[bot]` / `github-actions[bot]` 等の bot 著者。
- **マージコミット**: `--no-merges` により除外。
- **自動生成・リバート**: 件名に `[skip ci]` を含むコミット、および `Revert "..."`。

除外リストは `scripts/check-commit-messages.js` の `BOT_AUTHORS` と同時に更新する。

## CHANGELOG 生成時の誤記補正・除外規定

トレーサビリティは**既存の git 履歴を書き換えないこと**を大前提とする。過去コミットの起点 ID
やスコープに誤記があっても `rebase`／force push で修正してはならない。代わりに、`CHANGELOG.md`
を生成する `scripts/gen-changelog.js` が生成時のみ補正／除外を適用する。補正内容は
`scripts/changelog-overrides.json` の `overrides` 配列で宣言的に管理する（`hash` は短縮 SHA 前方一致）。

- **誤記補正（`action: "remap"`）**: 誤った起点 ID・種別・要約を、CHANGELOG 上でのみ差し替える。
  `type` / `scope` / `desc` を任意に指定でき、省略した項目は元コミットの値を保つ。
  - 例: 件名の起点 ID が誤って `feat(FR-10)` となっているが、実体は基盤スケルトン（P0）である場合、
    `scope` を `FR-10` → `P0` へ補正する。実体が大規模実装なら `type` は `feat` のまま保持し、
    `docs` へは remap しない（実装をドキュメントとして過小計上する新たな誤帰属を避ける）。
  - 配布時の `overrides` は空である。他リポジトリの SHA を引き継がないこと
    （`hash` は前方一致のため、偶然一致した無関係なコミットを誤って差し替える）。
- **除外（`action: "exclude"`）**: CHANGELOG に載せるべきでないコミット（試験的・巻き戻し前提の
  作業等）を生成物から除外する。git 履歴には残るため追跡可能性は失われない。
- 未知の `action`（タイプミス等）は `gen-changelog.js` が警告を出して補正を無視する（黙って
  remap 扱いにしない）。許可値は `remap` / `exclude` の 2 種のみ。

補正・除外はいずれも「履歴は不変・生成物のみ是正」という原則に従い、その根拠を各エントリの
`reason` に必ず残す。CI（`changelog.yml`）は `develop` / `main` への push で `fetch-depth: 0` の
全履歴から CHANGELOG を再生成し、本補正を含む差分を PR 経由で反映する。
