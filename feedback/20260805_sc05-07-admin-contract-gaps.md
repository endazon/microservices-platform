---
title: SC-05〜07 の画面要素 6 種が現在の API 契約に載らない（人手補正・デッドレター内訳・同期異常・次回同期・コネクタ設定・文書の変換状況）
type: plan-feedback
status: open
category: UC/画面の差異
related_ids: [SC-05, SC-06, SC-07, UC-03, UC-04, UC-06, FR-01, FR-02, FR-06, FR-09, FR-12]
source_repo: microservices-platform
source_ref: "feat/SC-05-08-admin-screens / docs/specs/20260805_issue-503_sc05-08-admin-screens.md（#503）"
author: Claude（実装）
created: 2026-08-05
updated: 2026-08-05
---

# フィードバック: SC-05〜07 の画面要素 6 種が現在の API 契約に載らない

## 起票状況（**計画リポジトリへの起票は未了**）

`feedback/README.md` の手順は 3 段（1. `/plan-feedback` 実行 → 2. 記録作成 → 3. **計画リポへの伝達**）である。

| 手順 | 状態 |
| --- | --- |
| 2. `feedback/` への記録作成 | **完了**（本ファイル） |
| 3-a. `planning/draft/feedback/` へのコピー | **未了** |
| 3-b. `endazon/project-planning` への Issue 起票 | **未了**（**起票は親が行う。起票後に本表を更新すること**） |

フロントマターの `status: open` は TEMPLATE の既定値であり、「未起票」と「起票済み・未トリアージ」を
区別しない。**本節の表が実態の正である。**

**本記録は planning#197 とは別の論点である。** planning#197（#502 由来）は
「検索・AI 回答の**対象範囲フィルタ**と検索結果 DTO の項目」であり、本記録は
「**管理系画面（SC-05〜07）の運用情報**」を扱う。ただし **SC-08 の分析対象チップ（タグ／フォルダ）は
planning#197 と同型の論点**（**権限内候補**を返す API が無い）であるため、**本記録には含めず
planning#197 の裁定を待つ**（重複起票を避ける）。

## 種別

UC/画面の差異（**計画は画面要素を定めているが、それを支えるバックエンド契約が存在しない**）。

## 起点となる計画書

- 機能要求（FR）: FR-01（データソースの登録・同期・カタログ化）・FR-02（取り込み）・
  FR-06（文書の CRUD・バージョン管理）・FR-09（属性・タグ）・FR-12（正規化変換）
- ユースケース（UC）: UC-03（文書を管理する）・UC-04（データソースを登録・同期する）・UC-06（文書を正規化変換する）
- 画面（SC）: **SC-05**（文書管理）・**SC-06**（データソース管理）・**SC-07**（変換ジョブ）
- 計画書リンク: `projects/microservices-platform/05_screens/01_screens.md` §SC-05 / §SC-06 / §SC-07、
  `05_screens/mockups/hi-fi/sc-05.html` / `sc-06.html` / `sc-07.html`（planning `d980a01`）

## 現状（計画書の記述 / As-Is）

実測はすべて対象コミット `de55761`（`origin/develop`）に対するものである。

| # | 画面 | 計画の記述 | 現在の API 契約（実測） |
| --- | --- | --- | --- |
| 1 | SC-07 | §主要素「**人手補正の2ペイン編集**（左=変換結果の編集・右=原本プレビュー）」・§アクション「人手補正の保存。**補正結果は取り込みへ再投入する**」。hi-fi 425-430 が「補正して再登録」ボタンを描く | 変換ジョブ API は `GET /jobs`・`GET /jobs/{id}`・`POST /jobs/{id}/retry` の **3 本のみ**。**補正済み Markdown を受け取る口が無い**。また `ConversionJobDto` が持つのは `OriginalPath` と `MarkdownUri` という**参照**だけで、**原本・変換結果の本文を返す口も無い**。`retry` は計画自身が「変換を最初からやり直す」と定義しており編集結果を受け取らない |
| 2 | SC-07 | §主要素「**デッドレター状態の表示**」・§データソース「デッドレターの表示は `failed` の**内訳**として扱う」 | `ConversionJobDto(Id, SourceId, SourceType, OriginalPath, Status, Error, DocumentId, MarkdownUri, Attempts, CreatedAt, UpdatedAt)` に**デッドレターの標識が無い**。`Attempts` は試行回数であり「デッドレターへ送られたか」とは別（**上限値も契約に無い**ため回数からも導けない） |
| 3 | SC-06 | §アクション「**同期異常は警告表示**」・hi-fi 422「⚠ 再試行中（3/5）」・UC-04 例外「接続失敗時は再試行し、継続失敗はアラートする」 | `DataSourceDto.Status` は **`active` / `disabled` の 2 値のみ**（`DataSourceStatus`）。連続失敗回数・再試行上限・直近エラーを持つフィールドが無く、**同期の健全性が表現できない** |
| 4 | SC-06 | hi-fi 418・420-423 の「**次回同期**」列（毎日 03:00 / 毎時） | 同期は `DataSourceSyncHostedService` が**全ソース共通の間隔**（`DataSourceSync__IntervalSeconds`。既定 300 秒）で回す。**ソース別のスケジュールという概念が無い**（`DataSourceDto` にも `NextSyncAt` が無い） |
| 5 | SC-06 | hi-fi 422-423 の行操作「**設定**」・§主要素「コネクタ設定」 | `/bff/datasources` にあるのは 一覧・個別取得・**登録**・手動同期・無効化 のみで、**更新（`PUT` / `PATCH`）が無い**。登録済みソースの接続先・既定属性を変更する手段が画面から無い |
| 6 | SC-05 | §主要素「文書一覧…**変換状況**」・hi-fi 419・421-423 の「変換」列（✓ 完了 / ✕ 失敗） | `DocumentDto` に変換の情報が無い（`Status` は **公開ライフサイクル** `draft`/`normalized`/`published`/`archived`）。`ConversionJobDto` 側から結合しようにも、**失敗ジョブは `DocumentId` を持たない**（文書が生成されないため）——すなわち「✕ 失敗」は**原理的に文書行へ結び付かない** |
| 7 | SC-05 | §SC-05 入力表（`01_screens.md:232`）「タグ｜任意｜**複数選択**｜**既定タグ辞書に整合**」 | タグ辞書は `/bff/admin/authz` 配下（**システム管理者限定**）にあり、SC-05 の利用者（管理者・運用者）が引ける保証が無い。**実装は自由入力のチップ**であり、辞書整合の制約を持たない。画面仕様書の対応表では「する」判定の行（row 11）に隠れる**部分未実装**である |

**#7 は本記録の表題が数える「6 種」の外側**である（表題と planning#198 の題は **6 種のまま**とする）。
提案 7 に対応する As-Is をここに置いたのは、**提案だけが根拠なしで planning 側へ渡る状態を避ける**ためである。
なお #7 は「画面が要素を描けない」型ではなく、**「描いたが制約を満たしていない」型の部分未実装**である
——画面仕様書の対応表では row 11 が「する」に数えられ、**二値の判定では表に現れない**。

### #6 の補足（計画の内部に食い違いがある可能性）

02_requirements のトレーサビリティ表は **2026-07-24 に FR-12 の関連画面を SC-07 / SC-03 へ是正**し、
変更履歴に「**SC-05 はモックの FR バッジ準拠で対象外**」と明記している。
一方 05_screens §SC-05 §主要素 は「…変換状況」を残したままである。
**どちらが正かの裁定を求める**（前者が正なら hi-fi の「変換」列と §主要素 の記述を落とす、
後者が正なら #6 の契約拡張が要る）。

### 参考: 画面のアクセス制御にも差異がある（**本記録の主題ではないが同じ節に属する**）

05_screens §共通シェル は「SC-05/06/07 = **管理者**（管理）」とするが、実装は
`platform-admin` **または** `platform-operator` に開いている（実装側 `IADR-0039`・Accepted・2026-07-08。
「データソース・変換ジョブ・文書 CRUD はいずれも運用／コンテンツ管理者の職務」という判断）。
2026-08-04 の確定は**再変換の実行権限**についてのものであり、閲覧には触れていないため、
#503 は再変換だけを `platform-admin` に絞り、閲覧は据え置いた。
**運用者に管理系 3 画面の閲覧を許すかどうかの裁定を求める。**

## 提案（To-Be）

| # | 提案 | 影響先 |
| --- | --- | --- |
| 1 | **人手補正の投稿 API**（補正済み Markdown の受け取り）と、**原本・変換結果の本文取得 API** を定める。あるいは SC-07 の主要素から人手補正 2 ペインを外し、UC-06 代替フロー「変換結果を管理者が補正して再登録する」の実現面を別画面（例: SC-03 の編集）へ移す | 05_screens §SC-07・03_usecases UC-06 |
| 2 | `ConversionJobDto` へ**デッドレター標識**（あるいは失敗種別）を加える。加えないなら 05_screens §SC-07 から「デッドレター状態の表示」を外す | 05_screens §SC-07・実装（契約） |
| 3 | `DataSourceDto` へ**同期健全性**（連続失敗回数 / 上限 / 直近エラー）を加える。UC-04 例外フローの「継続失敗はアラートする」を画面が受けられるようにする。**あわせて琥珀（警告色）の充て先の裁定も求める**——モック間相違の確定 ②（`01_screens.md:125` / `:241`）が琥珀へ与えた意味は「同期異常」であり、契約が同期健全性を持たない現状では**充てられる状態が無い**（`disabled` は管理者が意図した正常な設定状態であり、⚠ を付けると計画が琥珀へ与えた意味とずれる）。実装は琥珀を**空けたまま** `disabled` を中立で示している | 05_screens §SC-06・03_usecases UC-04 |
| 4 | **ソース別スケジュール**をモデル化し `NextSyncAt` を返す。定めないなら「次回同期」列を計画から外す | 05_screens §SC-06 |
| 5 | **データソース更新 API** を定める（接続先・既定属性の変更）。定めないなら「設定」操作を計画から外す | 05_screens §SC-06 |
| 6 | **文書 → 直近の変換ジョブの対応**を返す契約を定める（あるいは `DocumentDto` へ変換状態を持たせる）。定めないなら §SC-05 §主要素 の「変換状況」を落とし、02_requirements の是正（FR-12 の関連画面から SC-05 を除く）と揃える | 05_screens §SC-05・02_requirements |
| 7 | **SC-05 のタグ辞書**: 計画は「タグ｜任意｜複数選択｜**既定タグ辞書に整合**」と定めるが、タグ辞書は `/bff/admin/authz`（**システム管理者限定**）にあり、SC-05 の利用者（管理者・運用者）が引ける保証が無い。管理系ロールが引ける照会口を定めるか、SC-05 での辞書整合の要求を緩める | 05_screens §SC-05・07_abac-attribute-model |
| 8 | **SC-05/06/07 の閲覧ロール**の裁定（管理者のみか、運用者も含むか）。含むなら 05_screens §共通シェル のアクセス制御の割当を更新する | 05_screens §共通シェル |

## 影響範囲

- **裁定までは画面に当該要素を実装しない。** 「押しても結果が変わらないボタン」「常に空の列」
  「保存先の無い編集欄」は、計画が画面へ与えた役割（管理者が状況を正確に把握し操作する）を
  むしろ損なう。とくに #1 は、管理者が「補正したつもりで何も反映されない」という
  **満たしたように見せて満たさない**形になるため、実装しないことが安全側である。
- 実装しないことは各画面仕様書の「hi-fi モックアップとの対応」表へ**行番号つきで明記**した
  （`docs/screens/SC-05_document-management.md` / `SC-06_datasource-management.md` / `SC-07_conversion-jobs.md`）。

## 実装側の付随作業（**計画の裁定は不要**）

| # | 事項 | 引き受け先 |
| --- | --- | --- |
| 付-1 | `docs/api/openapi.yaml` に `/bff/datasources` と `/bff/conversion/jobs` が無く、SC-06 / SC-07 が orval 生成フックに載らない（SC-03 の `/bff/documents` と同じ問題） | **#506**（射程を広げる） |
| 付-2 | 同 `AiAnswerDto.citations` の型が `SearchResultDto[]` だが、後段の実体は `CitationDto[]`（`number` / `snippet` / `sourceUri` を持つ）である | **#506** |
