# NFR-21, NFR-05, ADR-0002, ADR-0008, IADR-0471 (#1560): platform-infra の Postgres（全 DB ＋ globals）を
# 日次で age 暗号化し、クラスタ外の 2 か所へ置く CronJob。
# 永続化 overlay（deploy/local/infra-persistence。既定）が取り込む —— 永続化しない使い捨てスタック（PERSIST=0）では描かない。
# 🔴 deploy/local 専用（ローカル開発・PoC 環境）。本番のバックアップ設計ではない。手順は docs/operations/platform-infra-backup-runbook.md。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../script
  - cronjob.yaml
