{
  "_comment": [
    "FR-05, FR-09, UC-05 / Issue #517: 経路B（dev）の ABAC 属性辞書の初期投入データ。",
    "投入は scripts/seed-abac-policies.js が AuthorizationService の管理 API 経由で行う（直 DB 書き込みはしない）。",
    "値集合は計画 06_technical/07_abac-attribute-model.md の文書基本属性・利用者属性に合わせる。",
    "required は **すべて false** にしてある。/authz/attributes/validate を呼ぶ取り込み経路は現時点で存在しないが、",
    "将来 required=true を入れると既存の取り込み（属性を 1 つしか付けない）を落としうるため、",
    "必須化は実データ側が属性を備えてから行う（#516）。",
    "本ファイルは dev の初期値であり、実運用の属性辞書は SC-09（管理者設定）から編集する。",
    "ADR-0080 決定 1 / フォローアップ 1（#1323）: 利用者の tags は集合値である。値は交差判定でマッチする（決定 2）。",
    "projects は意図的に入れていない —— ADR-0085 決定 1・2 が必須化と判定軸をいずれも保留しており、",
    "辞書に無ければ ValidatePolicy が projects を条件に持つポリシーを作らせない（保留を機械で守る）。"
  ],
  "attributes": [
    {
      "key": "confidentiality",
      "label": "機密区分",
      "scope": "document",
      "required": false,
      "allowedValues": ["public", "internal", "confidential", "restricted"]
    },
    {
      "key": "department",
      "label": "所管部門",
      "scope": "document",
      "required": false,
      "allowedValues": ["engineering", "sales", "hr", "finance", "legal"]
    },
    {
      "key": "lifecycle",
      "label": "状態",
      "scope": "document",
      "required": false,
      "allowedValues": ["draft", "active", "archived"]
    },
    {
      "key": "clearance",
      "label": "取扱可能区分",
      "scope": "user",
      "required": false,
      "allowedValues": ["public", "internal", "confidential", "restricted"]
    },
    {
      "key": "department",
      "label": "所属部門",
      "scope": "user",
      "required": false,
      "allowedValues": ["engineering", "sales", "hr", "finance", "legal"]
    },
    {
      "key": "tags",
      "label": "業務タグ",
      "scope": "user",
      "required": false,
      "allowedValues": ["engineering", "sales", "hr", "finance", "legal", "security", "support"]
    }
  ]
}
