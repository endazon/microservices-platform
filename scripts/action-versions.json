{
  "$comment": [
    "キットが配布するワークフローで使う GitHub Actions の**メジャーバージョンの下限**。",
    "check-action-versions.js がこの表と `uses:` を突き合わせ、下回っていれば CI を落とす。",
    "Dependabot は github-actions エコシステムではリポジトリ直下の .github/workflows/ しか",
    "走査しないため、テンプレート配下は自動追随しない（issue planning#148）。この表が単一情報源である。",
    "更新方法: node scripts/check-action-versions.js --check-latest で新しいメジャーを確認し、",
    "テンプレートの uses: と本表を同時に上げる。"
  ],
  "expected": {
    "actions/checkout": 7,
    "actions/setup-node": 7,
    "actions/setup-dotnet": 6,
    "actions/upload-artifact": 7,
    "actions/dependency-review-action": 5,
    "peter-evans/create-pull-request": 8,
    "softprops/action-gh-release": 3,
    "gitleaks/gitleaks-action": 3,
    "actions/cache": 6,
    "actions/download-artifact": 7,
    "actions/github-script": 8
  },
  "$exempt": {
    "github/codeql-action": "リリースタグが codeql-bundle-vX.Y.Z 形式でメジャー番号を引けない。実装リポ側の Dependabot が個別に pin する運用のため、テンプレート既定は v4 のままでよい。",
    "anthropics/claude-code-action": "🔴 40 桁の commit SHA で固定しているためメジャー番号を引けない（#1352）。浮動タグ @v1 の指す先が動いた版が Claude Code の CLI を導入できずに落ち、必須チェック claude-review が全 PR で落ちてマージ不能になった実測がある。固定と 2 か所の版の一致は scripts/check-ai-workflow-config.js が検査する（本表ではない）。"
  }
}
