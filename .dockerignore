# FR-14: 全 Dockerfile がリポジトリルートを context にするため、ホストのビルド生成物・
# 依存キャッシュを context から除外する（イメージ内で restore / pnpm install --frozen-lockfile を
# 実走する。#591: 従前は「npm ci」と書いていたが、フロントは IADR-0121 決定 2 で pnpm へ移行済み）。
.git
**/node_modules
**/dist
**/coverage
**/playwright-report
**/test-results
**/bin
**/obj
**/*.tsbuildinfo
