---
title: 引継資料 — issue 消化フェーズ運用の現在地と、繰り返し起きる事故の型
type: how-to
status: active
created: 2026-08-08
updated: 2026-08-28
author: Claude
---
<!-- trace:
ids: [FR-05, FR-06, FR-09, FR-12, FR-19, FR-20, FR-21, SC-05, SC-07, SC-09, SC-19, SC-20]
adrs: [ADR-0056, ADR-0057, ADR-0058]
iadrs: [IADR-0061, IADR-0277, IADR-0278, IADR-0115, IADR-0116, IADR-0120, IADR-0139, IADR-0140, IADR-0141, IADR-0144, IADR-0145, IADR-0146, IADR-0147, IADR-0180, IADR-0204, IADR-0238, IADR-0279, IADR-0280, IADR-0282, IADR-0286, IADR-0287, IADR-0291, IADR-0292, IADR-0293]
specs: [20260808_session-handoff, 20260815_issue-454_open-issue-stocktake-and-waves, 20260823_planning-adr-0056-0058-followup, 20260827_all-issues_wave-plan, 20260828_wave2-audit-followup, 20260828_wave3-audit-followup, 20260828_wave4-sweep, 20260828_wave45-vsa-migration, 20260828_issue-1025_notification-service-deployment, 20260828_issue-1020_internal-mcp-tools, 20260828_issue-1040_integration-config-timing-checker]
issues: [#443, #454, #555, #556, #562, #572, #612, #614, #617, #618, #701, #743, #752, #779, #780, #781, #782, #783, #791, #836, #901, #941, #953, #1011, #1018, #1020, #1021, #1022, #1023, #1025, #1032, #1033, #1034, #1036, #1037, #1038, #1040, #1041, planning#380, planning#470, planning#471, planning#472, planning#473, planning#474, planning#475, planning#479]
-->

# 引継資料 — issue 消化フェーズ運用の現在地

## 目次

- [1. 進め方（利用者裁定・2026-08-08）](#1-進め方利用者裁定2026-08-08)
- [2. 現在地（**スナップショット**）](#2-現在地スナップショット)
- [3. 絶対に破らない制約](#3-絶対に破らない制約)
- [4. 検査器と、それが実際に走る経路](#4-検査器とそれが実際に走る経路)
- [4.5 ★ 環境によって変わること —— **着手前に自分で実測する**](#45--環境によって変わること--着手前に自分で実測する)
- [5. ★ このリポジトリで繰り返し起きる事故の型（8 型）](#5--このリポジトリで繰り返し起きる事故の型8-型)
- [6. 裁定・方針決定が要る論点（実装側では決められない）](#6-裁定方針決定が要る論点実装側では決められない)
- [7. 引き継いだ人が最初にやること](#7-引き継いだ人が最初にやること)

> **この文書の位置づけ**: 全面再実装の issue 消化を引き継ぐ人／AI が、**最初に読む 1 枚**である。
> 規約の正は `CLAUDE.md` と `.claude/rules/traceability.md`、決定の正は `.ai-context/adr/` にある。
> **ここへ規約や決定を転記しない** —— 転記すると正が動いたとき黙って腐る（「参照点を 1 つに畳む」）。
> 本書が持つのは**現在地と、繰り返し起きている事故の型**だけである。

## 1. 進め方（利用者裁定・2026-08-08）

| 事項 | 決まっていること |
| --- | --- |
| **フェーズ** | **複数の issue をまとめて処理する作業のまとまり**。1〜2 件に細かく割らない |
| **PR** | **束ねない。1 issue = 1 PR**（全面再実装の進行方式・規約 1。変更なし）。**例外は、同型の契約追加をドメイン単位で束ねる実装 ADR の決定 1 が挙げる 6 条件をすべて満たすものだけ**（資源単位。**上限の値はここに書かない** —— 正は下記の 2 つで、写すと片方が腐る。実際に腐った: #791 で 2 件 → 4 件へ改まったとき、本行だけが取り残された）——［2026-08-08 追記 / フェーズ末クロス監査］#624 が上流ガイドを写した際に条件を落として一般化し、本行と真逆に読める状態になったため、両側から同じ範囲を指すよう明示した。正は `CLAUDE.md` の該当行と、束ね方を定めた実装 ADR である |
| **クロス監査** | **フェーズ末に 1 回**（`adr-guardian` ＋ `traceability-auditor`）。**PR ごとには走らせない**。条文は、クロス監査の巡数を定めた実装 ADR の決定 4（2026-08-08 追記）である |
| **フェーズ内** | **並行してよい**。ただし同じファイルを触る issue は直列にする（下記 §5） |
| **マージ** | **レビュー指摘がすべて対応済みだと確認できたら可**。承認待ちは不要 |

## 2. 現在地（**スナップショット**）

> **★ この節だけは必ず古くなる。** issue / PR の生きた状態は **GitHub が正**である。
> ここが食い違ったら GitHub を信じ、**気づいた人がこの節を直す**（機械検査は無い）。
> 本書の他の節（§1 の裁定・§3 の制約・§4 の結線・**§5 の事故の型**）は陳腐化しにくい。
> **引き継ぐ人にとって価値があるのは §5 であって §2 ではない。**

### フェーズ A — 記録・規約・検査器の是正（**完了**）

| issue | PR |
| --- | --- |
| #590 | #603 |
| #576 | #604 |
| #591 | #608 |
| #514 | #607 |
| #563 | #610 |
| #581 | #606 |
| **#609**（本セッション中に発見・起票） | #611 |
| #579 | #612 |

**8 件すべて develop へ着地済み。フェーズ末のクロス監査を 1 巡実施し、是正まで完了した**
（`adr-guardian` ＋ `traceability-auditor` を同時に。クロス監査の実装 ADR の決定 2〜4）。
**両監査とも「これ以上の全面巡回は不要。2 巡目は是正差分のみ」と打ち切りを明言した。**
是正は **PR #615**（指摘 17 件を全件、自分で実測して裏を取ってから対応。§5 型 7・型 8 はここで得た）。

> **★ 監査へ渡す母集合も引き直すこと。** 私は **6 PR** を渡したが `43cf6c4..2cd8508` の実体は
> **8 コミット**で、2 件落ちていた（両監査とも range から引き直して 8 件を見た）。
> **「issue 本文の一覧は母集合ではない」は監査の指示にも当てはまる。range を渡す。**

- **#605**（CHANGELOG 自動更新の bot PR）は **CI が 1 件も起動しない**（bot が `GITHUB_TOKEN` で
  push するためワークフローが再帰起動しない GitHub の既定挙動）。必須チェックが走らないので
  そのままでは緑にならない。**放置してよい** —— CHANGELOG は develop が動くたび再生成され、
  実装側の作業中にも `node scripts/gen-changelog.js --out CHANGELOG.md` で作り直している。

### フェーズ B — **完了**（2026-08-08）

| issue | PR |
| --- | --- |
| **#555**（画面からの `apiFetch` 再混入） | #614（再混入を ESLint で止める実装 ADR を新設） |
| **#554**（分割スモークの空振り） | #616 |
| **#556**（`manualChunks` の規則検査） | #618（規則欠落の検出方法を定める実装 ADR を新設） |
| **#562**（フロントの format ゲート） | #619 |

> **★ 上表は「issue の一覧」であって母集合ではない。** フェーズ B 末クロス監査は
> **`2cd8508..ae66549` = 9 コミット**に対して実施した（#613 / #615 / #620 / #621 / #624 を含む）。
> 是正は `.ai-context/specs/20260808_phaseB-audit-followup.md`。
> ［2026-08-08 追記 / フェーズ末クロス監査］**引き継ぎ時に渡された range（`96c2dbe..74d9d7f`）は誤りで、
> #614（再混入検査の実装 ADR 新設）を含む 3 コミットが落ちていた** —— §5 型 1 の 3 度目の再演である。

> **★ #554 / #556 / #562 は、いったん「AI だけでは完結しない」として保留された。**
> 根拠は当時の §4.5（`build` / E2E が動かない・`workflow` スコープが無い）だったが、
> **それは環境固有の観測であって、リポジトリの制約ではなかった** —— 別環境では 3 件とも実施でき、
> **同日中に着地した**。§4.5 はこの反省を受けて全面改稿してある。
> **「できない」と書かれていても、まず自分で実測すること。**

> **★ #555 が採番で足止めされた記録**: 新設した `IADR-0146` に対し `IADR-0145` が **#612 の中にあり
> 未マージ**だったため、`check-adr-numbering.js` が**欠番**で落ちた。**採番検査の実装 ADR の決定 3 が
> 「並行 PR の衝突は着地後にしか見えない」と書いた型そのもの**であり、**自分で作った検査器に
> 自分で捕まった**。#612 の着地後に develop を取り込み、索引行を足して解消した。

### 波（実施順）

> **★ この表は順序しか持たない。** 判定・根拠・除外理由は**層 a**（下記の棚卸し）が持ち、
> **PR 番号・open / closed・CI 状態は GitHub が正**である。
> **列を足さないこと** —— 人が更新する台帳を作れば、更新漏れという同じ失敗が別の場所で再発する（単一情報源を機械が読む形にする方針）。
>
> **層 a（判定・根拠・除外理由の正・不変）**:
> [`.ai-context/specs/20260815_issue-454_open-issue-stocktake-and-waves.md`](../../.ai-context/specs/20260815_issue-454_open-issue-stocktake-and-waves.md)

| 波 | issue |
| --- | --- |
| **波 0** | 環境準備・blocked の測り直し・`decision-needed` の起票 |
| **波 1** | #747 / #749 |
| **波 2** | #748 |
| **波 3** | #756 → #757（直列） |
| **波 4** | #754 / #493 |
| **波 5** | XL 群（**依存順序と着手ゲートは層 a を見よ**）。うち **#442 は子 5 件へ分割済み**。**それ以外は子 issue を起票しない** |

- **波は §1 の「フェーズ」と同じ単位である**（呼称だけを変えた）。
- **上の行に現れない OPEN issue は波 5 に属する。** 親から切り出した子 issue は**親と同じ波**に属する。
- **［2026-08-16 / #778］#442 の着手ゲートが 3 軸とも開いたので分割した。** 層 a §8 が
  「子 issue を起票しない」理由 ① に付した覆る条件（「dotnet が入り #455 / #442 のどちらかに
  着手できるようになった時点」）が満たされたためである。**#455 も 3 軸 ○ になったが、まだ分割していない**
  （着手しないうちに割ると、裏取りのない粒度が次の担当者に引き継がれる。同じ理由 ①）。
  **子 5 件の依存順序は層 a の同日追記が持つ。ここへ書かない** —— この表は順序しか持たず、
  依存グラフを 2 箇所に置けば片方が黙って腐る（単一情報源と「参照点を 1 つに畳む」方針）。
  **実際に本 PR の中で 1 度ドリフトした**（最初の並びが各子 issue 本文と食い違い、レビューで是正した）。

### ［2026-08-23］オープン issue の一括消化（進行中）

**オープン 65 件を全件トリアージし、リポジトリ内で完結する 28 件を対象と判定して着手した。**
判定・除外理由の正は作業仕様書側にあり、**生きた状態は GitHub が正**である（この節は必ず古くなる）。

> 🔴 **この運用は §1 の「束ねない。1 issue = 1 PR」と正面から食い違っている。**
> **食い違いを解消する条文はまだ無い。** 作業ブランチがセッションの制約として 1 本に固定された
> ためだが、**「そうせざるを得なかった」は条文を改定しない。** 束ねの限定例外は 2 つあり、
> どちらも上限 4 件で、片方は `src/**`・`.github/**`・`CLAUDE.md`・`deploy/**` が
> **1 件でも入ったら束ねられない**と明記している —— 本作業はその全項目を踏んでいる。
>
> **必要なのは、第 3 の限定例外を定める新しい実装 ADR と、その根拠となる利用者裁定である**
> （既存の例外を足したときも、裁定と新 ADR の両方を要した）。**それが無い間は、この運用は
> 記録された例外ではなく、記録された逸脱である。** 次に同じことをする人は、まず裁定を取ること。
>
> 代わりに置いた歯止め: **1 コミット = 1 issue = 1 論理変更**を守り、件名を `種別(起点ID): 要約`
> に揃えて**コミット単位で issue を追える**ようにした。監査も「束ねても監査の対象数は減らない」に
> 従い、コミット範囲を母集合として渡している。

> 🔴 **対象外と判定した 37 件は、着手のたびに引き直すこと。** 実測で引き直したところ
> **7 件が ACTIONABLE へ変わっていた** —— 同じセッションの着地がそれらの待ち条件を解いていたためである。
> **「前に見たときは対象外だった」は根拠にならない。** 待ち先が閉じた瞬間に対象は増える。

この作業で新たに分かった、**次に引き継ぐ人が踏みやすい 3 点**:

- **並行するサブエージェントの未コミット変更が、検査の測定値へ混ざる。** 特に共有ファイル
  （ロックファイル・`.slnx`・`Directory.*.props`・各種 baseline）は、**staging の前に中身を見る**。
  数値を baseline へ焼くときは**コミット済み状態だけを展開したクリーンな worktree で測る** ——
  混ざった数字を床にすると、後から誰も再現できない。
- **その worktree は submodule を引き継がない。** フロントのビルドは submodule のワークスペースを
  解決できずに落ちるので、測る前に初期化する。**落ちたのを「壊れている」と読み違えないこと。**
- **`git checkout` はローカル変更があると abort する。** 検証用 worktree で
  「新しいコミットを測ったつもりが、abort されて古い方を測っていた」を 1 度踏んだ。
  **切り替えた後に `git log -1` で、測る対象を目で確かめる。**

### ［2026-08-23］計画側の裁定 6 件への追随（**環流を出した側が追随を負う**）

同セッション中に起票した環流 6 件へ、**すべて裁定が下りて計画へマージされた**。
新設された計画 ADR は 3 本（`ADR-0056` / `ADR-0057` / `ADR-0058`）である。

🔴 **裁定を受け取ったら、まず宣言レンジを引き直すこと。**
`.claude/rules/traceability.repo.md` の宣言レンジは `check-commit-messages.js` と
`check-trace-blocks.js` が**実在性を fail-closed で検査する一次情報**であり、遅れたままだと
**新しい ADR を引いた瞬間に CI が理由の分かりにくい形で落ちる**。
**環流を出した側は、裁定がレンジを動かすことを起票の時点で予測できる**（別紙の世代記録も参照）。
検査器が教えるのは「踏んだこと」だけで、「遅れていること」は教えない。

🔴 **裁定文が書いた「実装の現状」を、そのまま真として受け取らないこと。**
`ADR-0056` は「実装側に一律の書き換えは生じない」と結論していたが、**実測すると前提が
成立していなかった**（対象 4 経路は読み取り認可を一度も評価していなかった）。
**計画側は実装リポジトリを直接読めないため、現状認識は環流に書かれたことだけで作られる。**
誤りの半分は起票側にあった —— 打ち分けの形は正しく書いたが、
**「何が評価されていないか」を書いていなかった**。

> **環流は「何がどう書かれているか」だけでなく「何が評価されていないか」まで書く。**
> 前者だけだと、計画側は「確かめたうえでそう選んだ」と読む。

### ［2026-08-28］全 open issue（72 件）一括対応（進行中・波方式）

**利用者の依頼で open issue 全件を波方式で消化中である。** 束ねは、**波単位の積み上げ PR を
第 3 の限定例外として認める実装 ADR を新設し、利用者裁定を根拠として記録したうえで**行っている
—— 2026-08-23 節の 🔴（記録された逸脱）は**この形で条文化して解消した**。条文の正はその実装 ADR、
判定・波割り・除外理由の正は統括の作業仕様書（層 a 相当・`.ai-context/specs/` の 2026-08-27 付）、
**生きた状態は GitHub（積み上げ PR と各 issue）が正**である。ここへ転記しない。

この運用で新たに実測した、引き継ぐ人が踏みやすい 3 点:

- **バックエンドの実走は「SDK が無い」ではなく「PATH に無い」ことがある。** §4.5 のとおり
  諦める前に実測する（本環境では導入済み SDK を PATH へ通すだけで build / test / format が全部動いた）。
- **`scripts.repo.test.js` は companion であり、単体起動では 1 件も検査せず fail する。**
  §4 の表のとおり `scripts.test.js` 経由（`REQUIRE_REPO_TESTS=1`）で走らせる。
- **コンテナ再起動でサブエージェントの worktree が消えることがある。** 成果はブランチに
  コミットさせておけば cherry-pick で回収できる（実測 2 回）。**未コミットの成果は消える。**

もう 1 つ、**この波で 2 度目に踏んだ罠**（型 1 の派生）:

- **「不変にする」を「その項目を送ってきたら拒否」と実装すると、正常系が全部壊れる。**
  文書の属性編集フォームは**既存属性をスプレッドして送る**ため、機密区分だけを変える保存でも
  `doc_scope` が同送される。**判定は「既存値と一致するか」で行う。**
  壊れ方は「機密区分が変えられない」という無関係な形で出るので、原因にたどり着きにくい。
  **陽性対照（同値の同送が通る）が無いと、この実装は陰性だけで緑になる。**

### ［2026-08-28］オープン issue 一括対応 第 2 弾（PR #1021）

> **上の「波（実施順）」表とは別の波番号である。** あちらは #454 の棚卸し（層 a）が持つ順序、
> こちらは 2026-08-27 のオーナー指示「オープン issue 全件を計画して実施する」の作業単位。
> **混ぜないこと。**

`.ai-context/specs/20260827_all-issues_wave-plan.md` が統括。波 0〜4.5 まで着地済み。

| 波 | 内容 | 記録 |
| --- | --- | --- |
| 0 | 帳簿の正常化（実装済み 18 件をクローズ） | 束ね例外の実装ADR |
| 1 | 認可 action 必須化・属性供給・k8s テーマ・被覆・アーキ土台 | 8 要素実体化の実装ADR（後に改定） |
| 2 | メッセージング解錠（E3a/E3b の Wolverine 切替）→ 削除伝播・ABAC デノーマライズ・リンク抽出・二段検索 | `20260828_wave2-audit-followup.md` |
| 3 | 文書管理／個人資料 BFF・個人資料の 2 画面・通知結線・検索観測・ルート検査 | `20260828_wave3-audit-followup.md` |
| 4 | 既定資格情報の撤去・xUnit1051 完了・床のラチェット | `20260828_wave4-sweep.md` |
| **4.5** | **14 サービスを単一プロジェクト＋VSA/DDD へ移送**（オーナー裁定） | 移送標準の実装ADR / `20260828_wave45-vsa-migration.md` |

**波 2・3 のフェーズ末クロス監査は重大 0**（指摘は全件処置済み）。

**この期間に決めた「やらない」判断**（次に同じ提案が出たら、まずここを読む）:

- **仕様書 `status` の据え置きに機械検査を置かない**（棄却を記録した実装ADR がある）。3 案を PR の 144 コミットへ
  総当たりで当てて実測し、**どれも「動機になった事故を捕まえる」と「正常な途中状態を誤検知しない」を
  両立しなかった**（PR 範囲案は 33% 誤検知しつつ動機の 1 件を取り逃がす）。
  実体は `draft` 滞留 **46 件**の系統的な放置であり、#1023 へ回した。
- **xUnit1051 の器（`NoWarn` / `WarningsAsErrors` の PropertyGroup）を完了後も消さない**
  （段階採用の実装ADR に 2026-08-28 の追記がある）。消すと移行済みが `WarningsAsErrors` を失って**再発が warning へ落ち**、
  同時に AST が `NoWarn` を失って警告ノイズが復活する。**「移行の完了」は器の役目を終わらせない。**

**この期間に自分で入れて自分で見つけた欠陥**（レビューが 2 回続けて指摘した）:

- **`ESO=1` で `postgres-app` の供給元が 0 本になっていた。** 手動 apply を `ESO != 1` ブロックへ
  入れることは「ESO 側が供給する」と約束することだが、対応する `externalsecret-*.yaml` を作っていなかった。
  **配備側の注入経路は helm 1 本ではなく、ローカル k8s に既定と ESO の 2 本ある**（既定資格情報の実装ADR の追記）。

### 波 5-1（2026-08-28・PR #1034 で進行中）

| 波 | 主題 | 閉じたか |
| --- | --- | --- |
| 5-1 | 仕様書 `status` の実態突合 46 件 ／ HelmChartConfig の反映失敗を落とす変異試験 ／ 版応答の本文契約の是正 ／ 辺の型辞書 DB 層防壁の器 ／ 通知サービスの配備 ／ 共有ライブラリの被覆 | **3 件のみ**（他は下記の理由で据え置き） |

**閉じなかったものと、その形**（この 3 つは同じ型である ——**「作った」と「効いていることを確かめた」は別**）:

- **防壁の器を直したが、防壁が発火したことは確かめていない**（Docker daemon が無く 6 件すべて `Skipped`）。
  実走は `integration.yml`（develop への push 時）が初回である。**緑・0 件・skip はいずれも「測った証拠」にならない。**
- **配備定義は入れたが、Pod が Ready になることも通知が届くことも測っていない**（k8s が無い）。
  **作業仕様書が `done` になっても issue の受け入れ基準は満たされていない** —— 当該仕様書の冒頭にその旨を追記した。
- **被覆は上げたが床（ratchet）を上げていない**ので改善が固定されていない。床の母集合は
  `src/` 配下の全 Cobertura の合算だが、統合テストが skip される環境ではキー総数が CI と大きく違う（12368 対 7801）。
  **床は CI（Release・全レポート）の実測を取ってから上げる。**

**この期間に自分で入れて自分で見つけた／レビューに見つけられた欠陥**:

- **索引タイトルの 200 字上限に 2 回落ちた。** 検査器（`scripts.repo.test.js`）は既に在り CI も捕まえるが、
  **`REQUIRE_REPO_TESTS=1` を付けないとローカルで走らない**。統合前に必ず付けて走らせること（検査器は足していない）。
- **`check-chunk-budget` が `develop` 単独で 1 バイト超過していた**（実測 602250 / 床 602249）。
  表示が `602.25 kB > 602.25 kB（+0.00 kB）` で**同じ値どうしの比較に見える**ため、
  **バイト単位で測るまで自分の PR の失敗と区別できなかった**。base で再現するかを先に確かめること。
- **集約ゲートの「失敗」は 2 回とも中断だった。** `image-build` / `build-and-test` のような集約ジョブは
  `BUILD_RESULT` が `cancelled`（後続 push で前の run が打ち切られた）でも「失敗したサービスがある」と表示する。
  **集約ゲートが赤いときは、まずジョブログの `BUILD_RESULT` を見る** —— `cancelled` なら中断であって壊れていない。
  必須チェックとしては中断を pass にしない方が正しいので、検査器側は直していない。
  **短い間隔で push を重ねないこと**（原因はこちら側にある）。
- **束ねの根拠を PR 本文に書いていなかった。** 波単位の積み上げ PR を認める限定例外は
  **4 条件（W-A〜W-D）**を課しており、そのうち **W-C（issue ↔ コミットの対応表と「閉じない理由」）**が欠けていた。
  AI レビューは例外の存在自体を見落として「規約違反」と判定したが、**その過程で W-C の未充足を正しく炙り出した。**
  **反論する側も、指摘の中に生きている部分が無いかを先に探すこと。**

### 波 5-2 / 5-3（2026-08-28・PR #1036 / #1041。**ここで一区切り**）

**着手時 72 件のオープン issue が 40 件になった。** `develop` は `9ae1136`。
**`integration.yml` が #1021 の着地以来はじめて全面的に緑**（全 19 テストプロジェクト・Failed 0）。

| 波 | 主題 | 閉じた issue |
| --- | --- | --- |
| 5-2 | ブローカ既定資格情報の撤去 ／ 統合テストの器の接続文字列 ／ 画面の陳腐化是正 | #1022 #1032 |
| 5-3 | 各サービスの MCP ツール自己申告 ／ 統合スタック診断の穴 ／ タグ移行試験の脆さ ／ 構成注入タイミングの検査器 | #1020 #1037 #1040（＋ #941） |

**「赤が別の赤を隠していた」** —— 28 件がホスト起動に失敗していた間（#1032）、その中に含まれる
#1037 と #1038 は**テスト本体に到達していなかった**。#1032 を直して初めて 2 件が露出した。
**赤を 1 つ直したら、その陰にあったものを数え直すこと。**

**この期間に自分で踏んだ／レビューに見つけられた事故**:

- 🔴 **「実データで緑」を検出力の証拠にした。** #1040 の検査器の初版はテスト木全体の設定注入を
  1 つの集合に集めており、**実データでは緑だったが、基底フィクスチャから 3 件のキーを 1 つずつ外す
  変異試験が 3 件とも生存した**（同じキーを別の器も与えていたため）。器（host group）ごとに数える形へ
  作り直して 4 件すべて KILLED。**§5 型 4 の新しい実例である** —— 変異試験を回していなければ、
  検出力ゼロの検査器を「置いた」と記録して終わっていた。**変異試験そのものを `scripts.repo.test.js` へ入れた**ので、
  将来「緑だが検出力ゼロ」へ退行すればそこで落ちる。
- 🔴 **走査を `head` で切って結論を出した。** #443 の残作業を見積もる際、`grep -rl … | head` の出力から
  「ナレッジ健全性は未実装」と判断したが、**`src/ai-stock-trading/…` が先に並んで本体の一致が切り落とされていた**
  だけで、実際は実装済みだった（エンドポイント・DTO・マイグレーション・テストすべて在る）。
  **母集合の規則「走査の出力を加工して読まない」の 3 度目の実例である。**
- 🔴 **診断が、最も必要な場面でだけ空になっていた。** 統合スタックの失敗時ログ採取は Ready な pod を
  skip するが、seed と導線検証は **readiness を通過した後**に走る。つまり「**全 pod が Ready なのに
  ジョブが落ちた**」ときだけ 1 行も出ない。**「異常時に情報を出す」仕掛けは、異常の起き方ごとに
  当てはまるかを確かめること。**
- 🔴 **PR 本文の更新を push より後にしていた。** AI レビューは push の瞬間に本文を読むため、
  **常に 1 つ前の本文を見る**。同じ指摘（本文と diff の食い違い）を 2 回受けた。原因はレビュー側ではない。
  **以後、本文の更新は push より前に行う。** 機械検査は置かない —— 「閉じない理由」は本質的に散文であり、
  本文と diff の一致は機械判定に落ちない。

**残っている主なもの**:

- **#1033** —— 統合スタックの `POST /documents` が **500**。**原因は未特定**。
  上記の診断の穴を塞いだので、次の実行で初めて依存を名指しできる。
  なお本 issue は**別原因の失敗を 2 系統抱えている**（起動待ちのタイムアウトと本件）ことを実測で切り分けた。
  自動起票は `marker_key` で同じ issue へコメントを足すため、**別原因が同居することを前提に読むこと。**
- **#1038** —— fan-out の負荷依存な失敗。**2 回連続で再現しなかったが、再現しないことは直った証拠にならない。**
- **#443** —— 調査の結果**ほぼ実装済み**だった（用途別・モデル別の計測、有効期間つき単価表、
  ダッシュボード、ナレッジ健全性の口）。**残りは 4 条件の突合であって実装ではない。**

## 3. 絶対に破らない制約

- **`force push` / `reset --hard` / `rm -rf` を使わない**（hooks が実際にブロックする）。
  履歴は不変。誤記の是正は `scripts/changelog-overrides.json` の `remap`（**生成物のみ**）。
- **`planning/` は pin 更新のみ**。計画書そのものを本リポから書き換えない。
- **`src/ai-stock-trading` を変更しない**（別プロジェクトの submodule）。
- **作業仕様書（`.ai-context/specs/`）なしで実装へ着手しない。**
- **モデル ID をコミット・PR・コード・文書へ書かない。**
- **サブエージェントに push させない**（親が監査してから push する。**本セッションで 1 度破られた**）。
- **コミット前に `git status` の削除（`D`）を確認する。**

> **［2026-08-08 / #617］「`.github/workflows/` は編集できない」を本節から外し、§4.5 へ移した。**
> 従前ここには「**GitHub App 権限で編集できない**。新しい検査は既存の呼び出し口へ相乗りさせる」
> という項があったが、**それは認証に依る観測であって、どの環境でも破らない制約ではない**
> （`workflow` スコープを持つローカル認証では編集できる。**`CLAUDE.md` は #163 以来この形で書いている**
> —— 「GitHub App 権限では編集不可。ワークフロー変更はローカルの `workflow` スコープでコミット/プッシュする」。
> **断定を落として片側だけ写したのが本書だった。**）。
> **本節に置いたままだと、引き継ぐ人が実測せずに結線を諦める** —— #562 で実際にそうなった（§5 型 8）。
> 能力の判定は §4.5 の表 2 行目で実測する。**編集できない環境での回避策（既存の呼び出し口への相乗り）は
> 既存の呼び出し口へ相乗りするという実装判断のとおりで、既存の結線は §4 の表にある。**

## 4. 検査器と、それが実際に走る経路

**既存の検査は、`.github/workflows/` を編集できない環境で作られたため既存ジョブへ相乗りしている**
（既存の呼び出し口へ相乗りする実装判断）。**この相乗りは既存の結線の由来であって、新しい検査への制約ではない** ——
編集できる環境なら新しいジョブを足してよい（#562 が実際に `frontend.yml` へ足した）。
まず §4.5 で実測すること。「検査器を作った」と「CI で走る」は別である。

| 検査器 | 走る経路 |
| --- | --- |
| `check-doc-links.js` | `ci.yml` の `doc-links` |
| `check-commit-messages.js` | `ci.yml` の `commit-messages`（**レンジモード**）／`pr-title.yml`（**`--title` モード**） |
| `check-cross-repo-refs.js` | 上記 `check-commit-messages.js` 経由 ＋ 単独 |
| `check-plan-id-qualification.js` / `check-adr-numbering.js` / `check-landed-subjects.js` / `check-i18n-catalogs.js` / `check-test-spec-coverage.js` | **`scripts/scripts.repo.test.js` 経由**で `ci.yml` の `scripts-tests`（`REQUIRE_REPO_TESTS=1 node scripts/scripts.test.js`） |
| ESLint（`src/eslint.config.js`） | `frontend.yml` の `lint` |

- **`scripts/scripts.test.js` は分類 A（変更禁止・キットとバイト一致）である。**
  **テストを足すときは必ず companion の `scripts.repo.test.js`** へ書く。
  この行は 2 度動いた —— #790 で「拡張点を埋めた配布先では原理的に通らない」試験があったため
  分類 B（X・期限つきの暫定）へ落ち、**#836 で計画側への環流が着地して A へ戻った**
  （キット追随の保留を機械検査する実装 ADR の決定 4）。**次に赤くなったら、まず「キットが配布先の構成を断定していないか」を疑う。**
- **計画 ADR（`ADR-xxxx`）の実在性は CI で守られていない**（どの決定的ジョブも submodule を populate しない）。

## 4.5 ★ 環境によって変わること —— **着手前に自分で実測する**

> **［2026-08-08 全面改稿 / #617］本節は従前「この実行環境でできないこと（実測）」という表題で、
> 環境固有の観測を恒久的な制約として書いていた。それが実害を出した** ——
> §2 は #554 / #556 / #562 の 3 件を「**AI だけでは完結しない**」として保留し、
> フェーズ B を打ち切る根拠にしていたが、**3 件とも別の環境では実施できた**（同日中に 3 件とも着地）。
> **「できない」と書かれた項目は、引き継ぐ人が実測せずに諦める。**
> したがって本節は**能力の一覧ではなく、実測の手順書**として書く。

**下の 4 つは環境ごとに違う。作業を諦める前に必ず自分で実測すること。所要は合計 5 分程度である。**

| 能力 | 実測コマンド | 使える場合の出力 | 使えない場合 |
| --- | --- | --- | --- |
| **`gh` CLI** | `gh auth status` | `✓ Logged in to github.com` | コマンドが無い／未認証 → MCP ツール（`mcp__github__*`）を使う |
| **`.github/workflows/` の編集** | `gh` が在れば `gh auth status`（Token scopes の行）。**`gh` が無い環境では、捨てブランチでワークフローを 1 行変えて `git push` してみる**（下記） | scopes に **`workflow`** が在る ／ push が通る | 拒否される → 結線は人手へ渡す |
| **submodule の populate** | `git submodule update --init src/ai-stock-trading planning` | `Submodule path ...: checked out` | 認証エラー → フロントの `build` / E2E と計画 ADR の実在性検査が不可 |
| **フロントの `build` / E2E** | 上の populate 後に `pnpm run build` ／ `pnpm run test:e2e` | ビルド成功 ／ **全件 passed（failed 0）** | `@ai-stock-trading/features` が `error TS2307`（＝populate に失敗している） |

> **`gh` が無い環境での workflow スコープの測り方**（上表 2 行目）:
> 2 行目の実測は 1 行目の結果に依存するため、`gh` が無いと測れないままになる。その場合は
> **捨てブランチで実際に押してみるのが唯一の確実な判定**である（拒否は push の時点で起きる）。
> ```
> git switch -c probe/workflow-scope
> printf '\n' >> .github/workflows/frontend.yml && git commit -am "chore(NFR): probe" && git push -u origin probe/workflow-scope
> ```
> **通れば scope が在る**。確認後はリモートのブランチを消し、コミットは捨てる
> （`git switch -` で元へ戻る。**force push は使わない**）。

- **`pnpm` が PATH に無いことがある**（Volta の shim が壊れている等）。その場合は
  `npx --yes pnpm@<package.json の packageManager の版> …` で代用できる（実測）。
- **Playwright のブラウザは初回だけ取得が要る**:
  `pnpm --filter @platform/frontend exec playwright install --with-deps chromium`。

> **`build` と E2E が動くかどうかは「変異試験ができるか」を決める。**
> このリポジトリの issue は「**壊すと落ちることを実測せよ**」を頻繁に要求する（§5 型 4）ので、
> ここが動くかで**着手可否そのものが変わる**。だからこそ諦める前に測る。

**環境に依らない制約は §3 に書いてある。** 本節と混同しないこと ——
§3（履歴不変・`planning/` は pin のみ・`src/ai-stock-trading` を変更しない）は
**どの環境でも破らない**。本節は**測れば変わりうる**。

### ★★ 一度「できない」と書いた判定は、棚卸しのたびに測り直す

**判定は環境に依り、環境は変わる。したがって判定には賞味期限がある。**

- **棚卸し（引き継ぎ・フェーズ末監査）のたびに、§2 の波 5 に置かれている issue を上表の手順で測り直す。**
  **「前回できなかった」を根拠に据え置かない。**
  ［2026-08-16 追記 / #454］**従前ここは「§2 の行 H」と書いていた。** 行 H を含む
  フェーズ C〜H の表は波の表へ差し替わり、**環境依存で保留していた issue は波 5 に入った**。
- **判定には「最後に測った時点」を必ず添える**（日付 ＋ 起票 ID）。
  **時点の無い判定は、再検証すべきかどうかを判断できない。**
  **時点と根拠を書く先は §2 ではなく棚卸しの作業仕様書（層 a）である** —— §2 は順序しか持たない。
- **測り直して覆ったら、§2 の波を動かし、覆った事実を層 a へ残す**（消さない。**§5 型 8 の実例になる**）。

> **★ 根拠は #617 の実測である。** §2 は #554 / #556 / #562 の 3 件を
> 「**AI だけでは完結しない**」として保留し、**フェーズ B を打ち切る根拠にしていた**が、
> **3 件とも別の環境では実施でき、同日中に着地した。**
> **「書くとき」の誤りは §5 型 8 が覆う。本項が覆うのは「書いた後、据え置かれること」である。**

> **★ 機械検査は置いていない。** 「環境固有の観測か恒久制約か」は**意味の理解が要り、
> 文字列一致では判定できない**（本リポが `AST` の近傍規則で既に採っている言い方と同じ）。
> **人と AI が守る。**

## 5. ★ このリポジトリで繰り返し起きる事故の型（9 型）

**新しい失敗はほとんど無い。同じ型が名前を変えて再発している。** 着手前にここを読むこと。

### 型 1: 母集合を引き直さない

**issue 本文の一覧・監査の一覧・前の PR の表は、いずれも母集合ではない。**
規則は `.claude/rules/traceability.md`「是正・追随の母集合の取り方」（キット・規則 1〜8）と `traceability.repo.md`（本リポ固有・規則 9・10）が正。
**破れた実例は、規則 1〜6 がクロス監査の実装 ADR の決定 1、規則 9・10（旧 7・8）が別紙 [`population-drawing-annex.md`](./population-drawing-annex.md)**（#730 で入口から外した。#755 で改番）。

**本セッションで新たに踏んだ形**:
- **`grep -l`（ファイル一覧）を母集合にした** —— #555 では 23 ファイルが該当するように見えたが、
  行まで見ると**是正対象は 0 件**（差はすべて経緯を述べたコメント）だった。
- **走査式の設計が結論を決めている** —— #609 は「`OK: 461 件` は 461 ファイルを見たと言っているが、
  **各ファイルのどのリンクを見たかは言っていない**」という形で 248 件が無検査だった。
  **「走査式が返さなかった」は「存在しない」を意味しない。**
- **［2026-08-23］検証を通した後に、自分でコードを足して検証を無効にした** —— 検査器一式を走らせて
  緑を確認し、**その後で入口チャンクへ数百バイト足し、予算の検査だけ引き直さなかった**。
  ビルドは通るので手元では気付かず、CI が落ちて初めて分かった。
  **母集合が動くのは他人の変更のときだけではない。自分の次の一手でも動く。**
  規則 7 の「数値を 1 つ直したら引き直す」は、**自分が動かした数値にも掛かる。**

### 型 2: 呼び出し口を 1 つだけ配線する（**本セッションで 3 度目**）

関数の引数を増やしたとき、**呼び出し口のうち 1 つにしか渡さない**。

| 回 | 何が起きたか |
| --- | --- |
| #507 | `crossRepoRefReasons` のラベル表に kind を 1 つ足し忘れ、CI が「未知の違反種別」を出した |
| #590 | **同じ表で同じ omission を再発**（型 4 を足したとき） |
| #612 | `validateIdExistence` の第 4 引数を `--title` 経路にだけ渡し、**CI の必須チェックが実行するレンジモードで無効**だった |

**対策**: **引数を増やしたら `grep -n '<関数名>(' ` で呼び出し口を全部出してから配線する。**
そして**経路ごとに実バイナリで当てる** —— #612 では `--title` の変異試験だけが通ったので
「検査が効いている」と誤って結論した。

### 型 3: 同じ数・同じ言明を 2 箇所に持つ

条文へ実測値を埋めると、**正例を 1 つ足しただけで黙って古くなる**。
**数は作業仕様書の §母集合 を正とし、条文・スクリプト・PR 本文へ書かない。**
（#590 が実際に踏み、その教訓を書いた `traceability.md` の行が**すでに古かった**）。

### 型 4: 変異試験をしていない検査を信用する

**「壊すと落ちる」を実測していない検査は、入れたのに効いていないことがある。**

本セッションの実例（いずれも自分で作って自分で踏んだ）:
- **`check-landed-subjects.js`**: baseline へ存在しないハッシュを**1 行足すだけで検査全体が緑**になった。
  「浅いクローン（skip）」と「履歴は完全なのに解決できない（fail）」を割って塞いだ。
- **`changelog-overrides.json`**: JSON の要素境界を誤り、**隣のエントリの説明文が別のコミット行に載った**。
  **構文が通ることは意味が通ることを保証しない。生成物を実際に作って目で見る。**

### 型 5: 開示を検査の代わりにする

**「この型は検出できないと書いた」は「検出できなくてよい」ではない。**
ただし**偽陽性は必ず塞ぐ**（検出漏れは開示してよいが、偽陽性は他人の PR を止める）。

**書けないことは書けないと明記する**のは正しい。実例は、着地件名の検査範囲を定めた実装 ADR の決定 3
（**PR タイトルからの ID 脱落は機械では判定できない** —— 落ちた後の件名は規約適合であり、
判定には PR タイトルとの突合が要るが、PR タイトルはリポジトリの中に無い）。

### 型 6: flat config / 設定ファイルの後勝ち置換

ESLint flat config は**同一ルールを後勝ちで置換する**。新しい禁止を足すつもりで
**既存の禁止を丸ごと無効化する**。`src/eslint.config.js` の冒頭が自ら警告している。
**新しいブロックを足す前に、既存ブロックへ 1 行足せないかを見る。**

### 型 7: **条文が片側しか書いていない**（そして書いた本人が逆側を踏む）

**フェーズ末クロス監査が出した 🔴 がこれである。**

#612 は「**スカッシュ件名から ID を落とすな**」という検査と条文を入れた PR だった。
**その同じ PR の着地件名が、実体を伴わない ID を足していた**
（`feat(NFR,FR-12,SC-07,IADR-0145)`。スコープに挙げた要求・画面の成果物は 1 つも触っていない）。

**条文は「落とすな」しか書いておらず、「足すな」が無かった。**
**穴を作った条文を書いた PR 自身が、その穴を踏んだ。**

しかも**先例は既にあった** —— #596 は同じ「remap で他コミットの記録を是正する」作業で、
対象の scope を継承せず自分の起点を名乗っている。**規約に無いだけで、作法は存在していた。**

- **禁止を書いたら、その逆向きも書けるか考える**（落とす／足す・広げる／狭める・消す／複製する）。
- **既存の似た作業を 1 つ探して、自分がその作法に従っているか確かめる。**
- **機械では捕まらない**——検査は「実在するか」しか見ないので、**落とした ID も足した ID も通る**。

### 型 8: 「できない」と「やると害の方が大きい」を混ぜる

着地件名の検査範囲を定めた実装 ADR の決定 3 へ「この事故は**機械では検出できない**」と 3 箇所書いたが、**実測すると過大だった**
（変更パス由来の ID 判定で着地件名 319 件中 26 件ヒットし、実際の事故を含む。**走査基準: develop `2cd8508` 時点**）。

正しくは「**PR タイトルとの突合はできない**」＋「**パス由来は可能だが偽陽性 25/26 で方向が合わない**」。
**後者の言い方は同じ規約が別の箇所で既に採っていた**（近傍規則の偽陽性）。

**「できない」は検証されると崩れる。「やると害の方が大きい」は実測で支えられる。**
**限界を書くときは、どちらなのかを確かめてから書く。**

> **［2026-08-08 追記 / #617］第 3 の形: 「この環境でできない」を「できない」と書く。**
>
> **型 9 として独立させず、ここへ畳む** —— 型 8 は「限界の言明を誤る」型そのものであり、
> 分けると**同じ教訓を 2 箇所に持つ**（型 3）ことになるためである。
>
> 旧 §4.5 は「**この実行環境でできないこと（実測）**」という表題で、
> `build` が通らない・`workflow` スコープが無いという**環境固有の観測**を書いていた。
> 観測自体は正しい。**誤りは、それを恒久的な制約の側（陳腐化しにくい節）へ置いたこと**である。
> §2 には「必ず古くなる」と但し書きがあったが、**§4.5 には無かった。**
>
> **実害**: #554 / #556 / #562 の 3 件が「**AI だけでは完結しない**」として保留され、
> フェーズ B が打ち切られた。**別環境では 3 件とも実施でき、同日中に着地した。**
>
> - **能力の言明には、必ず「どう実測するか」を添える**（旧 §4.5 には実測コマンドが無かった）。
> - **環境に依る制約と、リポジトリの制約を同じ表に混ぜない**（前者は §4.5・後者は §3）。
> - **「できない」と書かれているものを見たら、まず自分で実測する。**

### 型 9: **パターンの射程を、意図ではなく実際の一致で確かめない**

［2026-08-28 追記 / PR #1021］**書いたパターンが「自分が意図した範囲」より広く一致する。**
本セッションで 3 度踏んだ。いずれも**書いた本人には正しく見えていた**。

**① `src/*` は submodule に届く（最も危険だった）**

アーキ移送波の後片付けで、こう回した:

```
for s in <14 サービス名>; do rm -rf src/*/backend/Services/$s/src src/*/backend/Services/$s/tests; done
```

意図は「自分が持つ 2 ユニット（`src/knowledge` と `src/platform`）」だった。
実際の `src/*` は **`src/ai-stock-trading`（別プロジェクトの submodule）にも一致する**。
AST 自身の `NotificationService` は旧 8 要素配置のままなので `.../NotificationService/src` が実在し、
**67 ファイルを削除した。** gitlink は動いておらず、コミット前に気付いて復元したが、
**あと 1 コマンドでコミットしていた。**

- **気付けたのは stop hook が「未コミットの変更がある」と言い、それを commit せずに中身を見たから。**
  **警告に対して「じゃあコミットしよう」と反応していたら、他人のリポジトリの削除を push していた。**
- **教訓: 破壊的操作の対象は列挙する。** 「自分が持つ 2 つ」を意図しているなら
  `src/knowledge` と `src/platform` と書く。**`src/*` は「`src/` の下の全部」であり、
  その中には他人のリポジトリが在る。**

**② `\([^)]*\)` はコミットメッセージの括弧で早く閉じる**

cherry-pick の衝突マーカーを消す正規表現で `>>>>>>> <sha> \(<件名>\)` を書いたが、
本リポジトリの件名は `refactor(NFR,IADR-0282): …` と**括弧を含む**ため `[^)]*` が途中で止まり、
**置換が 0 件成功のまま「解決した」と表示された。** その状態で `git add` したので、
**衝突マーカーがコミットに入った**（次のコミットで気付いて amend）。

- **教訓: 「何件置換したか」を数えて、期待と突き合わせる。** 0 件なら失敗である。

**③ `git grep -l '\.Api\.'` は `.Worker.Composable` を拾わない**

移送で古くなった文字列を掃除するとき、検索語を `.Api.` に絞ったため
`IngestionService.Worker.Composable.Steps.…` が母集合から落ちた。
**型 1（母集合）と同じ病だが、こちらは「絞り込みの語が狭すぎた」形である。**

> **3 つに共通するのは「パターンを書いた時点の頭の中の範囲」と「実際に一致する範囲」の差**である。
> **書いたら、まず一致した集合そのものを見る**（`grep` なら結果を、`rm` なら `echo` に置き換えて、
> 置換なら件数を）。**実行してから確かめるのでは、破壊的操作には間に合わない。**

## 6. 裁定・方針決定が要る論点（実装側では決められない）

| 論点 | 状況 |
| --- | --- |
| **`feedback/` の位置づけ** | 「実装側で発生した記録だけ」か「MSP の記録の写し」か未確定。**計画側にしか無い MSP 記録が 5 件ある**。planning への起票が要る |
| ~~**#562 の CI 結線**~~ | **［2026-08-08 解消］裁定は要らなかった。** `workflow` スコープを持つ経路から結線できる（PR #619 で実施）。**論点として挙げたこと自体が §5 型 8 の第 3 の形**であり、環境固有の制約を恒久的な裁定事項として登録していた |
| **波 5（XL 群の再実装）** | #438〜#453 系。着手順と粒度に利用者の方針決定が要る（旧「フェーズ G」） |
| **`ADR-0044` / `ADR-0045`** | 実装の担い手が未定（`ADR-0045` は #600 が受ける） |
| **INDEX 決定 31（realm 改名）** | デプロイ資産の改名を定めた実装 ADR と衝突する。裁定が要る |

## 7. 引き継いだ人が最初にやること

1. **§4.5 の 4 つを実測して、自分の環境の能力を確かめる**（5 分）。
   **ここを飛ばして「できない」と書かれた項目を鵜呑みにしないこと** —— それで
   フェーズ B が一度打ち切られた（§5 型 8 の第 3 の形）。
   `gh` が使えない環境なら GitHub 操作は MCP ツール（`mcp__github__*`）を使う。
2. **`develop` を貼り直して健全性を見る**（§4 の検査器を一通り走らせる）。
   `git checkout -B develop origin/develop` —— **クローンが shallow の場合**、
   `git merge --ff-only` が「refusing to merge unrelated histories」を出すことがあるが**異常ではない**
   （full clone なら通常どおり ff-merge できる。これも環境による）。
3. **フェーズ末クロス監査の指摘が残っていれば消化する。**
   **★ 実施済みかどうかは §2 ではなく `.ai-context/specs/` の監査是正仕様書で確かめること**
   （フェーズ A = `20260808_phaseA-audit-followup.md`、フェーズ B = `20260808_phaseB-audit-followup.md`）。
   ［2026-08-08 追記 / フェーズ末クロス監査］**従前ここは「フェーズ A・B の監査は実施済み」と断定していたが、
   B は当時まだ未実施だった。** 完了宣言を「陳腐化しにくい」とされた本節に置いたのが誤りである
   —— **状態は §2 と同じく必ず古くなる。**
4. **§2 の波の並びに従い、未消化の最小の波から進む。**
   **どの波まで終わったかは GitHub と層 a で確かめる** —— **ここへ「次は波 N」と書かない**。
   旧版は手順 4 に「フェーズ C（#532 …）へ進む」と番号を書き、**その 7 件が全部閉じた後も残っていた**。
5. **波 5（XL 群）は、層 a の着手ゲート（3 条件）を自分で確かめてから。**
   **利用者の方針決定が先**であり、**子 issue は起票しない**（番号を増やしても消化は進まない）。
   ［2026-08-16 追記 / #778］**「起票しない」には層 a §8 が覆る条件を付している** ——
   **ゲートが開いて実際に着手する issue は、その時に分割してよい**（全面再実装の進行方式・規約 4）。
   #442 はこれに当たり、子 5 件へ分割した。**開いていない issue を先回りして割らないこと**が禁止の中身である。

> **着手前に必ず読む**: `CLAUDE.md` ／ `.claude/rules/traceability.md` ／ クロス監査の実装 ADR（母集合と監査巡数）。
> **そして §5 の 8 つの型。** 新しい失敗より、これらの再発の方がはるかに多い。
