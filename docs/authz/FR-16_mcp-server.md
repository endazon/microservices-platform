---
title: MCP サーバー 権限・認可仕様書
type: authz-spec
status: draft
author: claude
created: 2026-08-23
updated: 2026-09-04
---
<!-- trace:
ids: [FR-16, FR-19, UC-08, UC-09, SC-12]
adrs: [ADR-0004, ADR-0024, ADR-0034, ADR-0036, ADR-0046, ADR-0054, ADR-0062]
iadrs: [IADR-0269, IADR-0297, IADR-0366]
specs: [20260823_issue-445_mcp-server-integration, 20260903_issue-1185_unattended-account-attribute-subset]
issues: [#445, #1185]
-->

# 権限・認可仕様書: MCP サーバー

## ロール定義

| ロール | 説明 | 対象 |
| --- | --- | --- |
| 管理者 | クライアント登録・無効化・属性割当・公開ツール一覧の確認 | システム管理者 |
| 有人エージェント | 利用者の代理としてツールを実行する MCP クライアント | 登録済みの対話型クライアント |
| 無人エージェント（サービスアカウント） | バッチ等、自然人の主体を持たない MCP クライアント | 登録済みのサービスアカウント |

## 認可マトリクス

| 機能 / リソース | 操作 | 管理者 | 有人エージェント | 無人エージェント |
| --- | --- | --- | --- | --- |
| クライアント登録管理 | 参照 | ○ | × | × |
| クライアント登録管理 | 更新 | ○ | × | × |
| 実効ツール一覧 | 参照 | ○ | ○（自分に公開されたもの） | ○（同左） |
| ツール実行 | 実行 | — | ○（利用者の権限スコープ内） | ○（サービスアカウントの権限スコープ内） |
| **個人資料** | 参照 | — | ○（所有者・共有先の判定に従う） | 🔴 **一律で対象外** |

## 認可ルール

### 1. 主体の解決

- 主体種別（有人 / 無人）は**登録簿から採る。トークンのクレームからは採らない。**
  クライアント側の申告で除外の適用対象から外れられないようにするためである。
- 登録簿は**呼び出しのたびに引く。キャッシュを挟まない。** 無効化を即時に効かせるためである。
- 未登録クライアントと無効化済みクライアントは**同じ文言**で拒否する（登録の有無を漏らさない）。

### 2. 🔴 サービスアカウント実行時の個人資料の一律除外

計画の決定は次のとおりである。

> サービスアカウント（無人エージェント）による実行では、個人資料を一律で探索・応答の対象外とする。
> **所有者の個人資料であっても含めない。**

適用の要点:

- **経路で分けない。** 検索・文書取得・グラフ探索のいずれにも同じ規則が及ぶ。
  経路によって扱いが変わると除外の意味が失われる。
- **2 層で強制する。** 下流サービスへ渡す実行スコープに除外制約を立て（要求側）、
  応答からも個人資料を落とす（応答側）。応答側は下流が制約を無視した場合の防壁である。
- **件数からも外す。** 「権限が無い」と「存在しない」を区別させない原則に従い、
  除外した文書は全体件数にも含めない。件数自体がサイドチャネルになる。
- **判定は集合帰属で書く。** 「文書スコープ属性が個人資料の値を持つ」で判定し、
  「組織文書でない」では判定しない。属性は新設で既存文書へ遡及付与しないため、
  否定で書くと組織文書が一斉に落ちる。

理由（計画側の論拠）: 動的束縛（所有者が現在の利用者であること）は**自然人の主体を前提とした
記法**であり、サービスアカウントを束縛先に据えると「誰の個人資料か」を問わない判定になりかねない。
無人経路では**除外という単純な規則**で原則を担保する。

### 3. サービスアカウントへの属性割当の制約

**個人資料を読ませる属性割当を構成上禁止する。** 公開構成のスキーマ検証と管理 API の両方で拒否し、
検証は 1 つの関数を共用する（2 箇所に書くと片方だけが緩むため）。

#### 3-1. 🔴 登録者が持つ集合の部分集合であること（［2026-09-03 追加］）

**無人アカウントへ割り当てられる機密区分（`clearance`）とタグの集合は、登録操作を行った利用者自身が
持つ集合の部分集合でなければならない。**

- **なぜ要るのか**: 管理画面はシステム管理者限定だが、**ロールと機密区分は別の軸である。**
  機密区分が社内限の管理者が取扱制限のサービスアカウントを作れると、**登録者は自分では読めない文書を、
  自分が作ったサービスアカウント経由で読める。** 無人アカウントは人が居ないぶん、割り当てられた属性が
  そのまま恒久の権限になる。個人資料の一律除外に続く**2 つめの絞り**である。
- 🔴 **順序の語を使わない。** 本モデルは序数比較を意図的に排除しており、機密区分のあいだに上下は
  定義されていない（階段は各段の許可集合の明示列挙で表される）。**「上限を超えない」は書けない。
  部分集合で表す。**
- **判定は後段（管理 API）が行う。** 画面と境界層は**値域の絞り込み**（辞書に定義済みの値のみ）までを
  担う。🔴 **画面が判定しても API を直接叩けば素通しになる** —— 「検証がある」と読める UI を置きながら
  実際には統制が無い状態は、検証が無いことより悪い。
- **登録者が配れる機密区分の集合は、認可スコープの解決結果から採る。** 実装側に階段の表を持たない
  （持つと辞書を編集しても追随せず、排除したはずの序数が復活する）。
  🔴 **所有者ベースの分岐を「全値許可」と読まない** —— 登録者が自分の文書を読めることは、
  サービスアカウントへ取扱制限を配ってよい理由にならない（所有権は継がれない）。
- **登録と属性差し替えの両方に効かせる。** 同じ 1 つの関数が判定する（片方だけ緩むのを構造で防ぐ）。
- **拒否は 400 とし、本文に「どの値が部分集合から外れたか」を値そのもので載せる。**
  「権限がありません」で丸めない。**差集合だけを名指しし、外れていない値を混ぜない。**
  🔴 **存在秘匿と矛盾しない** —— 返すのは**登録者本人の集合と本人の入力の差**だけであり、
  他者の属性も、他者が持つ値域も、文書の存在も返さない。
- **登録者の属性を解決できないときは配らない**（deny-by-default）。ただし**「持っていない」とは
  書かない** —— どちらも配らない点では安全側だが、報告が嘘になる。
- **身元を返す口（`/bff/auth/me`）へ機密区分・部門を足さない。** 属性の正は認可サービスであり、
  画面へ配ると「画面が持つ値」と「判定に使う値」の 2 つができ、**画面で判定できるように見えてしまう。**

### 4. 公開範囲の統制（存在秘匿）

- 既定は非公開である。宣言的公開構成に明記されたツールだけが公開される。
- 構成に無いツールの呼び出しは **「不明なツール」** として拒否する。
  「権限がありません」と返すと、そのツールの存在が漏れる。
- 未登録・無効化されたクライアントには、ツール一覧そのものを空で返す。

### 5. データ越境（認可とは別の軸）

越境統制は「見えてよい文書の本文を外へ出してよいか」を決める。**存在秘匿とは別の軸**であり、
送信不可の文書を応答から消しはせず、本文を落として参照リンクのみへ縮退させる。

## 関連仕様

- [MCP サーバー 通信仕様書](../api/FR-16_mcp-server.md)
- [MCP サーバー統合 テスト仕様書](../tests/FR-16_mcp-server.md)
