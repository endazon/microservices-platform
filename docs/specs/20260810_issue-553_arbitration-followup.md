---
title: 作業仕様書 — 裁定で確定した閲覧ロールと機密区分の表示名へ実装側の記述を追随させる（#553）
type: work-spec
status: done
related_ids:
  - SC-05
  - SC-06
  - SC-07
  - IADR-0039
  - IADR-0127
  - IADR-0128
  - IADR-0125
author: claude
created: 2026-08-10
updated: 2026-08-10
plan_refs:
  - "../../planning/projects/microservices-platform/05_screens/01_screens.md"
  - "../../planning/docs/glossary.md"
---

# 作業仕様書: 裁定の追随（#553）

## 起点

- **SC-05 / SC-06 / SC-07**（管理系 3 画面）。関連 IADR: **0039 / 0127 / 0128 / 0125**
- 起点 issue: **#553**（裁定 2026-08-05・質問票 第12回 **Q19** ＋ 機密区分の表示名）

## 母集合（自分で引き直した）

**#553 は「記述 2 箇所」と書いている。引き直すと実態は違った** ——
**片方は既に完了しており、もう片方は 1 箇所ではなく 46 箇所の走査を要した。**

### 軸 1: issue 番号で引く

```console
$ git ls-files -z ':!planning' ':!src/ai-stock-trading' | xargs -0 grep -ln '#553'
docs/how-to/session-handoff.md
```

**1 件のみ**（引き継ぎメモ）。実装指示の引き継ぎは無い。

### 軸 2: 計画書の現状（**pin から実際に読んだ**）

**計画は既に実装へ合わせて改訂されている。**

| 節 | 計画の現在の文言（`05_screens/01_screens.md`） |
| --- | --- |
| `:124` | **アクセス制御の割当（2026-08-05 に是正）**: SC-05/06/07 = **閲覧は管理者・運用者／破壊的操作は管理者限定** |
| `:273`（SC-05） | **閲覧は管理者・運用者。作成・編集・削除は管理者限定**（確定・2026-08-05） |
| `:298`（SC-06） | 同上（登録・更新・無効化）。**手動同期は破壊的操作に含めず運用者にも開く**（2026-08-09 / planning#299） |
| `:314`（SC-07） | 同上（再変換の実行と人手補正）。**再変換の管理者限定は 2026-08-04 の確定を維持** |

表示名の正は **`planning/docs/glossary.md:48`** ——
`public`＝公開 / `internal`＝**社内限** / `confidential`＝秘 / `restricted`＝**取扱制限**。
**`restricted` を「極秘」としない**理由（個人資料が既定でこの区分）も同行にある。

**つまり実装の現行判断（IADR-0039 決定 1）が正しかった。** 残っているのは**実装側の記述の追随だけ**である。

### 軸 3: 「裁定待ち」の全数走査（**`| head` で打ち切らない**）

> #553 自身が「**`| head` で打ち切らないこと**——過去に打ち切りによる欠落が実際に起きている（#484 → #486）」
> と書いている。全件を出して分類した。

```console
$ git ls-files -z 'docs/adr/*' 'docs/screens/*' 'docs/api/*' 'docs/tests/*' 'docs/functional/*' \
    'docs/observability/*' 'docs/operations/*' 'docs/security/*' 'docs/data/*' 'docs/tech/*' 'src/*' \
  | xargs -0 grep -n '裁定待ち\|裁定を仰ぐ\|裁定に従う'
```

**live 文書で 46 箇所 / 24 ファイル。** 参照している裁定で分類した:

| 参照先 | 件数 | 判定 |
| --- | ---: | --- |
| **planning#198（閲覧ロール・提案 8）** | **22** | **Q19 で解決済み ＝ 追随対象** |
| planning#197（画面要素 19 種） | 12 | **混在。1 件ずつ判定する**（下記） |
| 番号なし | 10 | **大半は条件付きの将来**（「着手時に判断し**必要なら**仰ぐ」）＝陳腐化していない |
| **planning#306** | 2 | **本当に未解決。触らない** |

> **★ 一括置換してはならない。** 「裁定待ち」という同じ語で、**解決済み・条件付き・未解決**の 3 種が混ざっている。
> #553 の受け入れ基準が「追随**または対象外**の判断が記録されている」と書いているのはこのためである。

#### planning#197 の 12 件が混在する理由

planning#197 は**画面要素 19 種**を束ねた裁定であり、**論点ごとに着地が違う**:

| 論点 | 状態 |
| --- | --- |
| 対象範囲フィルタ（SC-01 / SC-08） | **#539 で実装済み**（候補の口は #540） |
| 結果に更新日時を含める | **#536 で実装済み**（IADR-0149） |
| 並び順 2 値 | **#532 で実装済み**（IADR-0150） |
| 検索モード 3 値 | **#531 で実装済み** |
| 左ナビのグループ分け（§付随の論点） | **Q11 で確定。実装（SC-03 を置かない）が追認された** |
| **機密区分の表示名** | **裁定で確定**（glossary が正）。写像の実装先は **#541** |

**したがって「planning#197 の裁定待ち」と書いてある箇所は、論点ごとに真偽が違う。**

#### ★ 12 件を 1 件ずつ判定した（判断 2 の実行結果）

**［2026-08-10 追記］初版はこの表を作らずに 2 件（`confidentiality.ts` / `attributes.ts`）だけを直した。**
**判断 2 を自分で書いておきながら実行していなかった。** レビューが `SC-05:92` の自己矛盾（同じ PR が
40 行下に「決着した」と書きながら、上の対応表に「裁定待ち」を残した）を 🔴 で挙げ、
**引き直したところ指摘の 3 箇所より多く見つかった。** 全 12 件の判定は次のとおり:

| # | 箇所 | 論点 | 裁定 | 判定 |
| ---: | --- | --- | --- | --- |
| 1 | `SC-01:132` | 対象範囲フィルタ | Q1・Q2・Q3・Q9 | **対象外** —— **直上に［2026-08-09 / #539］の解消追記があり「以下は当時（実装前）の記録である」と明示済み**。読み分けられている |
| 2 | `SC-01:309` | 同 | 同 | **対象外**（取り消し線 ＋ 追記済み） |
| 3 | `SC-02:107` | 並び順 / 検索モード / 更新日時 | Q4〜Q6 | **追随** —— 表の 3 行には解消追記があるのに、**まとめ文だけ「裁定を待っている」が現在形で残っていた** |
| 4 | `SC-03:86` | 左ナビのグループ分け | **Q11** | **追随**（実装「しない」が追認された） |
| 5 | `SC-03:126` | 同 | 同 | **追随**（「裁定が出るまで実装は変えない」→ 裁定が出て、変えないままで正しい） |
| 6 | `SC-03:189` | 機密区分の表示名 | Q7・Q8・Q30 | **追随**（4 値の表を追記。**「2 値しか無い」という前提が崩れている**） |
| 7 | `SC-03:256` | 同（未決事項 3） | 同 | **追随**（🟡 指摘） |
| 8 | `SC-05:92` | 同（対応表 #7） | 同 | **追随**（🔴 指摘。**同一ファイル内の自己矛盾**） |
| 9 | `SC-05:252` | 同（未決事項 3） | 同 | **対象外** —— 直上の追記表が 4 件すべての決着を明示している |
| 10 | `SC-08:39` | 分析対象チップ | Q1・Q3・Q9 | **追随** —— **［実装状態］注記が「未実装のまま残っている」に挙げていた**が #539 で実装済み |
| 11 | `SC-08:206` | 同 | 同 | **対象外**（取り消し線 ＋ 追記済み） |
| 12 | `SearchChatPage.tsx:27` | 対象範囲フィルタ | 同 | **追随（実装と食い違い）** —— §実装しない要素 に挙げながら、**同じファイルの 69 行目が `<ScopeFilter>` を描いている**。SC-08 側は #539 が直しており**本ファイルだけ取り残されていた** |
| 13 | `DocumentManagementPage.tsx:38` | 表示名 | Q7・Q8・Q30 | **追随**（🟡 指摘） |
| 14 | `DocumentManagementPage.test.tsx:73` | 同 | 同 | **追随** |
| 15 | `AnalysisDashboardPage.test.tsx:208` | 分析対象チップ | Q1・Q3・Q9 | **追随（後述。空振り試験）** |
| 16 | `IADR-0127:279`（フォローアップ 4） | SC-07 の実装しない 6 種 | #197/#198/#199 | **追随** —— 「裁定ののち後続 issue で実装する」の**前提が満たされた**。ただし着地は一様でない（実装 / 計画側が削除 / 実装の追認）ので、その内訳を書いた |
| 17 | `confidentiality.ts:12` / `attributes.ts:9` | 表示名 | 同 | **本 PR が既に追随済み**（当時の記述を引用として残している） |

> **★ 母集合の取り直しで自分の漏れが 1 つ出た。** 初版の軸 3 は「**裁定待ち**」という語で引いたため、
> **SC-03 が 1 度も仕様書に出てこなかった**（レビューの 🟡 が指摘したとおりである）。
> 引き直しは**「planning#197 を参照する記述」全体**（語ではなく参照先）で引いた。
> **語で引くと、同じ論点を別の言い回しで書いた箇所が落ちる。**

#### ★ 上の #15 で見つけたもの —— **落ちない試験が 1 本あった**

`AnalysisDashboardPage.test.tsx` の
`does not render tag or folder chips (no contract for permitted candidates)` は、
**同じファイルの先頭にある `puts the selected scope chips into the data range`（#539 が追加）と
主張が正面から矛盾していた。** さらに悪いことに、**この試験は空振りだった**:

```console
$ # 照合していたのは /タグ:/ と /フォルダ:/（コロン付き）
$ # 実際の軸ラベルは「タグ」「部門」「プロジェクト」（コロン無し）
$ # → チップが描かれるようになっても、この試験は落ちない
```

**「実装したのに落ちなかった」ので、#539 のときに気づけなかった。**
主張を実態（**軸は 3 つ・「フォルダ」は不採用**）へ書き替え、**空振りでないことを変異試験で確かめた**:

```console
$ # ScopeFilter を `return null` にして走らせる
$ pnpm vitest run …/AnalysisDashboardPage.test.tsx
× renders the three scope axes and offers no folder axis (Q9)
Tests  2 failed | 12 passed
# 旧試験ならここで落ちない。新試験は落ちる。
```

### 軸 4: 機密区分の表示名の全数（`極秘` / `社内限` / `取扱制限`）

**★ #553 の Part 2 は既に完了している。**

```console
$ sed -n '96,103p' src/packages/ui/src/stories/Primitives.stories.tsx
{/* 機密区分の表示名は計画リポジトリの用語集（planning/docs/glossary.md）が正である。
    restricted は「取扱制限」であって「極秘」ではない…（利用者裁定 Q30 / planning#200）。 */}
<option value="internal">社内限</option>
<option value="restricted">取扱制限</option>
```

**#541（PR）が既に是正済み**であり、同 issue の作業仕様書が
「`Primitives.stories.tsx`（`Select` の選択肢）… **是正した**（社内限 / 取扱制限）」と記録している。
**#553 が「実測」として引用した `社内` / `極秘` は、起票時点（2026-08-05）の姿であって現在の姿ではない。**

`極秘` の残存 3 箇所はいずれも**表示名ではない**:

| 箇所 | 性質 |
| --- | --- |
| `HybridSearchEndpointTests.cs`（`極秘プロジェクト`） | **テストデータの本文**（#553 自身が対象外と明記） |
| `EmbeddingEndpointTests.cs`（`極秘の本文`） | 同上 |
| `SearchResultDto.cs` / `FR-04` / `SC-01` / `Primitives.stories.tsx` | **「極秘ではない」と否定する説明文** |

**残っているのは保留コメント 2 件だけ**である（`confidentiality.ts:11` / `attributes.ts:11`。
いずれも「planning#197 で裁定待ち」と書いている）。

## 判断

### 判断 1: **planning#198 の 22 箇所を追随させる**（IADR-0128 だけではない）

#553 は IADR-0128 への追記を挙げているが、**同型は 22 箇所ある**。
IADR-0128 / IADR-0127 / IADR-0042 / IADR-0039 / `docs/adr/README.md` /
`docs/screens/SC-05・06・07` / 画面のソースコメントに散っている。

**日付つき［追記］で残し、元の記述は消さない**（このリポジトリの作法。先例は IADR-0117→0056 等）。
**「当時は裁定待ちだった」という事実自体は正しい記録**であり、消すと判断の経緯が読めなくなる。

> **★［2026-08-10 追記］「22 箇所を追随させる」という上の書き方は不正確だった。**
> 判断 2 が未実行だったのと同じ疑いで**自分で引き直した**ところ、22 箇所の内訳は 3 種に分かれていた。
> **全部を本 PR が直す必要は無かったが、直すべきものを 5 件取りこぼしていた。**
>
> | 種別 | 件数 | 扱い |
> | --- | ---: | --- |
> | **既に別 PR が追随済み** | 6 | `IADR-0039:63`（Q19 明記）／`SC-06:155`・`:252`・`:269`（planning#200 反映済み）／`DashboardBffEndpoints.cs:26`（Q19・Q28）／`DataSourceManagementPage.tsx:37`（裁定済み）。**触らない** |
> | **本 PR のファイル冒頭［追記］が「以降の本文は当時の記録」と宣言済み** | 11 | `IADR-0042` / `IADR-0127` / `IADR-0128` / `SC-05` / `SC-07` の本文。**SC-07:288 は残存箇所を名指しで宣言している**。**触らない** |
> | **★ 取りこぼし** | **5** | `IADR-0129:265`（SC-10 の閲覧ロール ← **Q28**・#544 で実装済み）／`docs/tests/SC-07:203`（「裁定を仰いでいる」が現在形）／`sc07-conversions/index.tsx:15`（閲覧ロール ← Q19）／`DocumentManagementPage.tsx:37`（変換列・タグ辞書 ← Q17・Q18）／`SC-05:132`（**「05_screens の旧記述が残っている可能性がある」という推測が Q17 で当たっていた**）。**本 PR で追随させた** |
>
> **比較のうえで重要なのは、`SC-05:233` / `SC-07:274` の「関連仕様」行**である ——
> **同じ役割の行を SC-06 だけが「裁定済み」へ更新しており、SC-05 / SC-07 が取り残されていた。**
> **同型の 3 兄弟のうち 1 つだけ直っている状態は、母集合を画面単位で引けば必ず見える。**

### 判断 2: **planning#197 の 12 箇所は論点ごとに判定する**

**「planning#197 の裁定待ち」を機械的に置換しない。** 論点が
「表示名」なら解決済み、「対象範囲フィルタ」なら #539 で実装済み、と着地が違う。
**1 件ずつ開いて、その箇所が何の論点を指しているかで決める。**

> **★［2026-08-10 追記］この判断は、初版では実行されていなかった。**
> 書いたのに 2 件しか開かず、残りを判定しなかった。**判定の結果は §軸 3 の 17 行の表**である。
> **「方針を書いた」と「方針を実行した」は別**であり、前者だけで済ませると
> 本 issue が是正しようとしている型（**統制を定めたが働いていない**）を自分で作る。

### 判断 2-b: **［2026-08-10 追記］「ファイル冒頭の追記で足りる」は、条件つきで正しい**

2 回目のレビューが 🟡 2 件 ＋ 🟢 1 件を挙げた。**3 件とも同じ型**であり、指摘は正しかった。

> **同じ一覧の兄弟項目のうち、一部だけを取り消し線＋解決注記へ改め、
> 同じく解決した項目を素のまま残すと、残した方が「まだ本当に未決」と読まれる。**

**判断 1 が採った「ファイル冒頭に 1 か所［追記］を置き、本文は当時の記録として残す」という作法は、
一覧の兄弟を揃えている限りで成り立つ。** SC-07 の §未決事項 は
**項目 1・3 を取り消し線で閉じ、項目 4（まさに冒頭追記が扱う閲覧ロール）だけ素のまま**にしていた。
**これは作法の問題ではなく、単に揃っていなかった。**

#### 走査 A: **現在形の断定**（`現在も真` / `は現在も` / `現在も有効` …）

```console
$ git ls-files -z 'docs/*' 'src/*' ':!docs/specs' ':!src/ai-stock-trading' \
  | xargs -0 grep -n '現在も真\|今も真\|現在もなお\|現在も有効\|現在も据え置き\|は現在も'
docs/adr/IADR-0101_default-model-opus-5.md:104
docs/screens/SC-07_conversion-jobs.md:64
```

**2 件しか無く、2 件とも偽になっていた。**

| 箇所 | 断定 | 実測 |
| --- | --- | --- |
| `SC-07:64` | 「据え置きは**現在も真である**」 | **admin/operator という実装の姿は真のまま。偽なのは「裁定待ち」の部分**（Q19 で追認へ変わった） |
| **`IADR-0101:104`** | 「実装の `PurposeModels` は**現在も** `claude-sonnet-4-6`」 | **偽。** `LlmGateway.Api/appsettings.json:42` は `"rag-answer": "claude-sonnet-5"`（[[IADR-0106]] が ADR-0022 へ追従済み） |

> **★ `IADR-0101:104` は #553 の論点（裁定の追随）ではない。** それでも直したのは、
> **同型の走査で見つかった、証明できる偽の記述**だからである。片方だけ直すと
> 「走査したのに残した」ことになる。**論点が違うことは仕様書に明記して残す。**

#### 走査 B: フォローアップ節の**条件つき項目**（`〜たら` / `〜次第` / `〜のち` / `〜を待つ`）

```console
$ for f in docs/adr/IADR-*.md; do awk '/^## フォローアップ/{p=1;next} /^## /{p=0} p && /たら|次第|のち|を待/' "$f"; done
```

**条件が既に満たされているのに閉じていない項目は `IADR-0128` の項目 3 だけ**だった
（`IADR-0150` / `IADR-0153` の 3 件は**将来の条件**であり、まだ発火していない）。
**姉妹ファイル `IADR-0127` は項目 1・4 を取り消し線で閉じてある** —— レビューの指摘どおり、
**同じ「裁定が出たら」を待つ項目が、片方では閉じられ片方では放置**されていた。

#### 是正した 7 箇所

| 箇所 | 直した内容 |
| --- | --- |
| `SC-07:63-64` | 「現在も真である」を**日付つきで否定**し、**何が真のままで何が偽になったか**を分けて書いた |
| `SC-07:99` / `:181` | 同じ論点の現在形へ訂正の追記（`:181` は「裁定に従う」→ **向きは「計画改訂」だった**） |
| `SC-07` §未決事項 項目 4 | **取り消し線＋解決注記**（兄弟の項目 1・3 と同じ形へ揃えた） |
| `IADR-0128` フォローアップ 3 | **取り消し線＋「条件は満たされた。追随すべきものは無かった」で閉じた**（`IADR-0127` と同じ形） |
| `IADR-0127:300` / `:302` | 見出しラベル「裁定待ちで並存する決定」→ **「裁定で追認され並存する決定」**（🟢） |
| **`IADR-0101:104`** | **走査で見つけた別論点。** 取り消し線＋実測（`appsettings.json:42`）で閉じた |

### 判断 3: **planning#306 の 2 箇所と、条件付きの 10 箇所は触らない**

- planning#306（`IADR-0044` / `DocumentEndpoints.cs`）は**本当に裁定待ち**である。
- 「着手時に判断し**必要なら**計画へ裁定を仰ぐ」（IADR-0139 / 0151 / 0152 / `DataSourceSyncService.cs`）は
  **将来の条件付き**であって陳腐化していない。

**除外した理由をここに書く**（母集合の規則 6）。

### 判断 4: **Part 2 は保留コメント 2 件の更新に限る**（Storybook は触らない）

`Primitives.stories.tsx` は **#541 が既に正しい値にしてある**。**触る必要が無い。**

`confidentiality.ts` / `attributes.ts` の保留理由（「計画内で表示名が揺れているため生値を出す」）は
**もう成り立たない**ので、**裁定で確定したこと**と**写像の実装先（#541）**へ書き換える。
**写像そのものは本 PR で実装しない**（#553 が明示的にそう指示している）。

> **`@platform/ui` に表示文言を持たせない規約（[[IADR-0125]] 決定 1）との整合**:
> #553 は「ストーリーへ本物の文言を書くこと自体が規約に触れないか」を判断せよと言うが、
> **既に #541 が本物の文言で書いており、その判断は済んでいる** —— Storybook は
> `@platform/ui` の**公開面（`src/index.ts`）ではなく例示**であり、
> 規約が禁じるのは**プリミティブが文言を持つこと**である。本 PR で覆さない。

### 判断 5: **`docs/specs/` と `feedback/` は触らない**

いずれも**作業当時の記録**である（`.claude/rules/traceability.md`）。
走査対象からも外してある（軸 3 のコマンドが `docs/specs` を含んでいない）。

## テスト（受け入れ基準の写像）

| # | 受け入れ基準（#553） | 確かめ方 |
| --- | --- | --- |
| 1 | IADR-0128 に日付つき［追記］があり、裁定の向きと #501 の有効性が読める | 追記の本文 |
| 2 | **同型が全数 grep で洗い出され、追随／対象外の判断が記録されている** | 本仕様書 §軸 3（**46 件を 4 分類**） |
| 3 | Storybook の表示名が確定値と一致するか、規約に沿う形へ改められている | **既に一致**（#541。実測で確認） |
| 4 | `confidentiality.ts` / `attributes.ts` の保留コメントが実態を反映し、実装先（#541）が書かれている | 差分 |
| 5 | `typecheck` / `lint` / `test:coverage` / `build` が green | 実走 |
| 6 | `check-doc-links.js` が green | 実走 |
| 7 | **planning#197 の 12 件が 1 件ずつ判定され、追随／対象外の理由が読める**（判断 2 の実行） | 本仕様書 §軸 3 の **17 行の表** |
| 8 | **書き替えた試験が空振りでないこと** | **変異試験**（`ScopeFilter` を `return null` にすると落ちる） |

## 射程外

- **機密区分の表示名の写像実装** —— **#541** の領域（#553 が明示）。
- **planning#306 の裁定待ち 2 箇所**（判断 3）。
- **条件付きの「必要なら仰ぐ」10 箇所**（判断 3）。
- **`docs/specs/` / `feedback/` の履歴記述**（判断 5）。
- **★ `docs/adr/README.md` の索引 4 行（`IADR-0126` / `0127` / `0128` / `0129`）** ——
  索引セルにも「裁定待ち」が残るが、**追記を入れてはならない**。
  `scripts/adr-index-title-baseline.json` が **`title-addendum`＝「索引タイトルセルへの
  ［YYYY-MM-DD 追記］の混入」を違反として定義**しており、**実際に落ちることを実測した**:

  ```console
  $ # IADR-0129 の索引セルへ「［2026-08-10 追記］Q19 で決着」を足して
  $ node --test scripts/scripts.test.js
  README.md:185 IADR-0129 title-addendum
  # fail 1
  ```

  `IADR-0127` / `0128` は既に `title-addendum` を**ラチェットへ据え置かれている**（過去分）。
  新しく足すと**ラチェットを増やす**ことになり向きが逆である。
  **索引は「決定当時の要約」であり、現在を示すのは本体の追記**という役割分担で通す。
