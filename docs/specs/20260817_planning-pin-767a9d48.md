---
title: 作業仕様書 — 計画 pin を 767a9d48 へ進め、ADR レンジ 0046 → 0047 とキット追随を引き直す
type: spec
status: in-progress
related_ids:
  - NFR
  - IADR-0115
  - IADR-0119
  - IADR-0130
  - IADR-0142
  - IADR-0183
  - IADR-0190
  - IADR-0192
  - IADR-0200
  - IADR-0201
  - IADR-0204
author: claude
created: 2026-08-17
updated: 2026-08-17
plan_refs:
  - "../../planning/tools/impl-handoff-kit/HOWTO.md (§B-5 キット版を採る前に実走して差を確かめる)"
  - "../../planning/docs/ai-implementation-workflow-guide.md (§8 必読規約の予算 51,200 バイト)"
  - "../../planning/tools/impl-handoff-kit/repo-template/scripts/kit-sync-classification.example.json"
  - "../../planning/projects/microservices-platform/07_adr/ADR-0047_edge-cert-scope-local-route.md"
  - "../../planning/projects/microservices-platform/02_requirements/01_requirements.md (NFR-11 の適用範囲)"
related_specs:
  - "../adr/IADR-0204_kit-catchup-deferral-with-expiry-ratchet.md"
  - "../adr/IADR-0201_class-c-rejudgement-and-fail-closed-kit-checks.md"
  - "../how-to/plan-id-range-history-annex.md"
  - "20260816_issue-790_planning-pin-8cae89d-and-kit-rejudgement.md"
---

# 作業仕様書: 計画 pin `767a9d48` の追随

## 1. 起点となる ID（トレーサビリティ）

- **`ADR-0030`**（バックエンド標準構成。ブランチ名の起点 ID）。**`IADR-0116` 規約 3 に従い `NFR` は読み飛ばす** ——
  同規約の worked example がまさに「`NFR` と併記されていても最初の**具体 ID** を採る」であり、
  **前波（#832）でこれを破って `nfr` を使った**ため、本波では最初から具体 ID を採っている。
- **無採番 `NFR`**（キット追随・pin 更新＝メタ作業。`.claude/rules/traceability.md`「無採番 `NFR` を許す 2 つの場合」の**場合 2**。
  計画の非機能要件表 `NFR-01`〜`NFR-27` は 27 件とも**稼働する製品**の要件であり、当たる番号が無い。**環流しない**。[[IADR-0179]] 決定 2）

## 2. 母集合 —— 引いた結果と、除外したものとその理由

**時点: 2026-08-17、worktree `/home/user/wt-pin2`、ブランチ `chore/ADR-0030-planning-pin-767a9d48`、base `origin/develop` = `26e45293`。**

**着手前に `git fetch origin develop` を実行してから走査した。** 前波で**10 コミット古いツリーを走査して誤った結論を出した**
（「`IADR-0218` は develop に存在しない」）ため、走査基準の確定を手順の先頭へ固定している。

### 2.1 ID レンジの引き直し（新旧両 pin で同一コマンド）

```bash
for SHA in 8cae89d 767a9d48; do
  for KIND in FR NFR UC SC; do
    git -C planning grep -h -o -E "\b$KIND-[0-9]{2}\b" $SHA -- projects/microservices-platform \
      | sed "s/.*$KIND-//" | sort -u
  done
  git -C planning ls-tree -r --name-only $SHA -- projects/microservices-platform/07_adr/ \
    | grep -oE 'ADR-[0-9]{4}' | sort -u
done
```

| 種別 | 旧（走査基準 `8cae89d`） | 新（走査基準 `767a9d48`） | 差 |
| --- | --- | --- | --- |
| `FR` | `01`〜`22`（22 件） | `01`〜`22`（22 件） | 不動 |
| `NFR` | `01`〜`27`（27 件） | `01`〜`27`（27 件） | 不動 |
| `UC` | `01`〜`11`（11 件） | `01`〜`11`（11 件） | 不動 |
| `SC` | `01`〜`21`（21 件） | `01`〜`21`（21 件） | 不動 |
| **`ADR`** | **46 件** | **47 件** | **`ADR-0047` 新設** |

**欠番は `seq` との `diff` で機械的に確かめた**（「47 件あるから連番」は連番の証明にならない）:

```bash
diff <(git -C planning ls-tree -r --name-only 767a9d48 -- projects/microservices-platform/07_adr/ \
        | grep -oE 'ADR-[0-9]{4}' | sort -u) \
     <(seq -f 'ADR-%04g' 1 47)
# EXIT=0（欠番なし）
```

**状態も引き直した**（`Proposed` でも ID としては実在するが、着手条件の判断に効くため）:

| 時点 | Accepted | Proposed | Superseded |
| --- | ---: | ---: | ---: |
| `8cae89d` | 40 | 5 | 1 |
| `767a9d48` | 42 | 4 | 1 |

差は **`ADR-0038` の `Proposed` → `Accepted`**（planning#392 / PR planning#394。MSP 分はこれ 1 件で、同 PR の残り 3 件は AST）と
**`ADR-0047` の新設**（Accepted）の 2 つである。

### 2.2 追随先の走査（規則 1・2・3・4 —— 誤りの側から、形を列挙し、拡張子でも行でも絞らない）

**軸 1: 旧 pin sha `8cae89d`**（`git grep -n --untracked "8cae89d" -- . ':!planning' ':!src/ai-stock-trading'`）。
**13 ファイルがヒットした。** 内訳と扱い:

| ファイル | 扱い | 理由 |
| --- | --- | --- |
| `.claude/rules/traceability.repo.md:7` | **直す** | 入口の**規範**。走査基準 sha ＋ `ADR-0001..0046` → `..0047` |
| `scripts/kit-sync-classification.json:4` | **直す** | `$comment` の pin は**導出値**。走査ではなく計算し直す（規則 10） |
| `scripts/kit-sync-classification.json:135` | **直す** | 分類 X の再判定（§3） |
| `docs/how-to/plan-id-range-history-annex.md:88` | **直す** | 「**現行** pin（`8cae89d`）」は導出値。世代記録の追加とは別物 |
| `docs/how-to/plan-id-range-history-annex.md:29/32/35` | **直さない** | **過去の世代記録**（`4d6a7d6` → `8cae89d` の回）。過去形の測定記録である |
| `docs/adr/IADR-0201:69` | **直さない** | 「pin を `8cae89d` へ進めた」= 過去形の経緯 |
| `docs/adr/IADR-0204:22/38/39/45` | **直さない** | #790 の実装記録（起点 issue・作業仕様書リンク・当時の文脈） |
| `docs/adr/IADR-0205:38` | **直さない** | 「`8cae89d` で +3,002B 育った」= 走査基準つきの過去の実測 |
| `docs/adr/IADR-0212:34` | **直さない** | 「pin `8cae89d` で実読」= 走査基準つきの過去の実測 |
| `docs/specs/*`（7 ファイル） | **直さない** | **`status: done` でマージ済**。走査基準つきの過去の測定記録である（`traceability.repo.md`「確定済みの `docs/specs/` は書き換えない」） |
| `scripts/scripts.repo.test.js:309` | **直さない** | 過去のコミット件名を固定した fixture 文字列 |
| `scripts/scripts.repo.test.js:7472` | §3 の結論に従う | 分類 X の理由をコメントで持っている。X が解消するなら追随する |

**軸 2: 「現行 pin」**（導出語。sha を書いていない形も捕まえるため）。8 ヒット。
`docs/adr/IADR-0176:46` と `IADR-0177:47` は **sha を書かずに「現行 pin」とだけ述べており腐らない**ため直さない。
`docs/specs/` の 3 件は確定済みのため直さない。`scripts/scripts.repo.test.js:6061` は
**`NORMS` 配列のコメント**であり、固定している文字列は `'FR-01..22'`（今回不動）であって `ADR` レンジではない ——
**`ADR-0001..0046` を `..0047` にしてもこの試験は落ちない**（実測で確認する。§4）。

**軸 3: 「走査基準」**（規則 5 —— 軸を 1 本で終わらせない）。**`.claude/rules/traceability.repo.md:7` のみが規範**で、
残りは走査基準つきの過去の実測（ADR・別紙・`docs/specs/`・`scripts/` の docstring）である。
`scripts/scripts.repo.test.js:7499` は `'走査基準: planning'` という**部分文字列の存在**を固定しており、
**sha を含まない**ため今回の変更で落ちない。

### 2.3 黙って除外していないもの（規則 6）

- **`planning/` 配下**: submodule。**pin 更新のみで中身は触らない**（本リポから計画書を編集しない）
- **`src/ai-stock-trading/`**: 別プロジェクトの submodule。**触らない**
- **`.ai-context/`**: gitignore 対象。再生成しても PR に差分は載らない
- **`CHANGELOG.md`**: 生成物。手で書き足さない（`changelog.yml` が再生成する）

## 3. キット追随の再判定

（`IADR-0204` 決定 1 のラチェットと決定 2 の 3 点突合。**実測を §4 に貼る**）

## 4. 検証

（`IADR-0183` の順序: `git add -A` → 検査器 → コミット → `check-doc-updated` / `check-commit-messages`）

## 5. 射程外の発見（**本波では直さない。記録して次へ渡す**）

### 5.1 `NFR-11` の適用範囲がローカル検証環境（経路B）へ及ぶことが確定した

新 pin の `02_requirements/01_requirements.md` `NFR-11` に、**利用者裁定 2026-08-16（裁定依頼 planning#383）**が入った:

> **★ 適用範囲は環境を問わない** …… **ローカル検証環境（経路B）も適用内である。**
> 実装側は「`LOCALEDGE=1` が loopback へ bind する閉域であり『外部から到達し得る』に当たらない」と読んで
> 適用外として扱っていたが、**その読みは採らない。**

**実装側の読みが名指しで否定されている。** 証明書の発行方式は新設の `ADR-0047` が定める
（`*.localhost` のように DNS-01 も Vault PKI も取れないドメインでは selfsigned CA を許容する）。

**本波の射程（pin ＋ 改定 ＋ `.gitkeep`）の外**であるため、ここでは実装を変えない。**別 issue として起票する。**

### 5.2 確定済み仕様書に誤引用を 1 件見つけた（**書き換えない**）

`docs/specs/20260816_wave8-large-epic-reverification.md` は #440 の ADR ゲートの典拠として `IADR-0119` を引いているが、
**同 IADR の射程は `FR-17`〜`FR-21` / `ADR-0033`〜`0037` であり `ADR-0038` は登場しない**。
同書は **`status: done` でマージ済み**のため書き換えず、本書へ記録するに留める
（`traceability.repo.md`「確定済みの `docs/specs/` は書き換えない」）。

**なお #440 の着手軸は本 pin で 1 本減った** —— `ADR-0038` が `Accepted` になったため **ADR 軸は外れた**。
`dotnet` も入っているので**環境軸も外れる**。**残るのは先行 `#455` だけ**である。
