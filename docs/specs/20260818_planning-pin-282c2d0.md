---
title: 作業仕様書 — 計画 pin を 282c2d0 へ進め、traceability.md を分類 A へ戻して環流 3 件の追随を取り込む
type: spec
status: done
related_ids:
  - NFR
  - IADR-0033
  - IADR-0115
  - IADR-0190
  - IADR-0192
  - IADR-0204
  - IADR-0221
  - IADR-0223
author: claude
created: 2026-08-18
updated: 2026-08-18
plan_refs:
  - "../../planning/tools/impl-handoff-kit/repo-template/.claude/rules/traceability.md"
  - "../../planning/tools/impl-handoff-kit/repo-template/docs/traceability-appendix.md"
  - "../../planning/docs/ai-implementation-workflow-guide.md (§8 必読規約の予算 51,200 バイト)"
related_specs:
  - "20260817_planning-pin-b095b8f.md"
  - "20260817_issue-846_planning-pin-f216783-mcp-skill.md"
---

# 作業仕様書: 計画 pin `282c2d0` の追随

## 1. 起点となる ID（トレーサビリティ）

- **無採番 `NFR`**（キット追随・pin 更新＝メタ作業。`.claude/rules/traceability.md`「無採番 `NFR` を許す
  2 つの場合」の**場合 2**）。
- 関連: `IADR-0192` / `IADR-0204`（キット追随の分類と保留のラチェット）/ `IADR-0190`（必読の余白）/
  `IADR-0223`（保留の根拠は上限と下限の両方）/ `IADR-0221`・`IADR-0033`（Playwright の棲み分け）。

## 2. 母集合の引き方（実測）

**走査基準**: `claude/pr-847-review-fixes-3fqg6o` `e132a21`。**pin**: `b095b8f` → `282c2d0`（**2 コミット**）。

```text
git -C planning log --oneline b095b8f..282c2d0
  282c2d0 docs(NFR): 証跡に git 履歴を引く前の shallow 判定を作法として足す（planning#410 の裁定） (planning#412)
  5e75167 docs(kit): 別紙を分類 A へ作り直して下限まで純減させ、E2E ランナーの射程を分ける（planning#408 planning#409 planning#410） (planning#411)
```

**追随対象の母集合は記憶で挙げず、`node scripts/check-kit-sync.js` に列挙させた**（母集合の規則 9）。
pin を進めた直後の出力:

```text
[unclassified] docs/traceability-appendix.md が分類表に無い
[drift]        scripts/kit-sync-classification.example.json が分類 A なのにキットとバイト一致でない
```

これに、**分類 X として保留していた `.claude/rules/traceability.md`**（検査器は drift として上げない）と、
**分類 B の土台変更 3 件**（`CLAUDE.md` / `AI_SETUP.md` / `docs/ai-workflow.md`）を加えたものが母集合である。

**除外したもの**: `planning` 側の `draft/feedback/*` 3 件と `docs/ai-implementation-workflow-guide.md`
（計画リポの記録・正本であり、本リポが複写する対象ではない）。`HOWTO.md` も同様（キットの手引きであり配布物ではない）。

## 3. 🟢 環流 3 件がすべて計画側に着地した

前 PR で起票した 3 件は、いずれも計画側で採用された。

| 環流 | 計画側の対応 |
| --- | --- |
| planning#408（純減が下限に届かない／移設は 2 ファイル同時） | **別紙 `docs/traceability-appendix.md` を新設して下限まで純減**（25,207B → 23,917B）。HOWTO と運用ガイドへ「移設元と移設先を同時に取り込む」「増減は上限と下限の両方に照らす」を明記 |
| planning#409（Playwright の統一指示が CI の E2E と衝突） | `CLAUDE.md` / `HOWTO.md` / `AI_SETUP.md` §4-3 を**役割の棲み分け**へ改訂。本リポの `IADR-0033` / `IADR-0221` を実測例として明記 |
| planning#410（shallow clone の境界コミットを出典に引いた事故） | 運用ガイドと `CLAUDE.md` の監査節へ**「`git log` / `git blame` を出典に引く前に `git rev-parse --is-shallow-repository` を確かめる」**を追加 |

## 4. 🟢 `traceability.md` を分類 A へ戻した（#793 系の債務を解消）

キット版が **25,207B → 23,917B（−1,290B）** へ純減し、**本リポ版 24,592B より小さくなった**。

| 判定 | 値 |
| ---: | --- |
| 取り込みコスト | 23,917 − 24,592 = **−675B**（減る） |
| 取り込み後（`CLAUDE.md` 追加前） | total 49,457B / 余白 **1,743B** |
| **上限 51,200B** | ✅ 収まる |
| **下限 1,000B** | ✅ **満たす**（余裕 743B） |

**上限・下限の両方を満たすため取り込んだ。** 分類は **X → A**。
[`IADR-0223`](../adr/IADR-0223_deferral-ratchet-reads-both-budget-bounds.md) 決定 3 の 3 点を**同時に**行った。

1. キット原文で `.claude/rules/traceability.md` を上書き（**バイト一致を `cmp` で確認**）
2. **別紙 `docs/traceability-appendix.md` を新規に取り込み**（4,911B・分類 A）
3. `docs/ai-workflow.md` へ別紙への導線を追加（キット土台に合わせる）

**別紙は `docs/` に置かれるため必読集合に入らない**（必読は `CLAUDE.md` ＋ `.claude/rules/*.md`）。
これが計画側の純減が成立した仕組みである。

## 5. 🔴 `CLAUDE.md` の土台追加 4 点のうち 1 点だけを取り込んだ

キット `CLAUDE.md` は **18,007B → 19,152B（+1,145B）** へ育った。**4 点すべてを入れると余白が
1,743B → 598B となり下限 1,000B を割る**（実測）。そこで 1 点ずつ判定した。

| # | 追加 | 実測 | 判定 |
| --- | --- | ---: | --- |
| 1 | Playwright の役割棲み分け | 388B | **見送り**。本ファイルは元々この行を持たない（`IADR-0221` 決定 3 で撤回済み）。正本は `AI_SETUP.md` §4-3 |
| 2 | **shallow 判定を出典の作法に** | 444B | **取り込み**。本リポが 3 度踏んだ事故の作法であり、**毎セッション効く** |
| 3 | 予算は上限と下限の両方に照らす | 268B | **見送り**。正本は運用ガイド §8 で本ファイルは既に導線を張っており、**`IADR-0223` 決定 1 が機械で強制する** |
| 4 | 別紙移設 pin では移設元と移設先を同時に | 270B | **見送り**。**`IADR-0223` 決定 3 がラチェットの失敗文へ埋め込み済み** |

**規範は 1 つも失っていない**（見送った 3 点はいずれも正本または機械が持つ）。判断は
`CLAUDE.md` の分類表の注記に残した。取り込み後の実測は **total 49,902B / 余白 1,298B**（下限比 +298B）。

## 6. 変更内容

| ファイル | 分類 | 変更 |
| --- | --- | --- |
| `planning` | —— | gitlink を `b095b8f` → `282c2d0` |
| `.claude/rules/traceability.md` | **X → A** | キット原文で上書き（23,917B・バイト一致） |
| `docs/traceability-appendix.md` | **新規・A** | キットの別紙を取り込み（4,911B・バイト一致） |
| `scripts/kit-sync-classification.example.json` | A | キット原文へ追随（別紙を A へ足す 1 行） |
| `CLAUDE.md` | B | 監査節へ shallow 判定の作法 1 行（+444B） |
| `AI_SETUP.md` | B | §4-3 をキット新版へ差し替え、固有デルタを具体名だけに縮小 |
| `docs/ai-workflow.md` | B | 別紙への導線節を追加 |
| `scripts/kit-sync-classification.json` | C | A へ 2 件・B から 1 件・`$comment` の pin・注記 2 件 |

## 7. 検証

**`planning` を pin `282c2d0` で populate した状態**で実測した。

```text
node scripts/check-kit-sync.js        OK: キット 117 件（A 81 / B 24 / C 4 / 対象外 8）   exit=0
node scripts/check-adr-numbering.js                                                       exit=0
node scripts/check-doc-links.js                                                           exit=0
node scripts/check-cross-repo-refs.js                                                     exit=0
node scripts/check-plan-id-qualification.js                                               exit=0
node scripts/check-doc-type-vocabulary.js                                                 exit=0
node scripts/check-feedback-dispatched.js                                                 exit=0
node scripts/check-reading-budget.js  warn 49,902B / 51,200B（97.5%）・余白 1,298B         exit=0
REQUIRE_REPO_TESTS=1 node scripts/scripts.test.js   ✓ 651 tests passed
```

**変異試験**（`#790 / #793` が空振りしていないことの確認）—— 分類を **B（X）へ戻すと保留側が鳴る**。

```text
AssertionError: キット版を取り込める（49902B <= 51200B かつ余白 1298B >= 下限 1000B）。
  保留の根拠が消えたので ... 分類 A へ戻すこと（#793）。**取り込みは docs/ai-workflow.md への転記と同時に行う** ...
```

**下限側も生きている** —— `READING_BUDGET_FLOOR=2000` にすると `#730` が
`必読の余白が 1298B まで減った（下限 2000B）` で鳴る。

### 検査していない範囲

- **`#790 / #793` と `#730` は PR の CI では走らない**（[`IADR-0058`](../adr/IADR-0058_doc-links-planning-submodule-ci.md)
  により private な `planning` を PR の CI で取得しないため）。**「CI が green」は本検査の裏付けにならない。**
  上記のとおり手元で populate して実走した。**配線をどこへ寄せるかは `IADR-0223` §未解決のまま**（本 PR では変えない）。
- 計画側の `draft/feedback/*` と運用ガイドの本文は**読んだが複写していない**（§2 の除外）。

## 8. 未了

| # | 内容 | 理由 |
| --- | --- | --- |
| 1 | **`#790 / #793`・`#730` を planning 込みで走らせる場所の決定** | `IADR-0223` §未解決。本 PR の射程外 |
| 2 | **`.claude/settings.json` への `mcp__context7__*` 追加と `/mcp` の接続確認** | 同ファイルは `Edit` / `Write` とも deny のため代行できない。**利用者が適用する**（#847 から継続） |
