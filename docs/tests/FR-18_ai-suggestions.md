---
title: FR-18 AI 提案（リンク・タグ）テスト仕様書
type: test-spec
status: draft
created: 2026-08-22
updated: 2026-08-28
author: claude
---
<!-- trace:
ids: [FR-18]
adrs: [ADR-0033, ADR-0034, ADR-0051]
iadrs: [IADR-0242, IADR-0266]
specs: [20260822_issue-914_ai-suggestion-state-machine, 20260823_issue-915_ai-suggestion-generation]
issues: [#450, #914, #911, #915]
-->

# テスト仕様書: AI 提案（リンク・タグ）

## 対象

AI が提案したリンク候補・タグ候補の**永続と状態遷移**。提案の**生成**（別 issue）と、一覧を消費する**画面**（別 issue）は本書の対象外である。

## 3 状態

| 状態 | 意味 | 探索結果 |
| --- | --- | --- |
| `pending` | 承認待ち | **含めない** |
| `approved` | 承認済み | 含める（確定リンクとして） |
| `rejected` | 却下 | **含めない**。再提示の抑止に用いる |

🔴 **探索側にフィルタは無い。** 未承認の提案は辺を持たず、承認して初めて辺が生まれる —— **絞る対象が存在しない**のが正しい形である。「実装しなかった」のではなく「構造的に不要」であることを T-05 が固定する。

## テストケース

| ID | 前提条件 | 手順 | 期待結果 | 対応受け入れ基準 | 区分 |
| --- | --- | --- | --- | --- | --- |
| T-01 | `pending` の提案 | 承認 / 却下 | いずれも通る | 3 状態 | 自動 |
| T-02 | `approved` / `rejected` の提案 | 承認・却下を試みる | **拒まれ、状態が一切変わらない** | 3 状態 | 自動 |
| T-03 | 権限内の両端 | リンク提案を承認 | 提案由来の辺が**ちょうど 1 本**できる | 承認で辺になる | 自動 |
| T-04 | 片端が権限外 | 同上 | **404 で拒み、辺を作らない** | 権限外を出さない | 自動 |
| T-05 | 未承認（`pending` / `rejected`）の提案 | 何もしない／却下する | **辺が 1 本もできない** | 未承認は根拠に使わせない | 自動 |
| T-06 | 承認済みの提案 | もう一度承認 | 409。**辺が二重にならない** | 3 状態 | 自動 |
| T-07 | 却下 → 解除 → 再却下 | 却下回数を見る | **1 → 2 と累積する** | 却下回数の保持 | 自動 |
| T-08 | 却下済み | 端点の指紋が変わる | `pending` へ戻り、**どちらの端点が変わったか**が理由に入る（陽性対照） | 本文変更で却下解除 | 自動 |
| T-09 | 却下済み | 指紋が変わらない | **解除されない**（T-08 の対） | 本文変更で却下解除 | 自動 |
| T-10 | 解除後 | 却下の記録を見る | **却下日時・解除日時・解除理由が残る**（削除されていない） | 解除は無効化として記録 | 自動 |
| T-11 | 解除後 | 同じ指紋で再判定 | 解除されない（**指紋が新しい値へ進んでいる**） | 本文変更で却下解除 | 自動 |
| T-12 | 端点が 1 つの提案 | もう一方の指紋を変える | **解除されない**（端点が無いので判定に使わない） | 本文変更で却下解除 | 自動 |
| T-13 | 一覧を引く | 既定の状態で取得 | `pending` が返り、**権限外の文書に関する提案は現れない** | 権限外を出さない | 自動 |
| T-14 | — | ルート表を走査 | **一括承認の口が存在しない**。承認の口は必ず id を取る | 一括承認を提供しない | 自動 |
| T-15 | 却下済みの提案・端点文書の本文が変わる | 文書更新イベントを購読側へ与える | **本文指紋が変われば `pending` へ戻る**（指紋が同じ・不明（null）・イベントが古い場合は戻らない） | 却下は本文変更で解除 | 自動 |
| T-16 | 権限外の文書が候補に含まれ得る | 提案を生成する | 🔴 **権限外の文書が LLM 要求の本体に一度も現れない**（要求ペイロードを捕捉して照合） | 提案先利用者の権限スコープ外の文書本文を LLM へ送信しない | 自動 |
| T-17 | 同上 | 候補列挙の戻り値を見る | **権限外ノードが 1 件も返らない**（絞りは列挙の段で完了している） | 同上 | 自動 |
| T-18 | 権限内の文書がある | 提案を生成する | **権限内の候補は要求ペイロードに現れる**（陽性対照。T-16 が「何も送っていないから緑」になるのを防ぐ） | 同上 | 自動 |
| T-19 | 権限外を除外した | 要求ペイロードを見る | **除外した件数を漏らさない**（件数フィールドが無い） | 同上（件数も情報である） | 自動 |
| T-20 | 起点が権限外 | 提案を生成する | **404 を返し、LLM を 1 度も呼ばない** | 同上 | 自動 |
| T-21 | スコープが不許可 | 提案を生成する | **404 を返し、LLM を 1 度も呼ばない** | 同上 | 自動 |
| T-22 | LLM が権限外の文書を指す提案を返した | 保存する | **その提案を捨てる**（LLM の出力を信用しない） | 同上 | 自動 |
| T-23 | — | 型を走査 | **`SuggestionPrompt` に公開コンストラクタが無く、生成経路はスコープを要求する 1 本だけ** | 同上（値として作れないことで塞ぐ） | 自動 |
| T-24 | — | 型を走査 | **LLM ポートは封をしたプロンプトしか受け取らない** | 同上 | 自動 |
| T-25 | — | 応答を見る | **裸の配列で、件数フィールドを持たない** | 同上 | 自動 |

## 🔴 T-15 について — 未配線の追跡から「発火の固定」へ書き換えた

従前の T-15 は「却下解除の判定に本番からの呼び出し元が 0 件」を**本番ソースの走査で固定する**
テストだった（判定ロジックだけが在り、発火経路が無かったため）。**発火経路（文書更新イベントの
購読）が配線されたので、その走査テストは所定の手順どおり削除し、発火の受け入れ基準を本 T-15 へ
写した** —— 本文指紋が変われば `pending` へ戻る。**更新時刻での代用は採らない**（指紋の変化のみが
条件。タグ・属性だけの更新で却下が解除され同じ提案が再び現れる、という広すぎる解除を防ぐ）。

## 実装マッピング

- `AiSuggestionStateMachineTests`（T-01・T-02・T-07〜T-12。ドメイン層）
- `AiSuggestionEndpointsTests`（T-03〜T-06・T-13・T-14。API 層）
- `GraphDocumentSyncConsumerTests`（T-15。購読ハンドラ経由の発火。指紋同値・不明・追い越しの否定形と対で置く）
- `AiSuggestionGenerationTests`（T-16〜T-22。生成経路。要求ペイロードを捕捉して照合する）
- `SuggestionPromptGateTests`（T-23〜T-25。**型でしか表せない境界**を走査で固定する）

### T-16〜T-25 について — 受け入れ基準「権限スコープ外の文書本文を LLM へ送信しない」の写像

計画側の受け入れ基準は「提案の生成時に、提案先利用者の権限スコープ外の文書本文が LLM へ
送信されない（監査ログで確認できる）」である。**この 1 行は 2 つの異なる主張に分かれる**ため、
表でも分けて写像した。

1. **走らせたときに送られないこと**（T-16〜T-22）。**否定形（T-16）だけでは足りない** ——
   何も送らない実装でも緑になるため、**陽性対照（T-18）と対で置く**。T-19 は「件数も情報である」
   という基準の読み方を固定する（何件除外したかが分かれば、存在の有無が漏れる）。
2. **そもそも送れる形の値を作れないこと**（T-23〜T-25）。「渡してから捨てる」と
   「渡せる値が存在しない」の差は後段フィルタの有無ではなく、**非許可ノードを持つ値が
   呼び出しの引数として存在し得るか**である（この判断の記録は実装 ADR 側にある）。

**監査ログでの確認は本表の対象外である。** 監査ログの出力そのものは実装済みだが、
その内容を検証するテストは置いていない（1 の 7 本が、ログを見るまでもなく送信自体を否定している）。

### 変異試験で確かめたこと

- **T-08 だけでは「常に解除する実装」を検出できない** —— T-09（変わらなければ解除しない）と対で置いて初めて検出できる。実測で確認した
- **T-05 は承認の口を壊しても落ちない**（approve を呼ばないため）。**対応表に書いたから落ちるはず、で終わらせず**、T-05 に届く変異（却下でも辺を作る）を別に当てて検出力を実測した
- **旧 T-15（未配線の走査）は前提（未配線）が変わったため削除した** —— 落ちたときの指示どおり、発火の受け入れ基準を現 T-15 へ写した
