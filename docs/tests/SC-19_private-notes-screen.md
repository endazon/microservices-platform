---
title: SC-19 個人資料管理画面 テスト仕様書
type: test-spec
status: completed
created: 2026-08-23
updated: 2026-09-12
author: Claude
---
<!-- trace:
ids: [FR-19, FR-21, UC-11, SC-19, SC-20]
adrs: [ADR-0031, ADR-0037, ADR-0046, ADR-0057, ADR-0098]
iadrs: [IADR-0131, IADR-0135, IADR-0270, IADR-0296, IADR-0437, IADR-0444, IADR-0445, IADR-0447, IADR-0449]
specs: [20260823_issue-451_private-note-obsidian-sync-core, 20260828_issue-451a_private-notes-bff, 20260828_issue-451c_sc19-sc20-screens, 20260912_1441-1442_private-note-contract-gaps, 20260912_1445-1446_share-targets-and-sync-history, 20260912_1447-1448_current-groups-binding-and-set-valued-matching]
issues: [#451, #1441, #1445, #1447, planning#614, planning#618]
-->

# テスト仕様書: 個人資料管理画面

## テスト対象・範囲

> **`status: completed` の意味**: **画面（フロントエンド）が実装され、本書の観点が実測で緑になった**。
> 従前の「先取りの台帳」は下の表へ実測として置き換わっている。
>
> 🔴 **画面が持っていない機能は本書の対象外である。** ［2026-09-12］公開範囲の 3 状態・同期状態・タグと
> その絞り込みは実装され、本書の観点に加わった。**指定先（誰と共有しているか）の一覧・検索・追加・
> 取り消しも同日に加わり、同日の 2 つ目の変更でグループ指定も加わった**
> （**「グループ指定は意図した不在である」と書いていた前段の記述は本追記が置き換える** ——
> 認可がグループ共有を評価するようになったためである）。
> **依然として無いのは、緊急アクセス・リンク作成**であり、いずれも呼ぶ口が無い。
> 本書はそれぞれを**陽性対照つきの陰性のケース**として持つ。
> 「テストが緑＝計画の全要素が満たされた」ではない —— 差分は画面仕様書の対応表が持つ。

- バックエンド API の写像は [FR-19_private-notes-lifecycle](FR-19_private-notes-lifecycle.md) が正。
- 認可（無認証 401・他人の資料は 404・書き込みの拒否）は BFF 側のテストが固定しており、
  画面側では再現しない（判定軸を 2 本持たない）。

## 実体

| 層 | テストクラス／ファイル | 主に固定していること |
| --- | --- | --- |
| BFF（認可） | `BffPrivateNoteEndpointTests` | 無認証は 401 ／ 他人の資料は 404（403 にしない）／ 書き込みの write スコープ 403 と、それぞれの陽性対照 |
| 画面 | `sc19-private-notes/components/PrivateNotesPage.test.tsx` | 固定文言・容量の段階・確認ダイアログ・一括操作・露出トグル・否定形 |
| 純関数 | `sc19-private-notes/types/quota.test.ts` | 段階警告の境界（両側）・内訳の合算・残り日数の切り上げ |
| 純関数 | `sc19-private-notes/types/noteBadges.test.ts` | 公開範囲・同期状態の値 → 表示状態と色の対応、**未知の値を既知へ丸めないこと**、タグ選択肢の重複排除 |
| 共有部品 | `components/ConfirmDialog.test.tsx` | 押すまで実行されないこと・初期フォーカス・Escape |
| ブラウザ E2E | `platform/frontend/e2e/sc19-private-notes.smoke.spec.ts` | 実ブラウザ・実ビルド成果物での描画（遅延チャンクの読み込みを含む）と、ユースケースの基本フロー 1・4 と例外フロー |

## 観点

- 🔴 否定形（本文編集欄が無い・管理者導線が無い・他人の資料の件数が出ない）は**陽性対照と対で**置く。
  何も描かない実装でも否定形だけなら緑になる。
- 🔴 **時刻は固定する**（残り日数と警告色の境界が「いま」に依存するため）。実時間のままだと
  日付をまたいだ瞬間に赤くなるテストになる。
- 境界（80 / 95 / 100 の使用率、残り 7 日と 8 日）は**両側**を置く。
- 🔴 **状態の検査は表の中だけを見る。** 絞り込みの選択肢にも同じ語が並ぶため、画面全体で引くと
  **列が無くても「選択肢が在る」だけで緑になる。**
- 🔴 **契約が文字列で運ぶ値には、未知の値の観点を置く。** 既知の 3 値だけを見るテストは、
  未知を既知へ丸める実装でも緑になる（丸めると、共有されている資料が非公開に見える）。
- 🔴 **識別子の陰性は「出さない値」の側から引く。** 指定先の識別子は 2 つの名前空間にあり
  （利用者名とグループの識別子）、**片方だけを隠す実装でも一方の観点だけなら緑になる**。
  グループの識別子は接頭辞つきと UUID の両方の形を検体に含め、**本文だけでなく属性値まで走査する**。

## テストケース一覧（計画の受け入れ基準からの写像）

| # | 受け入れ基準 | 実測 |
| --- | --- | --- |
| 1 | 論理削除の確認時に「削除しても容量は空かない（90 日保管）」が表示される | 緑（対象名の提示・押すまで要求が飛ばないことも同時に固定） |
| 2 | 完全削除の確認時に ①復元不可 ②90 日待てば自動 ③解放される容量 が表示される | 緑（①には但し書きを含む。**根拠は索引・グラフへの伝播が非同期であること**であり、実体削除の未配備ではない） |
| 3 | 削除済み一覧から単票・複数選択の一括完全削除の双方が実行できる | 緑（一括は単票と同じ口へ送ることも固定） |
| 4 | ラベルは「個人資料」で統一し、アイコン＋ラベル併用（色だけで意味を持たせない） | 緑（「マイスペース」「個人メモ」の不在を陽性対照と対で固定） |
| 5 | 一括完全削除の選択内容が実行直前に再提示される | 緑（対象件数と解放容量の合計を提示） |
| 6 | 業務関連資料としての扱いの固定文言が常時表示され、折りたたまれない | 緑 |
| 7 | 容量が使用量・上限・「うち削除済み」の内訳とともに出る | 緑（内訳は削除済み行の合算） |
| 8 | 80 / 95 / 100 で警告が段階的に変わり、同時に 2 段は出ない | 緑（4 段すべてと「95% で 80% を出さない」を固定） |
| 9 | 100% で新規作成のみ拒否され、既存資料の操作は残る | 緑（作成の無効化と削除ボタンが生きていることを対で固定） |
| 10 | 本文の入力欄も編集導線も無く、Obsidian 連携への導線がある | 緑（作成フォームの存在を陽性対照に置く） |
| 11 | 作成はタイトルだけを送り、本文を運ぶ項目を送らない | 緑（要求本文のキーを検査） |
| 12 | 削除済みタブの件数バッジが一覧の件数と一致する | 緑 |
| 13 | 残り 7 日以内だけが警告色＋文言になる | 緑（3 日と 60 日の両側） |
| 14 | 復元が単票・一括の双方でできる | 緑 |
| 15 | 露出 3 トグルが独立に設定でき、既定はいずれもオフ | 緑（変えた 1 つだけが真になることを要求本文で固定） |
| 16 | 空状態（作成導線つき）を描く | 緑 |
| 17 | タイトルで絞り込め、条件が URL に載る | 緑 |
| 18 | 未知のタブ名を既定へ倒す | 緑 |
| 19 | 一覧を引けないときは空状態へ縮退しない | 緑 |
| 20 | 管理者導線・他人の資料を示唆する表示が無い | 緑（自分の資料が見えることを陽性対照に置く） |
| 21 | 未認証で開くとログインへ誘導される | 緑（ブラウザ E2E） |
| 22 | 認証済みの利用者が実ブラウザで画面へ到達し、固定文言と個人資料の記号が出る | 緑（ブラウザ E2E。本文編集欄の不在を陰性対照として対で置く） |
| 23 | 作成した資料が一覧へ現れ、露出 3 トグルがすべてオフである | 緑（ブラウザ E2E。要求本文が本文を運ばないことを陰性対照として対で置く） |
| 24 | 使用量 100% で新規作成だけが止まり、既存資料の更新は止まらない | 緑（ブラウザ E2E。非対称そのものを陽性・陰性の対で見る） |
| 25 | 一覧に公開範囲・同期状態・タグの 3 列が出る | 緑（利用中タブ。削除済みタブでは同期状態の列を出さないことも対で固定） |
| 26 | 公開範囲の 3 状態が色ではなく文言で区別され、共有件数が併記される | 緑（3 状態を同時に描く一覧で固定） |
| 27 | 同期状態の 3 状態が色ではなく文言で区別される | 緑（同上） |
| 28 | 契約に無い値を既知の状態へ丸めず「判定できません」と出す | 緑（丸めていないことを陰性の対で固定） |
| 29 | 公開範囲・同期状態・タグの 3 軸で絞り込め、条件が URL に載る | 緑（未知の絞り込み値を「絞らない」へ倒すことも固定） |
| 30 | 一覧の行に指定先（共有相手）が出ない | 緑（3 状態が描かれていることを陽性対照に置き、バッジ自体が導線でないことも見る） |
| 31 | 実ブラウザで 3 列が出て、行に指定先が現れない | 緑（ブラウザ E2E） |
| 32 | 実ブラウザで絞り込みが URL に載り、URL だけで再現できる | 緑（ブラウザ E2E） |
| 33 | 利用中タブの行操作から共有先の変更ダイアログが開く | 緑（**開く前は共有先を 1 度も引かない**ことを陰性の対で固定） |
| 34 | 削除済みタブの行に共有先の変更を置かない | 緑（復元の導線が在ることを陽性対照に置く） |
| 35 | 現在の指定先が表示名で並び、各行に取り消しがある | 緑（無効化済みは「（無効化済み）」を併記し、引けなかった名前は「（不明な利用者）」として**行を残す**） |
| 36 | 🔴 利用者識別子が DOM に現れない | 緑（表示名が現れることを陽性対照に置く。本文だけでなく属性値まで走査する） |
| 37 | 名前で検索して候補から追加でき、送る本文が個人指定に固定される | 緑（候補は表示名だけ・**自分自身と共有済みは候補から除く**） |
| 38 | 2 文字未満では検索せず、理由を書く | 緑（要求が 1 件も出ないことを対で固定） |
| 39 | 取り消しが個人指定のパスへ送られる | 緑 |
| 40 | ~~🔴 グループを選ぶ導線・文言が無い~~ → **指定先の種別を選べる**（既定は個人） | 緑（個人の追加・取り消しが在ることを陽性対照に置く。［2026-09-12］**観点を反転した** —— 認可がグループ共有を評価するようになり、導線を置かない理由が消えたためである） |
| 41 | ~~グループ共有が残っている資料では、本画面で変更できない旨を告げる~~ → **告知の撤去** | 緑（グループの行が並ぶことと対で、告知の文言が 1 つも残っていないことを固定） |
| 42 | 追加に失敗したら理由を出し、ダイアログを閉じない | 緑 |
| 43 | 閉じる・Esc のいずれでも降りられ、台帳を変える要求は出ない | 緑 |
| 44 | 実ブラウザで 個人の 一覧 → 検索 → 追加 → 取り消し を踏め、識別子が現れない | 緑（ブラウザ E2E。`getByRole` だけで引く。［2026-09-12］**「グループの語も現れない」は観点から外した** —— 種別の選択を置いたため語そのものは現れる。**現れてはならないのは識別子だけである**） |
| 45 | 共有先ダイアログ（候補の listbox つき）に axe の違反が無い | 緑（ブラウザ E2E。**本リポジトリで唯一 `listbox` / `option` を持つ面**であり、既存の 5 面では評価されない規則が走る。［2026-09-12］種別の選択を足したあとも違反 0 件であることを実測した） |
| 46 | 既存のグループ共有が表示名（＋パス）で並び、各行に取り消しがある | 緑（引けなかったグループは「（見つからないグループ）」として**行を残す** —— 消すと取り消せなくなる） |
| 47 | 🔴 グループの識別子が DOM に現れない | 緑（表示名とパスが現れることを陽性対照に置く。接頭辞つきと UUID の両方の形を検体に含め、属性値まで走査する） |
| 48 | グループ名で検索して候補から追加でき、送る本文がグループ指定になる。取り消しもグループのパスへ送られる | 緑（**既に共有済みのグループは候補から除く**ことも同時に固定） |
| 49 | 種別を切り替えると検索語と選択を捨て、切り替えただけでは問い合わせない | 緑（別の名前空間の値を持ち越さない。追加ボタンが押せない状態へ戻ることも見る） |
| 50 | 実ブラウザで グループの一覧 → 検索 → 追加 → 取り消し を踏め、識別子が現れない | 緑（ブラウザ E2E。`getByRole` だけで引く） |

## ブラウザ E2E で見ていること・見ていないこと

- **見ている**: ビルド成果物の上でルート単位の遅延チャンクが実際に読み込まれ、画面が描かれること。
  経路の取り違え（パスの改名）は**ここでだけ落ちる** —— 未認証の往復では見分けられない（受け皿が
  認証ガードの配下に居るため）。変異試験で 3 件が落ちることを実測した。
- 🔴 **見ていない**: 後段の実応答との一致。セッションと BFF 応答はネットワーク層のスタブであり、
  **契約の写しであって後段ではない。** 実基盤を伴う実行は別 issue（#466）が持つ。

## 実行

```bash
cd src && pnpm run test
cd src/platform/frontend && pnpm exec playwright test e2e/sc19-private-notes.smoke.spec.ts
```

## 関連仕様

- 画面仕様書: [個人資料管理](../screens/SC-19_private-notes.md)
- テスト仕様書: [Obsidian 連携設定画面](SC-20_obsidian-settings-screen.md)
