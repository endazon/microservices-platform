---
title: SC-20 Obsidian 連携設定画面 テスト仕様書
type: test-spec
status: completed
created: 2026-08-28
updated: 2026-09-12
author: Claude
---
<!-- trace:
ids: [FR-19, FR-20, UC-11, SC-19, SC-20]
adrs: [ADR-0031, ADR-0036, ADR-0037, ADR-0046, ADR-0099]
iadrs: [IADR-0131, IADR-0135, IADR-0270, IADR-0352, IADR-0437, IADR-0444, IADR-0446]
specs: [20260823_issue-451_private-note-obsidian-sync-core, 20260828_issue-451a_private-notes-bff, 20260828_issue-451c_sc19-sc20-screens, 20260912_1441-1442_private-note-contract-gaps, 20260912_1445-1446_share-targets-and-sync-history]
issues: [#451, #1442, #1446, planning#614, planning#618]
-->

# テスト仕様書: Obsidian 連携設定画面

## テスト対象・範囲

> **`status: completed` の意味**: **画面（フロントエンド）が実装され、本書の観点が実測で緑になった**。
> 従前の「先取りの台帳」は下の表へ実測として置き換わっている。
>
> ［2026-09-12］**同期対象範囲（フォルダの指定）と競合の解決（一覧・2 ペイン差分・3 択）が加わった。**
> **同日に同期履歴も加わった**（供給元・表示件数・保持期間・行の内容・失敗の扱いが裁定で定まったため）。
> 🔴 **一時停止／再開は依然として無い**（呼ぶ口が無い）。
> 🔴 **履歴の行に資料のタイトルと Vault のパスが無いことは、陽性対照（端末の列は在る）と対で固定する** ——
> 「列が無い」だけなら何も描かない実装でも緑になる。
> 差分は画面仕様書の対応表が持つ。

- 同期プロトコルそのもの（マニフェスト・push・pull・削除の伝播）の写像は
  [FR-20_obsidian-sync](FR-20_obsidian-sync.md) が正である。本書は**設定画面が触る面**を扱う。
- 同期トークンは**ブラウザセッションとは別系統の資格情報**であり、画面はトークンで API を呼ばない。

## 実体

| 層 | テストクラス／ファイル | 主に固定していること |
| --- | --- | --- |
| BFF（認可） | `BffPrivateNoteEndpointTests` | 無認証は 401 ／ 他人の端末は 404（403 にしない）／ 平文トークンは発行応答にだけ現れる ／ 失効は文書ポリシーの有無に依存しない |
| 文書サービス | `SyncDeviceTokenTests` | 有効期限 30 日・手動再発行のみ ／ 個別失効・一括失効 ／ 他人の端末に到達できない |
| 画面 | `sc20-obsidian-settings/components/ObsidianSettingsPage.test.tsx` | 固定文言 3 段落・4 状態の描き分け・平文の一度きり表示・確認ダイアログ・否定形 |
| 純関数 | `sc20-obsidian-settings/types/deviceState.test.ts` | 4 状態の境界（残り 7 日と 8 日）・失効の最優先判定 |
| 純関数 | `sc20-obsidian-settings/types/syncFolders.test.ts` | パスの正規化（前後の区切りだけを落とす）・表記違いの重複を追加させないこと |
| ブラウザ E2E | `platform/frontend/e2e/sc20-obsidian-settings.smoke.spec.ts` | 実ブラウザ・実ビルド成果物での描画（遅延チャンクの読み込みを含む）と、本文を書く経路＝端末接続の導線 |

## 観点

- 🔴 否定形（他人の端末に到達できない・平文が一覧に出ない・承認ステップが無い・自動更新が無い）は
  **陽性対照と対で**置く ——「常に 404」「常に空」の実装でも陰性だけなら緑になる。
- **失効は通す側で固定する。** 個別失効は端末紛失時の唯一の防御線であり、
  文書側のポリシー整備状況に依存して塞がってはならない。**画面側でも状態を理由に隠さない。**
- **自動更新（リフレッシュ）の口が存在しないこと**は、API の一覧に現れないことと、
  画面に導線が無いことの両方で担保する。
- 🔴 **時刻は固定する**（残り日数・期限切れの境界が「いま」に依存するため）。
- 🔴 **「外す」と「削除」は、文言と要求の両方で見る。** 確認文が「削除ではない」と言っていることに加え、
  **資料を消す口を 1 つも叩いていない**ことを確かめる（文言だけ直して口が残る実装を通さない）。
- 🔴 **自動解決の不在は 3 択の存在と対で置く。** 3 択が描かれない実装でも、陰性だけなら緑になる。
- **区画は表示名（`region` のアクセシブル名）で引く。** 同じ語が複数の区画に現れるため、
  画面全体で引くと別の区画の要素を掴む。

## テストケース一覧（計画の受け入れ基準からの写像）

| # | 受け入れ基準 | 実測 |
| --- | --- | --- |
| 1 | 端末一覧が「有効（残り日数）／期限切れ間近（残り 7 日以内）／期限切れ／失効」を区別して表示する | 緑（4 値すべてを同時に描く一覧で固定） |
| 2 | トークンは発行直後に一度だけ表示し、再表示できない（再発行の導線を同じ行に置く） | 緑（発行前に平文が無いことを陰性の前提に置く） |
| 3 | 期限切れ後もプラグイン設定には旧トークンが残る旨の固定文言が表示される | 緑 |
| 4 | 一括失効の確認ダイアログに復元不可である旨が表示される | 緑（対象台数も提示。押すまで要求が飛ばないことも固定） |
| 5 | 同期対象フォルダの指定 UI の隣に、業務関連資料としての扱いの固定文言がある | 緑（区画の中に入力欄と文言の両方があること、**画面上部に重複して残していないこと**を対で固定） |
| 6 | 露出 3 トグルが独立に設定でき、既定はいずれも OFF である | 🔴 **本画面では未達**（資料単位の設定は個人資料管理画面のテストが固定。利用者単位の既定は口が無い） |
| 7 | 失効済み以外の全行に個別失効がある（期限切れの行にも残る） | 緑（失効済みの行に無いことと対で固定） |
| 8 | 期限切れの行から同じ行で再発行できる | 緑 |
| 9 | 再発行がその端末の口を叩き、平文を一度だけ出す | 緑 |
| 10 | 次の操作を始めると平文の表示が消える（画面に残さない） | 緑 |
| 11 | 自動更新（リフレッシュ）の導線が無い | 緑（手動再発行の存在を陽性対照に置く） |
| 12 | 端末登録に管理者承認のステップが無い | 緑（発行ボタンが押せることを陽性対照に置く） |
| 13 | 組織文書・他利用者の設定への導線が無い | 緑（自分の個人資料への導線を陽性対照に置く） |
| 14 | 固定文言 3 段落が出る（削除の説明に「90 日を過ぎると復元できない」を含む） | 緑 |
| 15 | 業務関連資料の説明を削除の説明と同じ段落にまとめない | 緑（同じ段落に 90 日の文が無いことを固定） |
| 16 | 端末が 0 台のときは空状態を出し、一括失効を押せない | 緑（発行の導線は残ることを対で固定） |
| 17 | 一覧を引けないときは空状態へ縮退しない | 緑 |
| 18 | 未認証で開くとログインへ誘導される | 緑（ブラウザ E2E） |
| 19 | 認証済みの利用者が実ブラウザで画面へ到達し、同期の範囲と削除の固定文言が出る | 緑（ブラウザ E2E。承認ステップと他利用者への導線の不在を陰性対照として対で置く） |
| 20 | 発行した平文トークンが一度だけ出て、一覧にも再読込にも戻らない | 緑（ブラウザ E2E。応答の状態コード 201 まで契約どおりでないと画面は何も描かない） |
| 21 | 同期対象フォルダのパス・配下の資料数・最終同期が出る | 緑 |
| 22 | フォルダ未設定は「すべての個人資料が対象」と明言する（異常として見せない） | 緑（空でも追加の導線が残ることを陽性対照に置く） |
| 23 | フォルダの追加が「いまの一覧に足した全量」を送る | 緑（前後の区切りを落とした形で送ることも固定） |
| 24 | 空・重複（表記違いを含む）のパスでは追加ボタンを押せない | 緑（別のフォルダなら押せることを陽性対照に置く） |
| 25 | 「対象から外す」の確認に「削除ではない・資料は消えない」が出る | 緑（外した 1 件を除いた全量を送り、資料を消す口を叩かないことも対で固定） |
| 26 | 未解決の競合をタイトル・パス・検出日時・端末・版とともに一覧する | 緑 |
| 27 | 競合が 0 件のときは「競合はありません」を出す | 緑 |
| 28 | 一覧は本文を持たず、行を選んで初めて両版の本文を引く | 緑（選ぶ前に詳細を引いていないことを陰性の前提に置く） |
| 29 | 2 ペイン（ローカル版／サーバ版）を並べて読める | 緑 |
| 30 | 3 択（ローカル採用／サーバ採用／両方を残す）だけを置き、自動解決の選択肢を置かない | 緑（3 択の存在を陽性対照に置く） |
| 31 | 3 択のいずれも確認を経てから送られ、確認文が択ごとに何が起きるかを言い分ける | 緑（押すまで要求が飛ばないことも固定） |
| 32 | 解決したら競合の一覧を引き直し、2 ペインを閉じる | 緑 |
| 33 | 実ブラウザでフォルダを外すと「削除ではない」旨が出て、全量が送られる | 緑（ブラウザ E2E） |
| 34 | 実ブラウザで競合を 3 択から解決でき、自動解決の択が無い | 緑（ブラウザ E2E） |
| 35 | 同期履歴が 実行日時 / 端末 / 方向 / 内訳 / 結果 / 失敗理由 の 6 列で出る | 緑 |
| 36 | 🔴 履歴にタイトル・パスの列が無い | 緑（端末の列が在ることを陽性対照に置く） |
| 37 | 方向が「送信」「受信」で出て、未知の値は生値のまま出る | 緑（純関数のテストが未知を既知へ丸めないことを固定） |
| 38 | 内訳は 0 の項目を出さず、すべて 0 の行は「—」になる | 緑 |
| 39 | 結果を色だけでなく「成功」「失敗」の文言でも示す | 緑 |
| 40 | 失敗理由の 7 コードすべてに「次に何をすればよいか」の文言が在り、コードをそのまま出さない | 緑（7 件の文言が互いに異なることと、未知のコードはコードを添えて出すことも固定） |
| 41 | 「直近 50 件・3 年保持・資料名を残さない」を表の直後に告げる | 緑 |
| 42 | 記録が 0 件のときは空状態を出し、表の器を描かない | 緑 |
| 43 | 履歴を引けないときは空状態へ縮退せず、再試行で実際に引き直す | 緑（再試行が飾りでないことを呼び出し回数で固定） |
| 44 | 実ブラウザで失敗した同期が 1 行出て、次の行動の文言が読め、タイトル・パスの列が無い | 緑（ブラウザ E2E） |

## ブラウザ E2E で見ていること・見ていないこと

- **見ている**: ビルド成果物の上でルート単位の遅延チャンクが実際に読み込まれ、画面が描かれること。
  経路の取り違え（パスの改名）は**ここでだけ落ちる** —— 未認証の往復では見分けられない（受け皿が
  認証ガードの配下に居るため）。変異試験で 2 件が落ちることを実測した。
- 🔴 **見ていない**: 後段の実応答との一致。セッションと BFF 応答はネットワーク層のスタブであり、
  **契約の写しであって後段ではない。** 実基盤を伴う実行は別 issue（#466）が持つ。

## 実行

```bash
cd src && pnpm run test
cd src/platform/frontend && pnpm exec playwright test e2e/sc20-obsidian-settings.smoke.spec.ts
dotnet test src/platform/backend/Bff/Platform.Bff.Tests
```

## 関連仕様

- 画面仕様書: [Obsidian 連携設定](../screens/SC-20_obsidian-settings.md)
- テスト仕様書: [個人資料管理画面](SC-19_private-notes-screen.md)
