---
title: UC-10 関係を辿って根拠に到達する テスト仕様書
type: test-spec
status: draft
created: 2026-08-22
updated: 2026-08-22
author: claude
---
<!-- trace:
ids: [FR-05, FR-17, UC-10]
adrs: [ADR-0004, ADR-0033, ADR-0034]
iadrs: [IADR-0242]
specs: [20260822_issue-908_graphservice-foundation]
issues: [#450, #908, #909, #916, #962]
-->

# テスト仕様書: 関係を辿って根拠に到達する

## 起点となる計画書（トレーサビリティ）

- ユースケース: 起点となる文書から関係を辿り、権限内の文書だけで構成された近傍を得る。深さは既定 2・上限 3
- 事前条件: 認証済みであること。利用者属性が解決できること。起点文書の閲覧権限があること
- 例外フロー: 権限のないノードへの辺は**応答のどの形でも返さない**（件数・匿名ノードを含む）。利用者は「リンク切れ」と「権限不足」を区別できない（受け入れ済みの副作用）

## テスト対象・範囲

- 対象: 起点ノードの読み取り経路における**存在秘匿**と、認証・認可の縮退。
- 対象: 起点ノードの読み取り経路における**存在秘匿**、認証・認可の縮退、**多ホップ展開・表示上限・深さ上限の拒否**。
- 対象外（別issueで追加する）: 根拠検索への統合（BFF 公開を含む）。

> **［2026-08-22 追記］多ホップ探索が入り、T-08〜T-10 を実装した。** 深さ 0 までだった段階から、
> 展開・表示上限・深さ上限の否定形と**その陽性対照**まで覆う範囲へ広がった。

## テスト観点

- **存在秘匿**: 権限なし／複製が未到達／文書が存在しない、の 3 経路が**応答として区別できない**こと。状態コードだけでなく本文とヘッダまで一致すること。
- **拒否の選択**: 権限がないときに 403 を返さないこと（403 は「存在はする」ことを漏らす）。
- **deny-by-default**: 許可ポリシーが無ければ起点自体も見せないこと。
- **認証**: 未認証は 401（存在秘匿の前段）。
- **対照群**: 見えるものは見えること（全経路が 404 になって検査が空振りしていないことの担保）。

## テストケース一覧

| ID | 前提条件 | 手順 | 期待結果 | 対応受け入れ基準 | 区分 |
| --- | --- | --- | --- | --- | --- |
| T-01 | 閲覧可の起点 | 起点の取得 | 200（対照群） | 関係の提示 | 自動 |
| T-02 | 権限のない起点 | 同上 | 404 | 権限外を出さない | 自動 |
| T-03 | 属性の複製が未到達の起点 | 同上 | 404 | 権限外を出さない | 自動 |
| T-04 | 存在しない起点 | 同上 | 404 | 権限外を出さない | 自動 |
| T-05 | T-02〜T-04 の 3 応答 | 本文と Content-Type の比較 | **1 バイトも違わない** | 区別させない | 自動 |
| T-06 | 許可ポリシー無し | 起点の取得 | 404（403 でない） | 権限外を出さない | 自動 |
| T-07 | 未認証 | 同上 | 401 | 認証必須 | 自動 |
| T-08 | 権限のない中継ノードを挟む経路（直辺なし） | 近傍探索 深さ 2 | 到達先が現れない（橋が成立しない） | ホップごと判定 | 自動 |
| T-08P | 同一トポロジで中継が許可 | 同上 | 到達先が現れる（**陽性対照**） | 同上 | 自動 |
| T-09 | 許可 150 件＋権限外 100 件 | 近傍探索 深さ 1 | ノード 151・打ち切りが立たない | 表示上限 | 自動 |
| T-09P | 許可 250 件 | 同上 | 打ち切りが立つ（**陽性対照**） | 同上 | 自動 |
| T-10 | 深さ 4 / 深さ 0 の指定 | 近傍探索 | 400（黙って丸めない） | 深さ上限 | 自動 |
| T-10P | 深さ 3 / 深さ省略 | 同上 | 200・既定は深さ 2（**陽性対照**） | 同上 | 自動 |
| T-11 | 被参照のみの辺 | 近傍探索 | 逆引きで返る（行を増やさない） | 関係の提示 | 自動 |
| T-12 | 上限を超える許可ノード | 2 回実行 | 応答が同一（打ち切りが決定的） | 表示上限 | 自動 |
| T-13 | 挿入順とソート順が逆の接続辺 | ストアの読み出し | 昇順で返る（**順序契約を直接突く**） | 表示上限 | 自動 |

### BFF 経由（#916a）

🔴 **「GraphService 側で効いているから BFF 経由でも効く」は測った証拠にならない。** 認可の境界を跨ぐので BFF 層で改めて測る。

| ID | 前提条件 | 手順 | 期待結果 | 対応受け入れ基準 | 区分 |
| --- | --- | --- | --- | --- | --- |
| T-20 | 権限のある利用者 | `/bff/graph/{id}` | 200・**資格情報が後段へ届く**（陽性対照） | 関係の提示 | 自動 |
| T-21 | 後段が 404 | 同上 | 404（403 へ変換しない） | 権限外を出さない | 自動 |
| T-22 | 権限外 と 不存在 | 同上 | **本文まで一致** | 区別させない | 自動 |
| T-23 | `hops` 指定 | `/bff/graph/{id}/neighbors` | 資格情報と `hops` が後段へ透過 | 深さ上限 | 自動 |
| T-24 | 後段が 400 | 同上 | 400 のまま透過（握り潰さない） | 深さ上限 | 自動 |
| T-25 | 後段が 5xx | 同上 | 空グラフへ縮退**しない** | 関係の提示 | 自動 |
| T-26 | 未認証 | 同上 | 401 | 認証必須 | 自動 |

## 辺の型カタログ（BFF 公開）

グラフビュー画面は辺の**描き分け**と**型フィルタ**を持つが、グラフ読み取りの応答が返すのは `edgeTypeId` だけである。表示名は辞書側で解決する（改名に追随させるため）。

🔴 **管理者向けの一覧（使用件数つき）を一般利用者へ開けてはならない。** 件数は全辺を数えており ABAC で絞られていないため、ホップごとの判定が個々のノード・辺を隠していても**集計値が総量を漏らす**。

| ID | 前提条件 | 手順 | 期待結果 | 対応受け入れ基準 | 区分 |
| --- | --- | --- | --- | --- | --- |
| T-27 | ロールを持たない一般利用者 | `/graph/edge-types/catalog` | 200・型が引ける（**陽性対照**） | 描き分け・型フィルタ | 自動 |
| T-28 | 辺が 3 本ある型 | 同上 | 応答本文に使用件数が**現れない** | 権限外を出さない | 自動 |
| T-29 | 同上 | 管理者向け一覧 | 使用件数が**現れる**（装置の検出力の確認） | — | 自動 |
| T-30 | 未認証 | 同上 | 401 | 認証必須 | 自動 |
| T-31 | 権限のある利用者 | `/bff/graph/edge-types` | 200・**資格情報が後段へ届く**（陽性対照） | 描き分け・型フィルタ | 自動 |
| T-32 | 同上 | 同上 | 後段の経路が**カタログ**であること（管理者向け一覧でない） | 権限外を出さない | 自動 |
| T-33 | 後段が 403 | 同上 | 403 のまま透過（200 + 空配列へ潰さない） | — | 自動 |
| T-34 | 後段が 5xx | 同上 | 空配列へ縮退**しない** | — | 自動 |
| T-35 | 未認証 | 同上 | 401 | 認証必須 | 自動 |

## 実装マッピング

- `GraphEndpointsSecrecyTests`（T-01〜T-07）
- `EdgeTypeCatalogTests`（T-27〜T-30。**GraphService 層**。T-29 は否定形 T-28 の検出力を担保する
  —— 管理者向け一覧が件数を返していないなら「件数が現れない」は自明に成り立ってしまう）
- `BffGraphEdgeTypeTests`（T-31〜T-35。**BFF 層**。T-31 の陽性対照が無いと、Authorization の
  伝播を外す変異で**全部 401 のまま緑**になる —— 変異試験で実測（5 件失敗・否定形は緑）)
- `BffGraphEndpointTests`（T-20〜T-26。**BFF 層**。T-20 の陽性対照が無いと、
  Authorization の伝播を外す変異で**全部 404 のまま緑**になり検出できない —— 変異試験で実測）
- `GraphTraversalTests`（T-08〜T-12）
- `AuthorizedGraphViewTests`（T-13。**T-12 だけでは順序保証を測れない** —— 変異試験で
  `OrderBy` を外しても T-12 が緑のままだった〔等価変異〕ため、挿入順とソート順を逆にした
  フィクスチャで順序契約を直接突くテストを別途置いた）
