---
title: SC-21 AI 提案一覧 テスト仕様書
type: test-spec
status: completed
created: 2026-08-23
updated: 2026-08-29
author: claude
---
<!-- trace:
ids: [FR-05, FR-17, FR-18, SC-03, SC-09, SC-21, UC-10]
adrs: [ADR-0033, ADR-0034, ADR-0050]
iadrs: [IADR-0122, IADR-0124, IADR-0135, IADR-0271, IADR-0272, IADR-0276]
specs: [20260823_issue-918_sc21-ai-suggestions]
issues: [#450, #452, #911, #914, #915, #918]
-->

# テスト仕様書: AI 提案一覧

## テスト対象・範囲

AI 提案の**一覧と導線**（棚卸し用の画面と、それを支える公開 API の読み取り口）。

**対象外**: 承認・却下の状態遷移（別のテスト仕様書が覆う）／提案の生成／却下解除の発火／
文書詳細画面の承認欄（**［2026-08-29］実装済み。テストはその画面のテスト仕様書が持つ**）。

## テスト観点

1. **一覧が計画どおりの形をしていること**（4 列・同一の表・両端の文書名・根拠）。
2. **絞り込みが URL を単一情報源として機能すること**（既定・4 値・種類・未知の値の縮退）。
3. 🔴 **描いてはいけないものが無いこと**（一括承認・承認/却下の操作・種類ごとの別画面・
   導線を持たない行）。**否定形は必ず陽性対照と対で置く。**
4. **秘匿と縮退**（権限外は件数にも出ない／取得失敗を空の一覧へ潰さない）。

## テストケース一覧

| ID | 前提条件 | 手順 | 期待結果 | 対応受け入れ基準 | 区分 |
| --- | --- | --- | --- | --- | --- |
| T-01 | リンク提案とタグ提案が 1 件ずつ | 一覧を開く | **表は 1 つ**で 4 列（種類 / 提案 / 状態 / 文書詳細）。両方の行が同じ表に並ぶ（陽性対照） | 一覧の列・同一の一覧 | 自動 |
| T-02 | — | URL に `state` を付けずに開く | **承認待ちで問い合わせる**。フィルタの表示も承認待ち | 既定は承認待ち | 自動 |
| T-03 | — | 状態フィルタの選択肢を数え、「すべて」「却下」を選ぶ | 選択肢は 4 値。選んだ値がそのまま問い合わせへ載る | 状態フィルタの値域 | 自動 |
| T-04 | — | 種類フィルタで「タグ」→「すべて」を選ぶ | 「タグ」では種類が問い合わせへ載り、**「すべて」では載らない**（陽性対照の対）。**表は 1 つのまま** | 種類フィルタ・画面を分けない | 自動 |
| T-05 | リンク提案とタグ提案 | 各行の導線を見る | **全行**が文書詳細（`/docs/<起点の文書ID>`）へのリンクを持つ | 全行に導線 | 自動 |
| T-06 | 行が描かれている | 承認・却下・一括選択の操作を探す | 🔴 **1 つも無い**（承認ボタン・却下ボタン・チェックボックス・一括操作）。**先に「行が在り操作可能な要素も在る」ことを確かめてから測る** | 一括承認を提供しない・本画面で実行しない | 自動 |
| T-07 | 再提示された提案 | 一覧を開く | **固定文言**が付く（「この提案は一度却下されましたが、文書が更新されたため再度提示しています。」） | 再提示の理由表示 | 自動 |
| T-08 | 初回の提案 | 一覧を開く | 再提示の文言が**出ない**（T-07 の対。常に出す実装を検出する） | 同上 | 自動 |
| T-09 | 提案 2 件 | 一覧を開く | 各行に**根拠**が出る | 提案の根拠 | 自動 |
| T-10 | 型辞書で辺の型が改名されている | 一覧を開く | 「提案」列の型名が**改名後**になる（応答へ焼き込んでいない） | 表示名は辞書で解決する | 自動 |
| T-11 | 3 状態の提案 | 一覧を開く | 状態が**言葉**で出る（色だけに頼らない）。却下は「却下（再提示を抑止中）」 | 色だけで意味を持たせない | 自動 |
| T-12 | 後段が失敗する | 一覧を開く | **エラーを出し、空一覧・表を出さない** | 縮退させない | 自動 |
| T-13 | 提案が 0 件 | 一覧を開く | 空状態を出す（エラーではない） | 空と失敗の描き分け | 自動 |
| T-14 | — | 説明文を読む | 「承認・却下はこの画面では行わない」「まとめて承認する操作は無い」「権限外は件数を含め出ない」が常に出る | 位置づけの明示 | 自動 |
| T-15 | — | ルート表を走査 | 🔴 **登録されたルートは `/ai-suggestions` **ただ 1 本**（種類ごとの別画面が無い）。**先にルートが取れることを確かめる** | 画面を分けない | 自動 |
| T-16 | — | 未知の `state` / `kind` を URL へ手打ちする | 既定へ倒れる（画面が壊れない） | URL の正規化 | 自動 |
| T-17 | 権限内・権限外の提案が混在 | 一覧を引く | 権限内は返り（陽性対照）、**権限外は件数にも現れない** | 権限外を出さない | 自動 |
| T-18 | リンク提案 | 一覧を引く | **両端の文書名**が応答に載る | 一覧の「提案の内容」列 | 自動 |
| T-19 | タグ提案 | 一覧を引く | 終点の文書名は **null**（空文字ではない。「名前の無い文書」と読ませない） | 同上 | 自動 |
| T-20 | 承認待ちと承認済みが 1 件ずつ | 既定と `すべて` の両方で引く | 既定は承認待ちだけ（陽性対照）、`すべて` は両方返る | 状態フィルタの 4 値目 | 自動 |
| T-21 | — | 状態の値集合を見る | 🔴 **`すべて` は永続化できる状態ではない**（フィルタの解除であり、行の状態列へ書けない） | 同上 | 自動 |
| T-22 | — | 未知の状態で引く | **400 のまま**（`すべて` を通したことで値域の穴が開いていない。T-20 の対） | 同上 | 自動 |
| T-23 | 権限のある利用者 | 公開 API の一覧を引く | 200 が返り、**資格情報が後段へ届いている**（陽性対照。伝播を外すと全部 401 になり否定形は緑のまま） | 権限伝播 | 自動 |
| T-24 | — | 後段へのパスを見る | **末尾スラッシュつき**（落とすと 404 になり「提案が無い」ではなくエラーになる） | 経路の固定 | 自動 |
| T-25 | — | 絞りを付けて引く | 状態・種類が**そのまま**後段へ渡る。**未指定なら既定を注入しない**（既定の情報源を 2 つにしない） | 透過 | 自動 |
| T-26 | 未認証 | 公開 API の一覧を引く | 入口で 401（後段へ行かない） | 認証必須 | 自動 |
| T-27 | 後段が 400 / 500 | 同上 | 400 は透過。500 は **200 + 空配列へ潰さない** | 縮退させない | 自動 |
| T-28 | — | 公開 API のルート表を走査 | 🔴 **一括承認のパターン（承認・却下に当たり、かつ 1 件の識別子をパスに取らない口）に一致するルートが 1 本も無い。** 陽性対照を 2 段で置く——提案の口が取れること／**単票の承認・却下が在ること**。生成の口が無いことも同時に見る | 一括承認を提供しない・承認は文書詳細経由 | 自動 |

> **［2026-08-29］T-28 の主張を組み替えた。** 従前は「提案の口は読み取り 1 本だけで、メソッドは
> GET のみ」を測っていた。**文書詳細の承認欄が着地して承認・却下の口が開いたため、その主張は
> 事実に反する。** 一方で**一括承認をどの層にも作らないという禁止は残る**ので、
> 「GET だけ」ではなく**一括のパターン**で固定する形へ変えた
> （「書き込みが 1 本も無い」を測っていると、一括承認を単票の隣へ足しても落ちない）。
| T-29 | 未認証 | 画面の URL を開く | ログイン画面へ誘導される（**認証ガードが先に効く**。ルートの実在は固定しない —— 下記） | 認証必須 | 自動 |
| T-30 | — | ルート木を組み立てる | 計画のルート表の `/ai-suggestions` が木に載り、左ナビ項目の遷移先が解決する（**ルートの実在はここで固定する**） | ルートパス | 自動 |

## 🔴 満たしていない受け入れ基準（意図的に「満たした」と書かない）

**「タグ提案がタグ辞書の値域に収まる」ことを検証するテストは無い。実装も無い。**

- 提案の生成は、言語モデルが返したタグ値を trim・重複除去するだけで、辞書と突き合わせていない。
- 辞書は文書サービス側にあり**管理者・運用者限定**である。一般利用者の画面からは引けないため
  （権限不足で拒否される）、**画面側での検証は原理的に不可能**である。
- サーバ間で引くには新しいサービス間依存と認可主体の決め方が要り、生成側の変更単位と
  新しい決定記録（あるいは計画側の裁定）が要る。**本作業では塞がない。**
- 🔴 **テストを置いて「緑だから満たしている」と読ませることのほうが危険**なので、
  ここに未充足として記録する。**繰り延べであって放棄ではない。**

## E2E で固定した（［2026-09-02 改訂 / #1139］満たした）

起票時の要求は「一括承認が存在しないことを E2E で固定する」であった。**満たした。**

> **［2026-09-02 追記 / #1139］従前ここには「本リポジトリでは満たせない」「認証済みの画面を
> E2E で実走できない」と書いていた。後者は誤りであり、前者はその誤りから導かれていた。**
> 認証状態の一次情報は身元を返す 1 つの応答だけであり、それをネットワーク層で差し替えれば、
> 実ブラウザ・実ビルド成果物の上で認証済みの画面が描かれる（認証基盤は 1 つも起動しない）。
> 土台は `e2e/support/bffSession.ts` にある。

- T-31（新）: 身元を与えて開くと、リンク提案とタグ提案が**同じ一覧**に並び、
  **全行が文書詳細への導線を持つ**（行数と導線の数の一致まで見る）＝陽性対照。
- T-32（新）: 同じ画面に**一括選択のチェックボックスも一括承認ボタンも行内の承認・却下も無く**、
  承認の口を**呼びにも行かない**＝陰性対照。上の陽性対照が行の存在を示しているため、
  「何も描けていないから 0 件」ではないと言い切れる。
- 🔴 **ただし「ルートが実在すること」を未認証の 1 本（T-29）だけでは固定できない**（#918 で実測）。
  未知のパスの受け皿は認証ガード配下に居るため、**ルートを消しても未認証なら同じくログインへ誘導される。**
  T-29 が固定するのは「認証ガードが先に効くこと」だけであり、**ルートの実在は T-31 / T-32 と
  T-30（ルート木の組み立て）が固定する。**
- T-06 / T-15 / T-28 の単体テスト（陽性対照つき）と公開 API のルート表走査は**そのまま残す**
  （層が違えば壊れ方も違う）。

**限界**: この層のスタブは**契約の写しであって後段ではない**。実応答との一致は固定しない
（実基盤を起こす検証は別 issue の射程である）。

## 実装マッピング

- `AiSuggestionListPage.test.tsx`（T-01〜T-16。画面。生成クライアントの要求関数をモックする）
- `AiSuggestionEndpointsTests`（T-17〜T-22。サービス層の一覧）
- `BffGraphSuggestionTests`（T-23〜T-28。公開 API 層。**認可の境界を跨ぐので改めて測る**）
- `sc21-ai-suggestions.smoke.spec.ts`（T-29・T-31・T-32。ブラウザ E2E）
- `router.test.ts`（T-30。ルート木の組み立て。計画のルート表とナビ項目の解決）

### 変異試験で確かめたこと（検出力の実測）

| 変異 | 落ちたテスト |
| --- | --- |
| 行に一括選択のチェックボックスと一括承認ボタンを足す | 2 件（T-01 の列数・T-06） |
| 行内に承認ボタンを足す | 1 件（T-06） |
| 種類ごとに別ルートを割る | 1 件（T-15） |
| タグ提案の行だけ導線を落とす | 2 件（T-05・T-06） |
| 状態の既定を承認済みにする | 2 件（T-02・T-16） |
| 取得失敗を空の一覧へ潰す | 1 件（T-12） |
| `すべて` を無視して常に 1 状態で絞る | 1 件（T-20） |
| 不可視の端点でも表示名を返す | 1 件（T-19） |
| 後段パスの末尾スラッシュを落とす | 2 件（T-24・ルート衝突） |
| 公開 API に承認の口を開ける | 1 件（T-28） |
| 画面のルート登録を外す | 2 件（T-30 のルート表・ナビ項目の解決） |
| 画面のルートのパスを改名する（E2E の検出力の確認・#918 当時） | 🔴 **0 件**（未認証だけの spec は落ちない。この事実は今も変わらない） |

**変異はすべて戻し、残渣が 0 であることを走査で確認した**（作業仕様書に手順を残した）。

［2026-09-02 追記 / #1139］**パスの改名は型検査（`tsc -b`）が先に落とす**ことを実測した ——
ルートのパスは型付きルータの union に入るため、改名するとビルドが通らない。
したがって「改名の変異」でブラウザ E2E の検出力は測れない。**代わりに測ったのは
ロール条件の緩め**（管理者限定の画面へ運用者を通す 1 語の変更。型検査は通る）であり、
**新しい陰性対照が落ち、未認証の 1 本は落ちなかった** —— 未認証だけでは見えない面である。

## 未決事項

1. **一覧の既定の並び順**が計画側で未確定である。現状はサーバ側が作成日時の昇順で返し、
   画面は列見出しから並べ替えられる。確定したらテストを足す。
2. **再提示の固定文言が実運用で出る経路が無い**（発火が未配線）。T-07 は「表示側の契約」を
   固定しているにすぎない。配線されたときに経路の受け入れ基準を別途起こすこと。
