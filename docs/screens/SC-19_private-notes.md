---
title: 個人資料管理 画面仕様書
type: screen-spec
status: completed
created: 2026-08-28
updated: 2026-09-12
author: claude
---
<!-- trace:
ids: [FR-19, FR-21, FR-22, SC-19, SC-20, UC-11]
adrs: [ADR-0031, ADR-0034, ADR-0036, ADR-0037, ADR-0046, ADR-0054, ADR-0057]
iadrs: [IADR-0121, IADR-0124, IADR-0125, IADR-0127, IADR-0131, IADR-0134, IADR-0135, IADR-0262, IADR-0270, IADR-0296, IADR-0437, IADR-0444]
specs: [20260828_issue-451c_sc19-sc20-screens, 20260828_issue-451a_private-notes-bff, 20260828_issue-451_deletion-propagation-to-object-storage, 20260912_1441-1442_private-note-contract-gaps]
issues: [#451, #1441, #1442, planning#614, planning#618]
-->

# 画面仕様書: 個人資料管理

> **［実装状態］`status: completed` は「本仕様書が記述する範囲の実装とテストが揃った」ことを表す**
> （`docs/README.md` 運用ルール 6）。**本書の範囲は、公開 API に口がある機能に限る。**
>
> ［2026-09-12］**公開範囲（3 状態と共有件数）・同期状態・タグの 3 列と、その 3 軸の絞り込みを実装した。**
> 契約がこの 5 項目を運ぶようになったためである。
> 🔴 **指定先（誰と共有しているか）の表示と変更ダイアログ・緊急アクセスは依然として実装していない。**
> 前者は**共有の単位そのものが未確定**（計画側の裁定待ち）、後者は**発動を記録する口が無い**。
> **「誰と共有しているかが見える」「共有先を変更できる」とは読まないこと。**
> 見送りの一覧と理由は §計画（画面設計）との対応 と §未決事項 にある。

## 画面概要・目的

利用者本人が、自分の個人資料を一覧し、露出（横断検索・ナレッジグラフ・AI 入力）を設定し、
削除済みの資料を復元・完全削除するための画面。**保存容量を自力で管理できることが眼目**である。

- ルート: `/my/notes`（削除済みは `/my/notes?tab=trash`。タイトルの絞り込みは `?q=`）
- アクセス: **認証済みの全利用者**（ロール限定なし）。**表示範囲は本人が所有する資料のみ**であり、
  他の利用者の資料は件数を含め一切現れない。**管理者が他人の資料を見る導線は置かない。**
- 画面ラベルは「個人資料」で統一する。「マイスペース」「個人メモ」は使わない。

## レイアウト / 主要素

| # | 要素 | 実装 |
| --- | --- | --- |
| 1 | 資料の一覧（種別 / タイトル / 公開範囲 / 同期状態 / タグ / 更新日時 / 露出 / 操作） | 共有 UI の表構造。行のタイトル下に同期先のパスを添える。**同期状態の列は利用中タブだけ**（削除済みは契約上つねに対象外） |
| 2 | 個人資料であることの表示 | 全行に **👤 ＋「個人資料（自分のみ）」**（検索結果・グラフのノードと同じ記号） |
| 3 | 業務関連資料としての扱いの固定文言 | 一覧の上部に**常時**表示（折りたたまない） |
| 4 | 保存容量 | 使用量 / 上限 ＋ 内訳「うち削除済み」＋ 版履歴が非算入である旨 |
| 5 | 段階警告 | 80%（予告）/ 95%（警告色）/ 100%（固定文言 2 段落）。**同時に 2 段は出さない** |
| 6 | 新規作成 | タイトル（必須）＋ 同期先のパス（任意）。**本文の入力欄を置かない** |
| 7 | 本文編集の代替導線 | 「本文はこの画面では編集できません」＋ 連携設定画面へのリンク |
| 8 | タブ | 利用中（件数）/ 削除済み（件数バッジ）。**URL が単一情報源** |
| 9 | 絞り込み | 4 軸（タイトルの部分一致 `q` / 公開範囲 `visibility` / 同期状態 `sync` / タグ `tag`）。**いずれも URL が単一情報源**。同期状態の絞りは利用中タブだけ |
| 10 | 削除済みの行 | 削除日時 ＋ **完全削除までの残り日数**（残り 7 日以内は警告色＋文言） |
| 11 | 一括選択と一括操作 | 復元・完全削除の**両方**。選択が 0 件のときは実行できない |
| 12 | 確認ダイアログ | 論理削除用と完全削除用の 2 種（下記） |
| 13 | 露出 3 トグル | 横断検索 / ナレッジグラフ / AI 入力。**独立**・既定はいずれもオフ |
| 14 | 空状態 | 「個人資料がまだありません」＋ 作成導線。削除済みタブでは別の文言 |
| 15 | 取得失敗 | 空状態へ縮退させず、取得できなかったことを示す |
| 16 | ヘルプ文言 | 「リンク先が表示されない理由は区別できない」旨（存在秘匿の帰結） |
| 17 | 公開範囲の表示 | 状態バッジ 3 値 ＋ 共有件数。🔴 **指定先は出さない**（下表と §未決事項 1） |
| 18 | 同期状態の表示 | 状態バッジ 3 値（競合あり / 同期対象 / 対象外） |
| 19 | タグの表示 | 分類チップを並べる。タグが無い行は「—」 |

### 状態の示し方（色だけで意味を持たせない）

削除済みの残り日数は共有 UI の状態バッジで示す。**色 ＋ アイコン ＋ テキスト**が型で強制される。

| 条件 | 表示 | 意味 |
| --- | --- | --- |
| 残り 8 日以上 | 中立（情報アイコン）「残り N 日」 | まだ猶予がある |
| 残り 7 日以内 | 警告（三角）「まもなく完全削除（残り N 日）」 | 通知が出る状態と一致させる |

**残り日数は切り上げる。**「あと 0.4 日」を 0 日と出すと、まだ間に合う資料が手遅れに見える。

公開範囲と同期状態も同じ状態バッジで示す。**色が担うのは「他人から見えるか」「同期が滞っていないか」だけ**で、
個人指定とグループ指定の区別は**文言と件数**が担う（色を落としても読める）。

| 値 | 表示 | 色 |
| --- | --- | --- |
| 非公開（共有 0 件） | 「非公開」 | 中立 |
| 個人指定 | 「個人指定（N 人）」 | 警告 |
| グループ指定 | 「グループ指定（N 件）」 | 警告 |
| 競合あり | 「競合あり」 | 警告 |
| 同期対象 | 「同期対象」 | 成功 |
| 対象外 | 「対象外」 | 中立 |

🔴 **契約は公開範囲・同期状態を文字列で運ぶ（値集合を列挙型にしない）ため、未知の値が届きうる。**
**未知を既知の値へ丸めない** —— 「判定できません」という別の表示にする。とりわけ公開範囲を
「非公開」へ丸めると、**実際は共有されている資料が非公開に見える**（この向きの誤りが最も高くつく）。

### 確認ダイアログ

| 操作 | 出す内容 |
| --- | --- |
| 削除（論理削除） | 「削除しても容量は空きません（90 日間保管されます）」を**事前に**示す。容量を空ける手段（削除済みからの完全削除）も添える |
| 完全に削除（即時） | ①復元できない旨 ②90 日待てば自動的に完全削除される旨 ③解放される容量（選択の合計） |

🔴 **①には但し書きを添えている。文言は据え置き、根拠だけが変わった。**

計画は「文言を出さない」か「但し書きを添える」の 2 択を実装側へ委ねており、**但し書きを選んだ**
—— 文言を落とすと、取り返しのつかない操作の手前で最も重要な警告が消えるためである。

**当初の根拠は解消した。** 完全削除は**オブジェクトストレージ上の本文実体を先に消し**、
消せなければ台帳の行を残して失敗を返すようになった（順序が逆だと、実体を指す値が失われて
不可視のまま残留する）。**個人資料は変換経路を通らないため図表資産を持たない**ので、
「取り込み済み文書の資産は台帳から辿れない」という一般の限界も本画面には掛からない。

**残っている根拠は別のものである。** 横断検索の索引とナレッジグラフの掃除は削除イベントを
介した**非同期**であり、応答を返した時点では検索・グラフ側に短時間残り得る。
「削除の反映には時間がかかる場合があります」は**この事実の記述としてなお真**である。
よって**利用者に見える文言は変えない**（変えると固定文言と翻訳カタログが動く）。

⚠️ **「実装したから外す」と機械的に処理しないこと。** 外す条件は消えたが、置いておく
別の条件が立っている —— 但し書きを消すには、この非同期性のほうを先に解消する必要がある。

## 表示・入力項目

| 項目 | 種別 | 必須 | 初期値 | 形式・制約 | 説明 |
| --- | --- | --- | --- | --- | --- |
| タイトル | テキスト | 必須 | 空 | 空白のみは不可 | 空のままでは作成ボタンを押せない |
| 同期先のパス | テキスト | 任意 | 空 | 省略時はサーバがタイトルから決める | 省略時は要求に `null` を送る |
| タブ | 選択 | 任意 | 利用中 | 利用中 / 削除済み | URL の `tab`。未知の値は既定へ倒す |
| タイトルの絞り込み | テキスト | 任意 | 空 | 部分一致（大小文字を無視） | URL の `q` |
| 公開範囲の絞り込み | 選択 | 任意 | 絞らない | 非公開 / 個人指定 / グループ指定 | URL の `visibility`。未知の値は**絞らない**へ倒す |
| 同期状態の絞り込み | 選択 | 任意 | 絞らない | 競合あり / 同期対象 / 対象外 | URL の `sync`。利用中タブのみ |
| タグの絞り込み | 選択 | 任意 | 絞らない | 一覧に現れるタグ名（完全一致） | URL の `tag`。選択肢は取得済みの一覧から作る |
| 露出 3 トグル | チェック ×3 | 任意 | すべてオフ | 独立 | 変えていない 2 つは現在値のまま送る |
| 削除済みの選択 | チェック | 任意 | 未選択 | 複数可 | タブを切り替えると選択は捨てる |
| 本文 | — | — | — | — | 🔴 **入力欄を置かない。** 本文は連携設定の経路でのみ書ける |

## バリデーション

| 項目 | 条件 | エラーメッセージ |
| --- | --- | --- |
| タイトル | 空白のみは作成できない | （出さない。ボタンを無効化する） |
| 新規作成 | **使用量が上限の 100% では実行できない** | 「保存容量の上限に達しました。…」（固定文言 2 段落） |
| タブ | 未知の値は既定へ倒す | （出さない。選択肢しか無い UI にエラー状態を持ち込まない） |
| 一覧の取得 | 失敗したら**空の一覧へ縮退しない** | 「個人資料の一覧を取得できませんでした。時間をおいて再度お試しください。」 |
| 書き込みの失敗 | 直近の操作の結果だけを出す | サーバの詳細（あれば）／無ければ「操作に失敗しました。…」 |

値域の防壁はサーバ側にある。画面は丸めるだけである。

## アクション・イベント

| 操作 | 挙動 | 遷移先 |
| --- | --- | --- |
| 作成する | タイトル（＋パス）だけを送る。成功で入力を空にする | 同じ画面 |
| 露出トグル | 3 値を揃えて送る（変えた 1 つだけが変わる） | 同じ画面 |
| 削除する | 確認ダイアログ → 論理削除 | 同じ画面 |
| 復元する | 単票・一括のいずれも即時に実行する（破壊的でないため確認を挟まない） | 同じ画面 |
| 完全に削除する | 確認ダイアログ → 単票／一括を**同じ口**へ送る | 同じ画面 |
| タブ・絞り込みの変更 | URL を書き換える | 同じ画面 |
| 連携設定へ | 本文を書く経路の案内から遷移する | 連携設定画面 |

**置かない操作**: 本文の編集・**公開範囲の変更（指定先の追加／削除を含む）**・管理者による一括閲覧／一括変更・
上限の引き上げ。不在は単体テストが陽性対照つきで固定する（公開範囲のバッジ自体が導線になっていないことも見る）。

## 画面遷移

```mermaid
flowchart LR
  SC19[個人資料管理] -- 本文を書く経路の案内 --> SC20[Obsidian 連携設定]
  SC20 -- 削除済み資料の確認・復元 --> SC19
```

## 権限・表示条件

- ロール限定なし（「個人」グループの画面）。左ナビに「個人資料」として出す。
- **表示範囲は主体（セッション）で決まる。** 画面は所有者を指定する口を持たず、
  要求にも所有者を運ぶ項目を載せない。他人の資料は件数にも現れない。

## 計画（画面設計）との対応

| 計画側の要素 | 実装 | 満たしていない条件 / 理由 |
| --- | --- | --- |
| 一覧（タイトル / 更新日 / 公開範囲 / 同期状態 / タグ） | する | 5 項目とも**同じ一覧の応答**から描く（問い合わせを増やさない）。同期状態の列は利用中タブのみ |
| 公開範囲の 3 状態の表示 | する | 状態バッジ 3 値 ＋ 共有件数。3 状態が色ではなく文言で区別されることを単体テストが固定 |
| 公開範囲の変更ダイアログ（指定先の一覧・追加・削除） | **しない** | 🔴 **共有の単位が未確定**であり、契約も指定先を運ばない（件数だけを載せる）。単位が決まる前に画面へ相手の一覧を置くと、決まった単位と食い違う語彙が利用者の記憶と翻訳カタログへ先に焼き付く（§未決事項 1） |
| 新規作成の導線（リッチエディタを描かない・本文編集の導線も持たない） | する | 入力はタイトルとパスだけ。本文の欄も編集導線も無いことを単体テストが固定 |
| 同期状態の表示（同期対象 / 対象外 / 競合あり） | する | 状態バッジ 3 値。**削除済みは契約上つねに対象外**なので、削除済みタブには列も絞りも置かない。競合の一覧と解決は連携設定画面が持つ |
| 検索・絞り込み（タイトル / タグ / 公開範囲 / 同期状態） | する | 4 軸とも URL が単一情報源。未知の値は「絞らない」へ倒す |
| 空状態 | する | 利用中・削除済みで文言を分ける |
| 緊急アクセス（Break-glass）の導線 | **しない** | 🔴 **発動を記録する口が公開 API に無い。** 理由入力・監査ログの明示・15 分の自動失効はサーバ側の発動記録が前提であり、画面だけでは統制にならない |
| 「削除済み」タブ（削除日時・残り日数） | する | 同じ応答を絞って描く（問い合わせを分けない） |
| 復元 | する | 単票・一括の両方 |
| 完全に削除（即時）＋確認ダイアログ | する | ①②③ を出す。①には但し書き（前掲） |
| 一括選択と一括操作（復元・完全削除の両方） | する | 一括完全削除は**単票と同じ口**へ送る（要素数の差でしかない） |
| 完全削除が近い項目の強調（残り 7 日以内は警告色） | する | 色に加えて文言も変える（境界の両側を単体テストが固定） |
| 削除済みの件数バッジ | する | 一覧の件数と同じ数え方（同じ応答から数える） |
| 容量表示（使用量・上限・うち削除済み・80/95 の段階警告） | する | 内訳は削除済み行の合算（契約が「数え方を 2 つにしない」と定めている） |
| 100% で新規作成のみ拒否・固定文言 | する | 既存資料の操作は残ることも単体テストが固定 |
| 業務関連資料としての扱いの固定文言（隠さない） | する | 折りたたみの中に入れていないことを単体テストが固定 |
| ヘルプ文言（リンク先が見えない理由は区別できない） | する | 常時表示 |
| リンク作成 UI（補完候補の絞り込み・404 原則） | **しない** | 🔴 **リンクを作る口が公開 API に無い。** ヘルプ文言だけは先に置いてある |
| 他人の資料の件数・存在を示唆しない | する | 示唆表現の不在を陽性対照つきで固定 |
| 管理者による一括閲覧・一括変更を描かない | する | 同上 |
| 本画面内での本文編集を描かない | する | 同上 |

## 関連仕様

- 通信仕様書: [BFF 境界](../api/BFF_bff-surface.md)（個々の要求・応答は [`openapi.yaml`](../api/openapi.yaml) を正とする）
- テスト仕様書: [個人資料管理画面](../tests/SC-19_private-notes-screen.md) /
  [個人資料のライフサイクル](../tests/FR-19_private-notes-lifecycle.md)
- 画面仕様書: [Obsidian 連携設定](SC-20_obsidian-settings.md)（露出設定と削除の説明が対になる）

## 未決事項

1. 🔴 **公開範囲の「指定先」（誰と共有しているか）の表示・変更が無い**（計画の主要素 3）。**裁定待ち**である
   —— 共有の単位（認可サーバーのグループ／部門／プロジェクトのいずれか）が計画側で未確定であり、
   契約も指定先を運ばない。**3 状態と件数までは実装済み**であり、残るのは相手の一覧と変更ダイアログだけである。
   裁定が付いたら、契約に指定先を足したうえでこの節と対応表を直す。
2. ~~同期状態の列とタグ列が無い~~ **［2026-09-12 解消］** 契約が同期状態・タグを運ぶようになり、
   3 列とも実装した。**「判定できない値」の表示を新設した**ことを併せて記録する（前掲 §状態の示し方）。
3. 🔴 **緊急アクセス（Break-glass）の導線が無い。** 発動記録の口が無く、画面だけでは
   「15 分で自動失効し、有効性はサーバ側の発動記録で判定する」という統制を満たせない。
4. **完全削除の確認文言に但し書きを添えている**（前掲）。実体削除の配線が入ったら但し書きを外す。
5. 計画が未確定としている論点（文書管理画面との責務分界、AI 入力の資料単位設定の設置場所、
   「非公開」の表示語）はそのまま残っている。**資料単位の露出は本画面に置いた** ——
   利用者単位の既定を置く口が無く、資料単位の口だけが在るためである。
