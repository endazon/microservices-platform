---
title: 全 open issue の一括対応 — 波 1〜3 の引き継ぎ記録
type: spec
status: done
related_ids: [NFR, ADR-0005, ADR-0021, ADR-0032, ADR-0065, ADR-0066, ADR-0067, ADR-0068]
author: Claude (orchestrating session)
created: 2026-08-31
updated: 2026-09-03
---

# 引き継ぎ: 全 open issue の一括対応（2026-08-30 〜 08-31）

着手時 44 件の open issue に対し、**稼働環境が要るものから**着手した（利用者の指定）。
本書は**着地した PR・close した issue・分割起票・未達と理由**を残す。
進捗の数字は最も速く腐るので、**判断と実測の要点だけ**を書く。

## 着地した主なもの

| PR | 内容 | 実測で分かったこと |
| --- | --- | --- |
| #1072 / #1109 | Istio を opt-in で導入 → **エッジを Istio Ingress Gateway へ移し mTLS を STRICT** | 計画 ADR-0021 が「Traefik は無効化する」と明記していた。メッシュ外の Traefik が入口だと STRICT で 502 になる |
| #1086 / #1081 | apiserver の OIDC 検証を再導入・Headlamp の issuer を https エッジへ | **apiserver（Go）は `.localhost` を特別扱いしないが `curl`/musl はする** |
| #1113 | 決定的ローカル埋め込みで「検索が実際に効くこと」を統合スタックの門にする | 🔴 **Wolverine の発行ルートが本番コードに一度も無く、移行したイベントはどの配備でも 1 通も出ていなかった**（例外なし・info ログ 1 行・health は緑） |
| #1114 | BFF セッションの構成を配備へ落とす | **secret を入れただけでは通らない** —— エッジが TLS を終端するので転送ヘッダを解釈させないと `redirect_uri` が `http://` になる |
| #1105 | lingui の許可リストを撤去し検査範囲を構造的に閉じる | **i18n 済みなのに `en` ロケールで日本語が出ていた** 9 件を発見 |
| #1082 / #1087 / #1095 | 3 段スライス（knowledge 8 サービス）・フロント層分類・古い記述 27 件 | — |

## クロス監査（別文脈・証跡必須）の結論

`#1083` / `#1084` を ADR-0068 で測り直した。

- **#1083 は否定された基準（メンバー共有）を使ったが結論はおおむね一致**（40 中 37 ファイル）。ADR-0068 が着地形を「変更不要」と裁定済みなので是正しない。
- 🔴 **#1084 は実測が理由を否定した** —— `McpToolContracts.cs` の残置理由「操作をまたぐ」に対し、使っている操作は `Declare` の **1 つだけ**。
- 振る舞い不変とテスト件数は両 PR とも適合（ルート 43 件完全一致・認可/タグ行の md5 一致・skip 0）。

**監査自身も罠を踏んだ** —— 初回のルート順比較は `execSync`（cmd.exe 経由）が rev の `^` を食い「after 対 after」を比べていた。

## 🔴 この期間に踏んだ事故（同じ形を繰り返さないため）

### 1. 「無い」と結論する測定で、探す範囲が足りていなかった（6 回）

| 測ったもの | 誤った結論 | 実際 |
| --- | --- | --- |
| 稼働イメージの LLM 計器 | 「未実装」 | **存在しないファイル名**（`LlmGateway.Api.dll`）で測っていた。実体は `LlmGateway.dll` |
| .NET アセンブリ内の文字列 | 素の `grep` で 0 件 → 「無い」 | **メタデータは UTF-16**。`tr -d '\000'` が要る |
| Istio の sidecar 注入 | `spec.containers` を見て「0 件」 | **1.30 はネイティブサイドカー**で `initContainers` に入る |
| apiserver → Keycloak の到達性 | `curl` が通る → 「問題ない」 | **`curl` は `.localhost` を特別扱いするが Go はしない** |
| Keycloak の永続化 | 「PVC 化は実装されていない」 | **base だけを見て `deploy/local/infra-persistence/` の opt-in オーバーレイを探していなかった** |
| `KeywordSearchAsync` の壊れ方 | 「`catch (RpcException)` が常に成立」 | **コードのコメントを実測と取り違えた。** v1.18.1 では例外は出ず**部分文字列走査へ黙って落ちる**（版上げで挙動が変わっていた） |

**規範**: 「無い / 0 件 / 到達しない」と書く前に、**「有るはずのもの」で同じ測定をして非ゼロが出ることを示す**。
出なければ測り方が壊れている。**このリポジトリは fail-safe な既定＋opt-in オーバーレイを多用するので、base に無い＝実装が無い、ではない。**

### 2. 並行 PR は必ず同じ IADR 番号を採る

`check-adr-numbering.js` は**重複も欠番も許さない連番**を要求する。
**事前に番号を配る方式は失敗した** —— 4 本並行で 2 巡し、各エージェントに 3 回ずつ改番させた。マージ順は CI・衝突・レビューで動くので事前配布は必ず崩れる。

**採った方式**: 各エージェントには「番号は好きに採ってよい・衝突しても気にしなくてよい」と伝え、
**マージ直前にオーケストレータが `develop` の最大値 +1 へ改番して 1 本ずつ入れる。**
🔴 **一括 `sed` をしないこと** —— 同じ番号が「他の PR の正当な ADR」としても本文に現れる。
🔴 **`scripts.test.js` は最初の失敗で止まる。** ADR 採番で落ちると残りが緑か分からないので個別の検査器を直接叩く。

### 3. サブエージェントに worktree を与え忘れた

`isolation: worktree` を指定し忘れると、エージェントはオーケストレータの worktree で作業する。
実際に別作業の未コミット変更の上で `git checkout` / `git merge` を走らせかけた。**必ず指定する。**

### 4. 衝突解決で構文を壊した

`scripts/k8s-local-up.test.js` で、両側が**同じ位置に**検査ブロックを追記していた。
衝突ハンクを機械的に連結したところ**共通の末尾と各側の追加ブロックの境界を取り違え**、`SyntaxError` になった。
**両側の版から共通部分と各々の追加ブロックを取り直して組み直す**のが正しい。**解決後は必ず構文と件数を実測する。**

## 未達と理由

| 対象 | 理由 |
| --- | --- |
| `#380`（Opus 5 の実運用値） | **測定不能**。実 LLM トラフィックが 0（API キーが空）・collector が `exporters: [debug]` で全破棄・Prometheus が永続化されていない。**数字を作らず据え置いた** |
| `#336` の「Voyage AI ゼロ保持契約の実認定」 | 契約・法務の判断であり AI が代行できない |
| `Knowledge.IntegrationTests` の 41 件 | **Docker daemon が無い**（containerd / nerdctl）。**skip のまま緑になるので判定に使えない。** 判定は CI の `Integration` 実走で行う |
| `dotnet test src/platform/backend/backend.slnx` | 未展開の submodule `src/ai-stock-trading` を参照する `Platform.Bff` が解決できない。**プロジェクト個別で測る** |
| `#858` / `#827` / `#1015` | **すべて submodule 側の欠陥**で本リポジトリからは直せない |

## 次に着手する側への申し送り

- **`blocked` の 5 件（`#1014` / `#600` / `#516` / `#752` / `#754`）は全部裁定済みだった。** planning に open な `decision-needed` は無い。**問題は追随漏れ 1 種類である。**
- **`#1118`（全文検索が日本語でほぼ引けない）は計画側の裁定が要るかもしれない。** 形態素解析 / N-gram / 別エンジン / ベクトルへ寄せる、のどれも計画の前提に触れる。
- **`#1088`（稼働 dev クラスタが `PERSIST=1` で立っていない）と `#324` は同じ PR で解く必要がある。** 永続化した瞬間に `IGNORE_EXISTING` が効きはじめ、realm 変更が二度と反映されなくなる。

## ［2026-09-03 追記 / #1213］波 4（2026-09-02 09:00 〜 09-03 21:00）の引き継ぎ

### 着地したもの（develop `68b38ec6` まで、IADR は 0330〜0360 が欠番なし）

| 領域 | PR（issue） |
| --- | --- |
| 画面・E2E | #1142(#1099) #1155(#1139) #1149(#1131) #1164(#1123) #1207(#1199) |
| 文書・規約 | #1140(#1092) #1148(#1141) #1180(#1179) #1191（計画 ADR レンジ 0077）#1205(#1196) |
| 認証・秘匿 | #1147(#1102) #1152(#1115) #1162(#1144) #1169(#1143) #1160(#1127) |
| 検索・取り込み | #1157(#1118) #1209(#1192) #1210(#1193) #1175(#1120) #1211(#1194) |
| 可観測性 | #1165(#1110) #1161(#1103) #1189(#1186) #1208(#1197) #1206(#1204) |
| Obsidian | #1156(#1098 第 1 段) #1177(#1153 第 2 段) #1173(#1154) #1212(#1176) |
| 構造 | #1150(#1063) #1170(#1146) #1171(#1093) #1174(#1094) #1151(#1126) #1172(#600) |

計画リポジトリには ADR-0069〜0077 が追加され（planning#517〜532）、受け皿 issue #1192〜#1204 を起票した。環流は planning#514 / #515 / #516 / #518 / #524。

### 運用上の判断（次回も同じ形で）

- **索引 `.ai-context/adr/README.md` は直列の衝突点。** 1 本着地するたびに残り全 PR が DIRTY になるため、待ち行列を **merge train**（各 PR に develop ＋ 先行 PR ブランチを順に取り込む）にし、索引の衝突は「行の和集合を番号昇順」で機械解決した。**IADR 番号は着地時に develop 最大＋1 へ詰める**（事前割当は衝突する）。
- **改番の罠 3 つ**: ①件名スコープに IADR を書いた PR は改番で `commit-messages` が落ちる（#1166 → #1172 に作り直し）②コミット本文の他リポ参照は `planning#NNN` の短縮形でないと落ちる（#1188 → #1189）③索引行のタイトルセルは 200 字（#1155 / #1175）。派遣プロンプトに 3 つとも入れる。
- **Lingui の `.po` / `messages.ts` は並行 PR で衝突する。** `.po` は msgid で和集合（msgstr 非空優先）→ `lingui extract --clean && compile --strict` で再生成（`npm_config_manage_package_manager_versions=false`、AST submodule を展開したうえで `pnpm install --frozen-lockfile`）。
- **PR 本文の `Closes #NNN` をバッククォートで囲むと閉じない**（#1143 / #1144 / #1176 を手で閉じた）。
- **Keycloak pod で `kcadm.sh` を exec しない**（本体が OOMKilled）。管理操作は Admin REST API で。
- サブエージェントの**無音の死**が 7 本あった（出力 0 バイトのまま）。派遣後 30 分で worktree のコミットが無ければ死んだとみなし、別エージェントを起こす。

### 稼働 dev クラスタの乖離（配備後の再測は #1088 に一覧）

`conversion-service:pdf-1192` / `bff:issue1199` / graph・dashboard は #1186 のイメージ / `wiki-service:latest` は 6 週間前（#1126 以前）。realm は Postgres 側にあり無事。オーケストレータの `kubectl apply` / `set image` は自動モードの分類器に拒否される（サブエージェントは通る場合がある）。

### 利用者の手が要るもの

1. `.claude/settings.json` 21〜25 行の許可 5 件の撤去（#1167。AI は編集不可）
2. #1115 の門 2 枚の適用と logout 実測（#1168）
3. #600 の AC-14（BFF イメージ差し替え）

### 残る open issue（2026-09-03 21:00 時点 42 件）の分類

- **実装待ち（受け皿あり）**: #1135 / #1163 / #1185 / #1187+#1014 / #1195 / #1200（担当エージェントが無音で死んだ。未着手として起こし直す）、#1184 / #1198 / #1202 / #1203 / #1201 / #1145 / #1064 / #1213 / #1190 / #1182 / #1159
- **blocked**: #336 / #380 / #1111 / #752 / #754 / #457 / #458 / #1091（LLM 429 の軸）
- **トラッカー**: #454 と子 11 件（#442 は #1135 が着地すれば閉じられる）
- **AST 側**: #858 / #1015（AST 側に受け皿 issue が無い）
