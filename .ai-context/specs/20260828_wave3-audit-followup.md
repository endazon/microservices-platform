---
title: 波 3 フェーズ末クロス監査の完了記録と指摘の処置
type: spec
status: done
related_ids:
  - FR-17
  - FR-19
  - FR-20
  - FR-22
  - SC-19
  - SC-20
  - ADR-0037
author: claude
created: 2026-08-28
updated: 2026-08-28
related_adrs:
  - IADR-0141
  - IADR-0280
  - IADR-0282
  - IADR-0284
  - IADR-0285
---

# 波 3 フェーズ末クロス監査の完了記録と指摘の処置

## 実施

- 2026-08-28、**adr-guardian ＋ traceability-auditor** を同時に、書いたエージェントと別の
  フレッシュな文脈で実施した。母集合は **range `ce52912..65dd77c`**（71 コミット・288 ファイル /
  +15012 −637）。両監査とも shallow（true）を先に確認し、git log / blame を出典に使っていない。
- 判定: **重大違反 0**（両監査）。ADR 側は制約 23 項目すべて判定づけ、トレーサビリティ側は
  検査器 12 本全緑・参照切れ 0・実装漏れ 0・孤立実装 0 を全数走査で確認。両監査とも打ち切りを明言。

## 指摘と処置

| # | 指摘 | 処置 |
| --- | --- | --- |
| T-推奨1 | `20260828_issue-449_document-wiki-completion.md` が完了済みなのに `status: draft`（**同型 2 回目** — 波 2 の R1 と同じ） | **是正済み**（done へ）。同型 2 回目のため**仕様書 status の機械検査を波 4 の掃き寄せで新設する**（検査器追加の条件を満たした） |
| T-推奨2 / A-A | IADR-0280 の Superseded 追随漏れ（live 86 ファイル中 84 件に後継併記なし） | **即時是正 9 ファイル**: `docs/tests/FR-17`（frontmatter へ IADR-0282 併記）/ `check-unit-dependencies.js` 7 箇所（単一情報源宣言に Superseded＋経過措置の注記）/ `scripts.repo.test.js` / `ObsidianLinkParser.cs` / Shared.Kernel 4 ファイル（決定 6 存続の注記）。**残り（層プロジェクトの .csproj / .cs 約 75 件）は波 4.5 の一括移送で物理的に消滅するため除外**（IADR-0282 決定 4-3。走査 86 − 凍結 2 − 即時是正 9 = **除外 75 件**。理由: 移送前の二重追記は移送 diff を汚す） |
| A-B | 完全削除ダイアログの解放容量が GB 固定で実質常に 0.00 GB（ADR-0037 決定 20 の目的不達） | **是正済み**。`formatBytes`（B/KB/MB/GB 自動選択）を新設しダイアログへ適用、単体・画面テストを追随（使用量／上限は GB スケールが適正のため `toGb` のまま） |
| A-C | private-notes BFF の認可ゲート非対称（端末・トークン系は write ゲート外）の判断が実装 ADR に無い | **是正済み**。**IADR-0285** として記録（未消費だった予約番号を充当）。コード注釈から ADR への導線も追加 |
| T-軽微3 | 波 2 の監査記録コミットに計器コメント 5 行が同梱（1 コミット 1 論理変更の唯一の例外） | **記録のみ**（履歴不変） |
| T-軽微4 | IADR-0284 が `Proposed` のまま実装がマージ | **追跡**: 決定 6 の保留（SEARCH_HITS）は明示的で適法。**#992 の裁定（案 2）が出たら Accepted へ遷移**させる —— 波 5 の帳簿で #992 コメントに残す |

## 検査器候補（同型 2 回目の記録）

- **仕様書 status の据え置き検査**: `.ai-context/specs/` の `status: draft` な仕様書名が、
  同一 PR 範囲の実装コミット（feat/fix/test）に現れたら fail。波 2 R1（4 件）→ 波 3 T-推奨1（1 件）で
  条件成立。**波 4 の baseline 掃き寄せで新設する。**

［2026-08-28 追記 / #1023］🔴 **上の「波 4 で新設する」を取り消した。新設しない。**
波 4 で 3 案（チェックボックス整合 / 上記の PR 範囲交差 / live な `docs/` の trace ブロックが
`draft` を引く）を本 PR の 144 コミットへ総当たりで当てて実測したところ、**どの案も
「動機になった 2 件を捕まえる」と「正常な途中状態を誤検知しない」を両立しなかった** ——
上記の案そのものは **144 コミット中 47 で発火（33%）し、しかも波 3 の #449 を取り逃がす**。
判断と数字の正本は [[IADR-0287]]。あわせて**事故の実体は 2 件ではなく `draft` 滞留 46 件**である
ことが測定中に判明したため、母集合ごと専用 issue **#1023** へ回した（本 PR では一括是正しない —— 
`status` を確認せずに倒すと滞留が「機械的に done と書かれた嘘」へ悪化する）。
