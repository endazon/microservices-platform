---
title: 作業仕様書 — 計画 ADR レンジを ADR-0001..0117 へ更新する（#1612・ADR-0116・ADR-0117 を引く作業の前提）
type: spec
status: done
related_ids:
  - NFR
  - ADR-0116
  - ADR-0117
author: claude
created: 2026-09-27
updated: 2026-09-27
plan_refs:
  - planning:projects/microservices-platform/07_adr/ADR-0116_sc17-department-edits-group-membership.md (Accepted 2026-09-26)
  - planning:projects/microservices-platform/07_adr/ADR-0117_mcp-tool-destination-and-execution-context.md (Accepted 2026-09-26)
related_specs:
  - 20260926_1565_plan-adr-range-0115
issue: "#1612"
---

# 作業仕様書 — 計画 ADR レンジを `ADR-0001..0117` へ

## 目的と射程

`.claude/rules/traceability.repo.md`「起点 ID の種別（固有）」節の計画 ADR レンジ宣言を `ADR-0001..0115` → `ADR-0001..0117` へ更新する。
計画側に次の 2 件がマージされた。0116・0117 を引く作業が件名・trace ブロックで引けるよう、前回（#1565 / #1566）と同じ形で開ける。

- ADR-0116（SC-17 の部門欄は部門グループの所属を変える。外れた利用者の部門属性は消し、属性辞書の部門の値は realm の部門グループから導く。環流 planning#672 / planning#675）
- ADR-0117（MCP のツール定義から endpoint を外し、実行先は申告したサービスとツール名で決める。ツールの実行口は FR-16 の別作業、利用者文脈は本文で運ぶ。環流 planning#677 / planning#679）

**射程はレンジ宣言 1 行と別紙への 1 回分の追記に限る**（#1488 / #1497 / #1522 / #1545 / #1554 / #1566 と同じ形）。判断を伴う決定は無いため IADR は起こさない。

## 計画側の実在確認（隣接クローン。読み取り専用）

```console
$ git -C ../project-planning rev-parse --short origin/main
2b25250
$ git -C ../project-planning log --oneline aeba6e6..origin/main -- projects/microservices-platform/07_adr/ tools/doc-checks/kg-ranges.json projects/microservices-platform/02_requirements projects/microservices-platform/03_usecases projects/microservices-platform/05_screens
2b25250 chore: /sync-impl で実装 IADR 対応表を再生成する（AST 394 件・MSP 475 件） (#682)
a45d181 adr: MSP ADR-0117 MCP のツール定義から endpoint を外し、実行口は FR-16 の別作業とし、利用者文脈を本文で運ぶ（#677） (#679)
a4a4268 adr: AST ADR-0045 …（#676） (#678)
b3e0751 adr: MSP ADR-0116 SC-17 の部門欄は部門グループの所属を変え、…（#672） (#675)
33490bf adr: AST ADR-0044 …（#673） (#674)
$ git -C ../project-planning ls-tree --name-only origin/main projects/microservices-platform/07_adr/ | tail -3
…/ADR-0116_sc17-department-edits-group-membership.md
…/ADR-0117_mcp-tool-destination-and-execution-context.md
…/README.md
$ git -C ../project-planning diff aeba6e6 origin/main -- projects/microservices-platform/07_adr/ | grep -E "^[+-]status:"
+status: Accepted
+status: Accepted
```

`2b25250` は `07_adr/README.md`（IADR 対応表）だけ、AST の 2 件は `kg-ranges.json` の AST 行と AST の ADR だけで MSP の宣言には効かない。
既存の ADR-0086・ADR-0115 は本文が変わったが `status:` は動いていない。`05_screens/01_screens.md` は §SC-17 の本文追補（ADR-0116）で、採番は動いていない。
`02_requirements` / `03_usecases` は差分なし。非機能要件表の末尾は `NFR-28` のまま（宣言 `NFR-01`〜`NFR-28` と一致。更新不要）。

作業ツリーでは測らず、前回と同じ `git archive origin/main tools projects docs` の展開物で:

```console
$ node tools/doc-checks/gen-plan-ranges.js --check
| FR | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| UC | [1, 11] | **[1, 11]** | 11 | なし | ✅ |
| SC | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| ADR | [1, 117] | **[1, 117]** | 117 | なし | ✅ |
EXIT=0
```

AST 側も `FR [1,21] / UC [1,7] / SC [1,4]` で本リポジトリの AST 修飾の宣言と一致（ADR は AST の宣言対象外）。

### 起票後に計画側が進んだこと

宣言を書き換えた後、`PLANNING_REPO_TOKEN` に `gh` の資格情報を渡して `node scripts/check-planning-adr-range.js --out …` を走らせた:

```console
  warning  計画 ID レンジ鮮度: behind（突合 4 種）— ADR: 宣言 0001..0117 が実物 0118 に 1 件遅れている
EXIT=0   # status: "behind", scanned: 4
```

公開 API で `07_adr/` を読むと、`2b25250` の直後に ADR-0118（所要時間の判定の偶然の赤の再実行と月次の記録。`4d08116`・planning#681 / planning#683）がマージされていた。
**本回は起票どおり 0117 で止める**（0113 の回と同じ扱い。宣言は実物の部分集合であり値域検査は落ちない。定期の突合が 0118 を宣言の遅れとして示す）。別紙の本回の記録にもその旨を書いた。

## 母集合の引き方（規則 1〜7・9・10）

走査は編集前の `HEAD`（= `origin/develop` `f195df1f`）に対し `git grep -n -o` で行い、出力を切らずに読んだ。

**軸 1（旧上限 `0115` のレンジ表記）**: `git grep -n -o -E "\.\.0?115\b|\[1, ?115\]|115 ?件" HEAD -- . ':!src/ai-stock-trading'` —— 25 行

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7`（宣言） | **対象** |
| `.ai-context/specs/20260926_1565_plan-adr-range-0115.md`（2・18・22・47×2・71・72 行） | 除外。確定済みの作業仕様書（前回の記録） |
| `docs/how-to/plan-id-range-history-annex.md`（27・37×2 行） | 除外。過去の引き直しの記録 |
| `.ai-context/adr/IADR-0254…`（51・117）・`.ai-context/specs/` の 12 行（`issue-756`×3・`issue-755`・`issue-790`・`issue-793`・`issue-801`・`issue-836`・`planning-pin-767a9d48`・`issue-975`×2・`issue-452`） | 除外。「115 件」は別の数（キットのファイル数・対の数・テスト件数）でレンジではない |

**軸 2（`ADR-0001..` 形の宣言）**: `git grep -n -o -E "ADR-0001\.\.0?1[0-9][0-9]" HEAD -- . ':!src/ai-stock-trading' ':!.ai-context/specs'` —— 宣言（本節）と
`IADR-0432…:358`（「本追記の時点で `ADR-0001..0110`」と時点を明示した記録）の 2 件。後者は時点つきの記録であり書き換えない。

**軸 3（計画 ADR 0116 以降の引用）**: `git grep -n -o -P "(?<!I)ADR-011[6-9]" HEAD -- . ':!src/ai-stock-trading'` —— 前回の作業仕様書 73 行（「陰性対照 `ADR-0116` は違反」）だけ。
当時の事実であり、確定済みの記録として書き換えない（規則 10）。

**軸 4（NFR の範囲表記）**: `git grep -n -o -E "NFR-01(\.\.|〜NFR-)2[0-9]" HEAD -- . ':!src/ai-stock-trading' ':!.ai-context/specs'`（別紙を除く）—— `IADR-0224…:14` と `scripts/changelog-overrides.json:58` の `NFR-01..27`。
いずれも当時の実測を記した記録で、#1527 で除外済み。計画側の NFR は不動のため本回の対象外。

**規則 10（本変更で新たに誤りになる自分の記述）**: 別紙の本回の記録は可視テキストへ計画 ID を書かない（`check-trace-blocks` が最初の走行で `SC-17`×2・`FR-16` を検出したため「利用者管理画面」「MCP 連携の要求」へ言い換え、`FR-16` を trace ブロックの `ids` へ足した）。

## 受け入れ基準

- [x] 宣言が `` `ADR-0001..0117` `` で、欠番なしの宣言と書式を保つ
- [x] `PR_TITLE="chore(NFR,ADR-0116,ADR-0117): 試験" node scripts/check-commit-messages.js` が適合（宣言レンジ 117 件）
- [x] 陰性対照 `ADR-0118` は違反（exit 1）
- [x] 別紙の trace ブロックが `ADR-0116`・`ADR-0117` を引き、`check-trace-blocks.js` が通る
- [x] `gen-knowledge-graph.js --check` が通る
- [x] `REQUIRE_REPO_TESTS=1 node scripts/scripts.test.js` が緑

## 必須チェックへの影響

レンジを広げる変更であり、値域検査が新たに赤くなる入力は無い。ワークフロー・ジョブ名は触らない。
