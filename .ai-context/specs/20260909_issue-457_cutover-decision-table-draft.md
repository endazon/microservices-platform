---
title: 再実装版への切替（#457）— 6 資産の移行/破棄 判断表の下書き（未承認・決めない）
type: spec
status: draft
related_ids: [NFR, ADR-0005, ADR-0008, ADR-0021, ADR-0026, ADR-0032, IADR-0082, IADR-0210, IADR-0369]
author: Claude（実装）
created: 2026-09-09
updated: 2026-09-09
---

# 仕様書: 切替（cutover）判断表の下書き（#457）

## 🔴 本書の性格

**本書は決めない。** #457 の 2026-09-05 棚卸しコメントが「次の一手」として明示した作業 ——

> 判断表の下書きを作る —— `.ai-context/specs/<YYYYMMDD>_issue-457_*.md` に migration 種別の仕様書を起こし、
> 6 資産それぞれについて「移行する場合のコスト／破棄する場合の失われるもの／推奨」を並べる。
> **決めない。利用者に選んでもらう形にする。** これは承認待ちのまま置いておける成果物であり、
> **#439 / #458 の着地を待つ必要が無い。**

に従って作成する。**利用者の承認欄はすべて空欄のまま置く。** 本書自身は #457 のスコープ①「移行/破棄の判断表」の
下書きであり、②切替方式・③旧実装の廃止には触れない（骨子のみ後段に置く。決定はしない）。

`docs/templates/migration_spec_template.md`（移行概要 / データマッピング / 手順・スケジュール / ロールバック・
リスク / 未決事項の構成）を土台に借り、`.ai-context/specs/` 配下の frontmatter 規約（本リポの既存仕様書と同形。
`docs/` 用の trace ブロックは適用しない）に合わせている。

## 起点

- issue #457（本体）／#454（親トラッキング）／#456（先行条件・完了済み）／#439・#458（go-live 前提）
- 計画: NFR（可用性 99.9% 目標・セキュリティ: セッション管理）／ADR-0005（サービスメッシュ Istio）／
  ADR-0008（実行基盤 k3s）／ADR-0021（エッジ）／ADR-0026・ADR-0032（認証・BFF セッション）
- 実装: [[IADR-0082]]（経路B 永続化の opt-in 起点）／[[IADR-0210]]（qdrant・可観測性の永続化）／
  [[IADR-0369]]（**現行**。永続化を既定オンへ変更し、realm 差分適用を Job 化。決定 1〜4 が本書の実測の根拠）

## 1. 現状の配備実測（2026-09-09・本仕様書作成時点の作業ツリーを走査）

### 1.1 compose（`deploy/docker-compose.yml`）—— 経路A・開発者ローカル

| 資産 | 永続化 | 実体 |
| --- | --- | --- |
| PostgreSQL | ✅ 名前付きボリューム | `postgres-data:/var/lib/postgresql/data` |
| Keycloak | ✅（Postgres 経由） | `KC_DB=postgres` の `keycloak` DB。H2 ファイルではない（compose 固有。k8s 側と異なる） |
| Qdrant | ✅ 名前付きボリューム | `qdrant-data:/qdrant/storage` |
| MinIO | ✅ 名前付きボリューム | `minio-data:/data` |
| Wiki.js | 🔴 **確認できず**（本走査では compose の wiki-js サービス定義に永続ボリュームの記載を見ていない。要再確認） | — |
| 可観測性（Prometheus/Loki/Tempo） | ✅ 名前付きボリューム | `prometheus-data:/prometheus`／`loki-data:/tmp/loki`／`tempo-data:/tmp/tempo` |

### 1.2 helm（`deploy/helm/microservices-platform/`）—— 本番像 chart

| 資産 | 既定 | 実体 |
| --- | --- | --- |
| MinIO | ✅ `persistence.enabled: true`（`values.yaml:969`）・`size: 20Gi` | `templates/minio.yaml` が PVC を条件生成 |
| Wiki.js | ✅ `persistence.enabled: true`（`values.yaml:1048`）・`size: 5Gi` | `templates/wikijs.yaml` が PVC を条件生成 |
| PostgreSQL / Qdrant / Keycloak / 可観測性 | chart 本体では管理しない（下記 1.3 のローカル overlay 側） | — |

### 1.3 `deploy/local/*` overlay（経路B・k3s ローカル dev）＋ PVC 化の状況

- `deploy/local/infra-persistence/pvcs.yaml`: **PostgreSQL（2Gi）／Keycloak（1Gi・H2 ファイル DB）／Qdrant（2Gi）** の
  3 本。[[IADR-0082]] が opt-in で導入し、[[IADR-0210]] が Qdrant を追加。
- `deploy/local/observability-persistence/pvcs.yaml`: **Prometheus（5Gi）／Loki（2Gi）／Tempo（2Gi）／Grafana（1Gi）**
  の 4 本。`OBSERVABILITY=1` のときのみ適用。
- **[[IADR-0369]]（現行・status: Proposed）が既定を反転させた**: 決定 1 により `scripts/k8s-local-up.sh` は
  **既定で上記 overlay を当てる**（`PERSIST=0` を明示したときだけ emptyDir へ戻る）。**MinIO・Wiki.js は
  この overlay の対象外**（1.2 のとおり helm chart 自身が `persistence.enabled` を持ち、経路Bでも helm 経由で
  デプロイされるため、既に既定 ON で永続化されている）。
- [[IADR-0369]] は同時に **realm の反映経路**（静的 import は空 PVC のときのみ・以降は `reconcile-realm.sh` の
  Job が Admin REST で宣言と実行時の差分を当てる）と、**3 つの検知門**（G9 realm 乖離／G10 永続化参照／
  G11 イメージ参照）を導入し、2026-09-04 の作り直し後実測で **PVC 3＋4 本すべて参照・Bound、realm 差分 0** を
  確認済み（同 ADR §実測）。

### 1.4 旧実装からの移行対象が実在するか（実測）

- **git ls-files 走査**: 作業ツリーに `.sql` / `dump` / `backup` / `snapshot` を含むファイル名は **0 件**
  （`node_modules` ・ `bin` / `obj` を除く全域）。**リポジトリ内に「移行元となるデータのエクスポート」は存在しない。**
- **`docs/` ・ `.ai-context/adr/` の走査**: 「旧実装」「移行対象」を名指しする記述は `docs/migration/rename-knowledge-platform.md`
  （knowledge/platform のリネーム作業。**本件と無関係**）と `docs/templates/migration_spec_template.md`（雛形そのもの）
  のみ。**#457 が扱う「稼働クラスタのデータ資産」を継承対象として記録した文書は無い。**
- **稼働クラスタの実測（#457 コメント履歴からの引用。本書独自の再実測ではない）**:
  - 2026-08-16 棚卸し: PostgreSQL 各サービス DB は `document_svc` 3,630 件のみで**全件 `Status=draft`・全件
    ai-stock-trading 経由の自動取り込み**、他サービス DB は全件 0。Qdrant はコレクション 0。MinIO はバケット 1・
    オブジェクト 0。Wiki.js はページ 0・アセット 0（初期セットアップ未実行）。可観測性は約 52 分ぶんのみ。
    → **人が作った資産は 1 件も無いと判定済み。**
  - 2026-09-04（[[IADR-0369]] 実測）: **クラスタを namespace ごと削除して作り直した**（`ai-stock-trading` は
    残置）。この作り直しにより、2026-08-16 時点で残っていた自動取り込みデータ・realm の runtime state を含め、
    **その時点の状態は既に失われている。**
  - **結論: 「旧実装（再実装前の稼働クラスタ）」のデータ資産は、実質的にはすでに存在しない。** 現在稼働している
    dev クラスタのデータは 2026-09-04 以降に新実装（develop 最新）が新規に作った状態であり、#457 が想定する
    「移行元」に当たるものは無い。**6 資産すべてについて「移行対象なし」と読める状態にある。**
  - 🔴 **ただし本書はこれを断定しない。** 上記は 2026-08-16／2026-09-04 の時点実測の引用であり、**その後
    （2026-09-05〜09-09）に稼働クラスタへ投入された検証データ**（各 issue の受け入れ基準実測で作られた
    一時利用者・E2E 用データ等）まではカウントしていない。**判断の直前に、本仕様書 §3 の手順で再実測すること。**
    下記の判断表は「移行対象が実質的に無い」前提と「念のため実データが見つかった場合」の両方を選択肢に含める。

## 2. 判断表（決めない。利用者の承認欄は空欄）

凡例: 「コスト」は主に**時間・手間**、「失うもの」は**選ばなかった場合に失われる価値**、
「前提」は**その選択肢を採るために必要な条件**、「戻せるか」は**選び直しの可否**。

### 2.1 PostgreSQL（各サービス DB）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（`pg_dump`/`pg_restore` 等でデータを引き継ぐ） | データ量に比例（現状実測では最大でも `document_svc` 3,630 件・他 0 件なので**小さい**）。スキーマ差分の突合が要る | なし（引き継ぐため） | 移行元 DB が生きていること。マイグレーション適用済みであること | 可（移行後も移行元を残せば） |
| B. 破棄して作り直す（初期投入・シードのみで開始） | ほぼ無し（`deploy/local/abac-seed/` ＋ `scripts/seed-abac-policies.js` で `authz_svc` は再投入可能。他は空スキーマから開始） | **2026-08-16 実測が正しければ実質ゼロ**（全件 draft・全件 finnhub 自動取り込みで人手の成果物なし）。実測が古ければ、その後投入された検証データを失う | §1.4 の再実測で「移行対象なし」を確認していること | 不可（破棄後は復元できない。移行元を別途保全しない限り） |
| C. 両立させる（新旧 DB を並行稼働し段階移行） | 最も高い。接続文字列・スキーマバージョンの二重管理、切替時の整合検証が要る | なし | 新旧を同時に指せる構成（DB ホスト分離）。#457 のスコープには無い運用の複雑さが増える | 可 |

### 2.2 Qdrant（埋め込みベクトル）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（スナップショット/リストア） | 2026-08-16 実測ではコレクション 0 件のため**移行するデータが無い**。将来データが増えた場合は埋め込みモデル・次元の一致が前提 | なし | コレクションが実在すること（現状は非該当） | 可 |
| B. 破棄して作り直す（再インデックス） | 🔴 **埋め込み再生成の費用と時間**（LLM/埋め込みプロバイダへの再送。#457 本文が最初から論点に挙げている）。現状はコレクション 0 のため**再生成対象も実質無い** | 対象が無ければ失うものも無い。将来データがあれば再生成コストがそのまま損失になる | 再生成用の原文書（MinIO/PostgreSQL 側）が残っていること | 不可（再生成には原文書と費用時間が要る） |
| C. 両立させる（新旧コレクションを並行） | Qdrant は軽量な複製が可能だが、検索先の切替（BFF/RetrievalService 側の接続先）を二重化する手間が乗る | なし | — | 可 |

### 2.3 MinIO（文書本文・資産）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（`mc mirror` 等でバケットを複製） | 2026-08-16 実測ではオブジェクト 0 件のため**複製対象が無い** | なし | オブジェクトが実在すること（現状は非該当） | 可 |
| B. 破棄して作り直す（空バケットから開始） | ほぼ無し。バケット `knowledge-normalized` を再作成するのみ | **原本保全が要否論点**（#457 本文）。現状は原本自体が無い（`document_svc.MarkdownUri` も NULL で整合） | §1.4 の再実測で「対象なし」を確認していること | 不可（原本があった場合は復元不能） |
| C. 両立させる | 複製先ストレージのコストと同期の仕組みが要る | なし | — | 可 |

### 2.4 Keycloak realm（ユーザー・ロール・ABAC 属性・TOTP 登録）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（realm export/import でユーザーを引き継ぐ） | **利用者の再登録・TOTP 再登録を避けられる**（#457 が最も費用対効果の判断が要ると名指しした資産）。実利用者がいる場合はコストが最も高い（人が絡む作業） | なし（引き継ぐため） | 引き継ぐべき実利用者が実在すること | 部分的（一度移行した認証情報は利用者側の TOTP 再登録なしには戻せない） |
| B. realm.json から再構築（2026-08-16 判断の踏襲） | 低い（`deploy/keycloak/*-realm.json` が単一情報源として既に整備されている） | 2026-08-16 実測時点のユーザー 4（`admin`/`developer`/`poc-operator`/`poc-user`）は**開発用アカウントであり実利用者ではない**と判定済み。**実利用者がいる場合はその認証情報（TOTP 登録含む）を失う** | 実利用者が存在しないこと（§1.4 の再実測で確認）。加えて稼働 realm 名の食い違い（旧名 `microservices-platform` 残存）の是正を兼ねられる | 不可（利用者の TOTP 登録は再構築で消える） |
| C. 両立させる（新旧 realm を並行） | 高い。OIDC issuer の切替先を全クライアントで揃える必要があり、[[IADR-0369]] の realm 差分適用（宣言/実行時の境界）とも整合させる設計が要る | なし | — | 可 |

### 2.5 Wiki.js（閲覧データ・OIDC 設定）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（DB エクスポート/インポート） | 2026-08-16 実測ではページ 0・アセット 0（初期セットアップ未実行）のため**移行対象が無い** | なし | ページ/アセットが実在すること（現状は非該当） | 可 |
| B. 破棄して作り直す（#1108/PR #1136 の冪等 bootstrap で再セットアップ） | 低い。**既に自動化済み**（`WikiService` 側の同期が正常化している。#1108 参照） | 対象が無ければ失うものも無い | — | 不可（が、対象が無いため実質的な損失も無い） |
| C. 両立させる | Wiki.js は同期先が 1 つの設計（`WikiJs__GraphQlEndpoint`）であり、並行稼働は構成の二重化が要る | なし | — | 可（構成変更のみ） |

### 2.6 可観測性データ（Prometheus/Loki/Tempo 履歴）

| 選択肢 | コスト | 失うもの | 前提 | 戻せるか |
| --- | --- | --- | --- | --- |
| A. 移行する（TSDB/チャンクのコピー） | #457 本文が最初から「原則破棄可」と位置づけている。2026-08-16 実測でも実データは約 52 分ぶんのみ | 障害調査等に使える直近の履歴（現状は僅少） | — | 可（が、価値が低い） |
| **B. 破棄する（推奨側の既定に近い。ただし本書は決めない）** | ほぼ無し | 直近の運用履歴のみ（#457 本文が許容している） | — | 不可（が、失うものが小さい） |
| C. 両立させる | 監視基盤の二重化は運用コストに見合わない可能性が高い | なし | — | 可 |

**承認欄（利用者記入）**

| 資産 | 選択（A/B/C） | 承認者 | 日付 | 備考 |
| --- | --- | --- | --- | --- |
| PostgreSQL | 　 | 　 | 　 | 　 |
| Qdrant | 　 | 　 | 　 | 　 |
| MinIO | 　 | 　 | 　 | 　 |
| Keycloak realm | 　 | 　 | 　 | 　 |
| Wiki.js | 　 | 　 | 　 | 　 |
| 可観測性データ | 　 | 　 | 　 | 　 |

## 3. 判断前に必ず再実測すること（§1.4 の限界を埋める）

本書の§1.4は 2026-08-16／2026-09-04 の**時点**実測の引用であり、以降に投入された検証データを数えていない。
利用者判断の直前に、次の再実測を行うこと（[[IADR-0369]] §実測と同型のコマンドで足りる）。

1. 各サービス DB の件数（`SELECT count(*)` を全テーブルへ）と `Status` / `Tags` の内訳
2. Qdrant `GET /collections` のコレクション数
3. MinIO のバケット一覧・オブジェクト数
4. Keycloak realm のユーザー一覧（`admin`/`developer`/`poc-operator`/`poc-user` 以外が増えていないか）
5. Wiki.js のページ数・アセット数
6. Prometheus/Loki/Tempo の保持期間（`minTime`/`maxTime`）

## 4. 切替手順の骨子（凍結 → バックアップ → 切替 → 検証 → 旧廃止）— 骨子のみ。詳細は判断表の承認後

1. **凍結**: 移行対象と判断された資産への書き込みを止める（メンテナンスウィンドウの要否は #457 本文が可用性
   99.9% 目標とのトレードオフとして未決事項に残している。本書では選ばない）
2. **バックアップ**: 凍結直後の状態をエクスポートする（対象が無いと判定された資産は本段をスキップできる）
3. **切替**: 新実装の受け皿（DB 接続文字列・Qdrant コレクション名・MinIO バケット・realm 名・Wiki.js 同期先）を
   本番向けに切り替える
4. **検証**: 件数突合（移行前後の一致）とサンプル照合。再実行可能なスクリプトとして残す —— 型は
   `scripts/measure-abac-combinations.js` の `--json`/`--dump`/`--input`（収集と集計の分離）を踏襲する
   （#457 の 2026-08-30 棚卸しコメントが同型を指定済み）
5. **旧廃止**: 旧 ArgoCD Application・イメージ・不要ブランチの整理、`CLAUDE.md`/`AGENTS.md`/`docs/`/README の
   実態への更新、#454 残存 issue 一覧の最終トリアージ

**本節は骨子であり、方式（メンテナンスウィンドウ／段階切替）そのものは決めない。** #457 本文が要求する
「可用性 99.9% 目標を踏まえた切替方式の決定」は、判断表の承認後に別途行う。

## 5. 判断条件 6 つの現況（GitHub の issue 状態を実際に確認。2026-09-09 時点）

#457 の 2026-08-30／2026-09-03 棚卸しコメントが定めた 6 条件を、`issue_read` で state を確認して更新する。

| # | 条件 | 状態（本書作成時点で確認） |
| --- | --- | --- |
| 1 | **#1107**（BffSession の配備構成） | ✅ **CLOSED**（2026-08-30・PR #1114 マージ済み） |
| 2 | **#1108**（Wiki.js の初期セットアップ） | ✅ **CLOSED**（2026-08-31・PR #1136 マージ済み） |
| 3 | **#1101**（IdentityAdmin の実 Keycloak 化） | ✅ **CLOSED**（2026-08-31・PR #1138 マージ済み） |
| 4 | **#439 の④実疎通**（無効化 → 次リクエストで 401） | ✅ **測定は完了**（受け皿 **#1168** が 2026-09-04 CLOSED。PR #1239 で稼働クラスタ上「2 枚組あり＝5 秒後 401・`KC-SERVICES0057` 0 行・受理ログ有り」を陰性/陽性の対で実測）。🔴 **ただし #439 自体はまだ OPEN**（2026-09-05 時点。残る 1 点は「実基盤のブラウザ E2E を置く場所が無い」という**運用・CI 設計の未決**であり、即時失効という機能そのものの再検証事項ではない） |
| 5 | **#442 / #458 の着地** | #442 は ✅ **CLOSED**（2026-09-04）。**#458 は 🔴 OPEN のまま**（2026-09-06 時点で `blocked` ラベルは解除されたが、残作業として「Retrieval/AiAnalysis の未認証短絡是正（#1318）」「IngestionService/LlmGateway の REST 面が無認可のまま並走」「mTLS STRICT 化の棚卸し（#1316 待ち）」「`ISecretStore` 抽象・コネクタ資格情報の Vault 移行」が残る。planning 側の裁定 `ADR-0084` で判定単位は確定したが実装側の残作業は未完了） |
| 6 | **#454 の残存 issue 一覧の凍結** | 🔴 **未達**。2026-09-05 時点でも新規起票が続いている（#457 自身の棚卸しコメントが確認済み） |

**結論**: 6 条件中、1〜4 は実質的に満たされている（4 は #439 自体の close 待ちではあるが、切替判断に関わる
「即時失効の実効性」という中身は実測で確認済み）。**5（#458）・6（一覧凍結）が満たされておらず、#457 の
2026-09-05 棚卸しの判定「C（blocked・open 維持）」は本書作成時点でも変わっていない。**

## 6. 未決事項

- 上記判断表の A/B/C は**利用者が選ぶ**。技術的な推奨は各行に書いたが、Keycloak realm と Qdrant は
  「費用と手間のトレードオフであって技術的正解が無い」（#457 の 2026-09-05 棚卸しコメント）ため、
  本書はどちらにも丸をつけない。
- 切替方式（メンテナンスウィンドウ／段階切替）は可用性 99.9% 目標とのトレードオフであり、判断表の承認後に
  別途決定する。
- §3 の再実測は本書作成時点では実施していない（読み取り専用の稼働クラスタ操作が必要なため、判断の直前に
  実施することとし、本書では実測手順のみ提示した）。
- compose（1.1）の Wiki.js 永続化の有無は本走査で確認しきれていない。判断表の対象は経路B（helm/k3s）を
  主とし、compose 側は開発者ローカルの利便性の話であるため実質的な影響は無いと考えられるが、確認が要る。
