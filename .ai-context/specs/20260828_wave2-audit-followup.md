---
title: 波 2 フェーズ末クロス監査の完了記録と指摘の処置
type: spec
status: done
related_ids:
  - FR-05
  - FR-17
  - FR-19
  - FR-22
  - ADR-0027
  - ADR-0033
author: claude
created: 2026-08-28
updated: 2026-08-28
related_adrs:
  - IADR-0141
  - IADR-0151
  - IADR-0215
  - IADR-0245
  - IADR-0253
  - IADR-0281
---

# 波 2 フェーズ末クロス監査の完了記録と指摘の処置

## 実施

- 2026-08-28、**adr-guardian ＋ traceability-auditor** を同時に、書いたエージェントと別の
  フレッシュな文脈で実施した（実装ADR IADR-0141 の巡数規約）。
- 母集合は **range `3788a48..ce52912`**（28 コミット・149 ファイル / +7972 / −1017）。
  issue 一覧ではなく range を渡した（引継資料 §2 の教訓）。
- 両監査とも `git rev-parse --is-shallow-repository` → `true` を先に確認し、
  git log / blame を出典に使っていない（★規約）。
- 判定: **重大違反 0**。両監査とも「これ以上の全面巡回は不要」と打ち切りを明言した。

## 指摘と処置

| # | 指摘 | 処置 |
| --- | --- | --- |
| 🟡A | attribute-values 端点だけが分岐時にも旧算出値 `scope.Filters`（キー単位 union）を連言に残し、IADR-0253 決定 2 の非包含により候補が過度に絞られ得る（deny 側。IADR-0151 決定 1 が禁じた「検索には出るが候補に無い値」の形） | **是正済み**。`SearchEndpoints.cs` を検索側 `BuildFilters` と同形へ（分岐があれば連言は空）。端点経由の回帰テスト `AttributeValues_WithBranches_DoesNotAndTheLegacyUnionFilters` を追加し、**変異試験で旧実装に戻すと赤になることを実測**（Failed 1 → 修正で緑） |
| 🔵B | 無認証の受け口が計器タグ `notification.kind`（非有界）を駆動できる | **受容として記録**。値域の開放は IADR-0215 決定 2 の意図した決定であり、丸めると新種別ごとに計器の追随が要る。`NotificationDeliveryMetrics` へ受容コメントを追記 |
| 🔵C | `IWikiAccessResolver.ResolveAsync` に `action` 引数が無い（read 暗黙） | **記録のみ**。WikiService は read 専用端点のみで IADR-0272 決定 4 の射程外。**書き込み端点を足す時点で必ず引き直す**（本記録が引き当て先） |
| 🔵D | Qdrant 入れ子フィルタは「型で使える」までの確認で、実ブローカの意味論は未実測 | **残件として記録**。統合スタック側で覆うか受容するかは波 4 の baseline 掃き寄せ時に判断（E3b の「マージ後に integration.yml 確認」と同じ束） |
| R1 | E3a / E3b / #1016 / #911 の 4 仕様書が `status: draft` のまま・宣言した変異試験が未実測 | **是正済み**。5 変異すべて実測して各仕様書へ追記し `done` へ（下記） |
| R2 | E3b 仕様書に受け入れ基準節が無い | **是正済み**。既存実測をチェックボックス化した「受け入れ基準（段 c）」節を追加 |
| R3 | 4 件は仕様書が feat コミットに同梱され「着手前作成」の証跡が粒度から読めない | **以後の作法として確定**: 仕様書の起草は独立の docs コミットに切る（遡及分割はしない） |
| M1 | 新規テストのメソッド直前コメントに ID 無し（クラス冒頭・節見出しでは担保） | **受容**。TEST_STRATEGY の規約は「起点 ID を持たないテストを禁止しない」と明記し、クラス単位の担保と `check-test-traceability` 緑で足りる |
| M2 | EF 生成マイグレーション 4 件に ID 無し | **受容**（ツール生成物。手書き注記は再生成で消える。起点はエンティティ側が保持） |
| M3 | CWE-117 是正コミットの無採番 `NFR` | **適合の確認のみ**（当たる番号が無い作業。別紙の規定どおり） |
| ④-2 | `check-landed-subjects` がローカル shallow でスキップされ、この波では未実施 | **確認済み**: `ci.yml` の該当ジョブは `fetch-depth: 0` を持ち、CI では全履歴で実効する（ローカル限定の限界） |

## 変異試験の実測（R1 の回収。詳細は各仕様書の追記）

| 変異 | 結果 |
| --- | --- |
| W1（E3a: 本番 Program.cs の削除段登録を外す） | **生存**（knowledge 全 1,037 件緑）。予告したテスト名は実在せず、本番結線の欠落はユニットの射程外 —— 防御の所在（ヘルパ両向きテスト・宣言突合・統合スタック）を E3a 仕様書へ記録。検査器の新設はしない（同型事故 1 回目・記録に留める） |
| D1（Retrieval: 索引掃除を外す） | **kill**（Failed 1） |
| D2（Graph: 辺削除を外す） | **kill**（Failed 1） |
| A1（GraphDocument.TryApply の順序ガードを外す） | **kill**（Failed 2） |
| A2（#911: 指紋変化ゲートを外す） | **予告どおり生存＝受容**。二重防御の内訳を実測: 消費側ゲートは走査費用の最適化、意味上の正はドメイン `AiSuggestion.TryReinstate` の同値比較（#911 仕様書へ記録） |
| 監査 A の回帰（attribute-values を旧実装へ戻す） | **kill**（Failed 1。新設テストが捕まえる） |

## 監査依頼書の誤り（自己記録）

- 監査依頼文が通知内容の制約の出典を「ADR-0034 決定 9」と誤引用した（正は FR-22 /
  ADR-0037 決定 6 / ADR-0045 決定 7）。実装側のコメントは正しく、制約自体は適合。
  **監査へ渡す依頼文の引用 ID も実装と同じく計画書から引き直すこと**（記憶で書かない）。
