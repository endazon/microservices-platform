---
title: 作業仕様書 — 計画 ADR レンジを ADR-0001..0114 へ更新する（#1553・ADR-0114 を引く作業の前提）
type: spec
status: done
related_ids:
  - NFR
  - ADR-0114
author: claude
created: 2026-09-26
updated: 2026-09-26
plan_refs:
  - planning:projects/microservices-platform/07_adr/ADR-0114_sync-token-rejected-after-account-disable.md (Accepted 2026-09-26)
related_specs:
  - 20260926_1542_plan-adr-range-0113
issue: "#1553"
---

# 作業仕様書 — 計画 ADR レンジを `ADR-0001..0114` へ

## 目的と射程

`.claude/rules/traceability.repo.md`「起点 ID の種別（固有）」節の計画 ADR レンジ宣言を `ADR-0001..0113` → `ADR-0001..0114` へ更新する。
前回（#1542 / #1545）は射程を ADR-0113 までに限り、`origin/main` に在った ADR-0114 を開けずに残した。ADR-0114 を引く作業が件名・trace ブロックで
0114 を引けるよう、同じ形で開ける。

**射程はレンジ宣言 1 行と別紙への 1 回分の追記に限る**（#1488 / #1497 / #1522 / #1545 と同じ形）。判断を伴う決定は無いため IADR は起こさない。

## 計画側の実在確認（隣接クローン。読み取り専用）

```console
$ cd ../project-planning && git fetch origin && git rev-parse --short origin/main
8b44bba                                   # 前回（#1545）の出典と同じ
$ git log --oneline 8b44bba..origin/main -- projects/microservices-platform/07_adr/ tools/doc-checks/kg-ranges.json
（空）
$ git ls-tree --name-only origin/main projects/microservices-platform/07_adr/ | grep -o "ADR-01[1-9][0-9]" | sort -u
ADR-0110 … ADR-0114
```

ADR-0114 の frontmatter は `status: Accepted`（前回の仕様書 §計画側の実在確認 の `git diff 244e63c origin/main` で `+status: Accepted` を確認済み。以後 `07_adr/` の変更は無い）。
作業ツリーでは測らず、前回と同じ `git archive origin/main tools projects docs` の展開物で:

```console
$ node tools/doc-checks/gen-plan-ranges.js --check
| ADR | [1, 114] | **[1, 114]** | 114 | なし | ✅ |      # FR [1,22] / UC [1,11] / SC [1,22] も一致
EXIT=0
```

## 母集合の引き方（規則 1〜6・9・10）

**軸 1（旧上限 `0113` のレンジ表記）**: `git grep -n -E "\.\.0?113\b|\[1, ?113\]|113 ?件" -- . ':!src/ai-stock-trading'`

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7`（宣言） | **対象** |
| `.ai-context/specs/20260926_1542_plan-adr-range-0113.md`（2・22・27・116 行） | 除外。確定済みの作業仕様書（前回の記録） |
| `docs/how-to/plan-id-range-history-annex.md:27`（前回の見出し） | 除外。過去の引き直しの記録 |

**軸 2（`ADR-0001..` 形の宣言）**: 前回（#1545 の仕様書 軸 2）と同じ集合。宣言は本節 1 か所だけで、他は書式説明・合成レンジ・過去の記録。

**軸 3（計画 ADR 0114 以降の引用）**: `git grep -n -P "(?<!I)ADR-011[4-9]" -- . ':!src/ai-stock-trading'` —— 前回の作業仕様書（57・78・111・118 行）だけ。
「0114 を開けていない」「陰性対照 0114 は違反」は当時の事実であり、確定済みの記録として書き換えない（規則 10）。

## 受け入れ基準

- [x] 宣言が `` `ADR-0001..0114` `` で、欠番なしの宣言と書式を保つ
- [x] `PR_TITLE="chore(NFR,ADR-0114): 試験" node scripts/check-commit-messages.js` が適合（宣言レンジ 114 件）
- [x] 陰性対照 `ADR-0115` は違反
- [x] 別紙の trace ブロックが `ADR-0114` を引き、`check-trace-blocks.js` が通る
- [x] `node scripts/scripts.test.js` が緑

## 必須チェックへの影響

レンジを広げる変更であり、値域検査が新たに赤くなる入力は無い。ワークフロー・ジョブ名は触らない。
