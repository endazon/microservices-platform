---
title: 作業仕様書 — 計画 ADR レンジを ADR-0001..0113 へ更新する（#1542・ADR-0113 の実装の前提）
type: spec
status: done
related_ids:
  - NFR
  - ADR-0111
  - ADR-0112
  - ADR-0113
author: claude
created: 2026-09-26
updated: 2026-09-26
plan_refs:
  - planning:projects/microservices-platform/07_adr/ADR-0111_reset-floor-replicas-and-no-bypass-on-failure.md (Accepted 2026-09-26)
  - planning:projects/microservices-platform/07_adr/ADR-0112_infrastructure-admin-endpoint-criterion.md (Accepted 2026-09-26)
  - planning:projects/microservices-platform/07_adr/ADR-0113_timing-judgement-rank-sum-test.md (Accepted 2026-09-26)
related_specs:
  - 20260926_1519_plan-adr-range-0110
issue: "#1542"
---

# 作業仕様書 — 計画 ADR レンジを `ADR-0001..0113` へ

## 目的と射程

`.claude/rules/traceability.repo.md`「起点 ID の種別（固有）」節の計画 ADR レンジ宣言を
`ADR-0001..0110` → `ADR-0001..0113` へ更新する。同節は `check-commit-messages.js` /
`check-trace-blocks.js` の一次情報であり、更新しないまま `ADR-0111`〜`ADR-0113` を引くと
コミット件名・PR タイトル・trace ブロックの値域検査が落ちる。

後続の 2 件が件名で ADR-0113 を引く: ①#1526 が判定式より先に入れた整数 ns の時計の差し戻し
（ADR-0113 決定 4「時計の変更だけを先に入れない」）②ADR-0113 決定 1〜4 の実装（#1541）。

**射程はレンジ宣言 1 行と、その追随記録（別紙への 1 回分の追記）に限る**（#1488 / #1497 / #1522 と同じ形）。
判断を伴う決定は無いため IADR は起こさない。

## 計画側の実在確認（隣接クローン。読み取り専用）

```console
$ cd ../project-planning && git fetch origin && git log --oneline -3 origin/main
8b44bba adr(AST): 確定した AI の監視銘柄の入れ替え案を Discord の確認ボタンで適用してよいとし、…（環流 #663） (#665)
04d873e adr: アカウントを無効化したら同期トークンを次の同期要求から拒否する。…（環流 #662） (#664)
6847277 adr: 所要時間の判定を順位和検定（両側・有意水準 1%・片側 12）へ改める（環流 #659）(#661)
$ git rev-parse --is-shallow-repository
false

$ git log --oneline 244e63c..origin/main -- projects/microservices-platform/07_adr/
04d873e adr: アカウントを無効化したら同期トークンを次の同期要求から拒否する。…（環流 #662） (#664)
6847277 adr: 所要時間の判定を順位和検定（両側・有意水準 1%・片側 12）へ改める（環流 #659）(#661)
8aa6a75 adr: SeaweedFS の受け入れ試験の合格を記録し ADR-0106 決定 3 の見込みを訂正する。管理用の口の選定基準を ADR-0112 で加える（環流 #658）(#660)
231dd97 adr: リセット申請の床は複数レプリカで動かし、落ちても床を外さない。…（環流 #656）(#657)

$ git diff 244e63c origin/main -- projects/microservices-platform/07_adr/ | grep -E "^[-+]status:"
+status: Accepted     # ADR-0111
+status: Accepted     # ADR-0112
+status: Accepted     # ADR-0113
+status: Accepted     # ADR-0114
```

既存 ADR（0001〜0110）の `status:` 行の変更は無い（変更のあった既存 10 ファイル（0037・0078・0082・0094・0096・0097・0103・0106・0107・0108）は本文の日付つき改訂のみ）。

作業ツリー（`9014e4f`）は遅れており、`git diff --stat HEAD origin/main -- tools/doc-checks/kg-ranges.json` が
`1 file changed, 2 insertions(+), 2 deletions(-)` —— 前回と同じく作業ツリーでは測らない。
`git archive -o <scratch>/rs-plan.tar origin/main tools projects docs` を scratch へ展開して走らせた:

```console
$ node tools/doc-checks/gen-plan-ranges.js --check      # origin/main 8b44bba の展開物
## microservices-platform
| FR | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| UC | [1, 11] | **[1, 11]** | 11 | なし | ✅ |
| SC | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| ADR | [1, 114] | **[1, 114]** | 114 | なし | ✅ |
EXIT=0
```

### 0114 を開けない理由

`origin/main` には ADR-0114（同期トークンの拒否。planning#664）も在るが、本 issue は ADR-0113 を引く作業の前提として
起票したものであり、0114 を引く作業は本リポジトリにまだ無い。**宣言は実物の部分集合であり、値域検査は落ちない。**
`check-planning-adr-range.js`（定期の突合・警告のみ）は宣言の遅れとして 0114 を示す —— 次の追随の契機として残す。

## 母集合の引き方（規則 1〜6・9・10）

**軸 1（誤りの側の文字列＝旧上限 `0110` のレンジ表記、あり得る形を列挙）**:

```console
$ git grep -n -E "\.\.0?110\b|0001[-〜~–]0110|\[1, ?110\]|110 ?件" -- . ':!src/ai-stock-trading'
```

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7`（宣言 1 行） | **本作業の対象** |
| `.ai-context/specs/20260926_1519_plan-adr-range-0110.md`（7 行） | 除外。確定済みの作業仕様書（前回の記録。当時の値として正しい） |
| `docs/how-to/plan-id-range-history-annex.md:27,40,41`（前回の見出し・実測行） | 除外。過去の引き直しの記録 |
| `.ai-context/specs/` の 4 件（`110 件`：テスト件数・未記載件数・フォルダ件数） | 除外。無関係な一致 |

**軸 2（`ADR-0001..` 形のレンジ宣言全般。他の宣言箇所の有無）**:
`git grep -n -E "ADR-0001\.\." -- . ':!src/ai-stock-trading' ':!CHANGELOG.md' ':!.ai-context'`

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7` | 対象（同上） |
| `scripts/check-commit-messages.js:254,667` / `scripts/check-trace-blocks.js:23,219` | 除外。`ADR-0001..NNNN` の書式説明で値を持たない |
| `scripts/check-test-traceability.js:439,455` / `scripts/scripts.repo.test.js:1513,1620` | 除外。合成レンジ（0039 / 0068）のテスト入力 |
| 別紙の過去回の本文（467・537・556・577・590・604・682 行） | 除外。過去の記録 |

**軸 3（計画 ADR 0111 以降の引用。「最新は 0110」と書く箇所の有無）**: `git grep -n -P "(?<!I)ADR-011[1-9]" -- . ':!src/ai-stock-trading'`
—— ヒットは前回の作業仕様書の陰性対照（`ADR-0111 は違反`）1 行だけであり、上限としての言及は無い。

**規則 10（この変更で新たに誤りになる自分の記述）**: 前回仕様書の「陰性対照 `ADR-0111` は違反」は本変更の後は
成り立たなくなるが、確定済みの記録（当時の事実）であり書き換えない。本仕様書の陰性対照は `ADR-0114` とした。
必読規約（`traceability.repo.md`）は数字 3 桁の置換のみでバイト数は不変。

## 受け入れ基準

- [x] 宣言が `` `ADR-0001..0113` `` で、欠番なしの宣言と書式を保つ
- [x] `PR_TITLE="chore(NFR,ADR-0111,ADR-0112,ADR-0113): 試験" node scripts/check-commit-messages.js` が EXIT=0
- [x] 陰性対照 `ADR-0114` は違反
- [x] 別紙の trace ブロックが `ADR-0111`〜`ADR-0113` を引き、`check-trace-blocks.js` が通る
- [x] `node scripts/scripts.test.js` が緑

（実行結果は PR 本文に貼る。）

## 必須チェックへの影響

レンジを広げる変更であり、既存の履歴・文書が引く計画 ADR はすべて 0110 以下（軸 3）。
値域検査が新たに赤くなる入力は存在しない。ワークフロー・ジョブ名は触らない。
