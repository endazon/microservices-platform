---
title: 作業仕様書 — 計画 ADR レンジを ADR-0001..0110 へ更新する（#1519・ADR-0108 の実装の前提）
type: spec
status: done
related_ids:
  - NFR
  - ADR-0108
  - ADR-0109
  - ADR-0110
author: claude
created: 2026-09-26
updated: 2026-09-26
plan_refs:
  - planning:projects/microservices-platform/07_adr/ADR-0108_timing-samples-at-microsecond-resolution.md (Accepted 2026-09-26)
  - planning:projects/microservices-platform/07_adr/ADR-0109_bff-user-credential-relay-is-edge.md (Accepted 2026-09-26)
  - planning:projects/microservices-platform/07_adr/ADR-0110_sc22-supplier-three-values-no-public-key-restart-confirmed-at-write.md (Accepted 2026-09-26)
related_specs:
  - 20260925_1496_plan-adr-range-0107
issue: "#1519"
---

# 作業仕様書 — 計画 ADR レンジを `ADR-0001..0110` へ

## 目的と射程

`.claude/rules/traceability.repo.md`「起点 ID の種別（固有）」節の計画 ADR レンジ宣言を
`ADR-0001..0107` → `ADR-0001..0110` へ更新する。同節は `check-commit-messages.js` /
`check-trace-blocks.js` の一次情報であり、更新しないまま `ADR-0108`〜`ADR-0110` を引くと
コミット件名・PR タイトル・trace ブロックの値域検査が落ちる。後続の planning#650 の裁定（ADR-0108。
所要時間の検査器の時計を 1 ms より細かくする）の実装が件名で ADR-0108 を引く。

**射程はレンジ宣言 1 行と、その追随記録（別紙への 1 回分の追記）に限る**（#1488 / #1497 と同じ形）。
3 件の中身の実装反映はそれぞれの作業が持つ。判断を伴う決定は無いため IADR は起こさない。

## 計画側の実在確認（隣接クローン。読み取り専用）

```console
$ git -C ../project-planning log --oneline -3 origin/main
244e63c adr: SC-22 の供給元を 3 値にし公開鍵の欄を取り下げ、書き込みの確認を再起動の確認とする（環流 #652）(#655)
59b68ff adr: BFF が利用者の資格情報を中継する呼び出しをエッジと分類する（環流 #651）(#654)
248a7f9 adr: 所要時間の項目 ID を T-25 へ戻し、標本を 1 ms より細かい分解能で測る（環流 #650）(#653)

$ git -C ../project-planning ls-tree --name-only origin/main projects/microservices-platform/07_adr/ | grep -o "ADR-[0-9]\{4\}" | sort -u | wc -l
110
# 末尾: ADR-0108_timing-samples-at-microsecond-resolution / ADR-0109_bff-user-credential-relay-is-edge /
#       ADR-0110_sc22-supplier-three-values-no-public-key-restart-confirmed-at-write

$ git -C ../project-planning log --oneline -1 HEAD
9014e4f ...                                  # 作業ツリーは origin/main から遅れている
$ git -C ../project-planning diff --stat HEAD origin/main -- projects/microservices-platform/07_adr/ tools/doc-checks/kg-ranges.json
 tools/doc-checks/kg-ranges.json | 2 +-       # 🔴 遅れ分が公開レンジに触れている
 18 files changed, 682 insertions(+), 32 deletions(-)
```

前回（#1497）と同じく作業ツリーでは測らない。読み取り専用の隣接クローンを checkout で動かさず、
`git archive origin/main tools projects docs` を scratch へ展開して走らせた:

```console
$ node tools/doc-checks/gen-plan-ranges.js --check      # origin/main 244e63c の展開物
## microservices-platform
| FR | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| UC | [1, 11] | **[1, 11]** | 11 | なし | ✅ |
| SC | [1, 22] | **[1, 22]** | 22 | なし | ✅ |
| ADR | [1, 110] | **[1, 110]** | 110 | なし | ✅ |
**参考（レンジ表へは足さない）**: NFR は実物 [1, 28]・28 件。
✅ 宣言と実物は一致している
EXIT=0
```

3 件とも frontmatter `status: Accepted`（2026-09-26）。前回の出典 `6d88ca2` から `244e63c` までの
`07_adr/` の差分に、既存 ADR（0001〜0107）の `status:` 行の変更は無い（追加 3 ファイルの `+status: Accepted` のみ）。

## 母集合の引き方（規則 1〜6・9・10）

**軸 1（誤りの側の文字列＝旧上限 `0107` のレンジ表記、あり得る形を列挙）**:

```console
$ git grep -n -E "\.\.0?107\b|0001[-〜~–]0107|\[1, ?107\]|107 ?件|max.{0,10}107" -- . ':!src/ai-stock-trading'
```

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7`（宣言 1 行） | **本作業の対象** |
| `.ai-context/specs/20260925_1496_plan-adr-range-0107.md`（6 行） | 除外。確定済みの作業仕様書（前回の記録。当時の値として正しい） |
| `docs/how-to/plan-id-range-history-annex.md:27,38,39`（前回の見出し・実測行） | 除外。過去の引き直しの記録 |
| `IADR-0185` / `IADR-0291` / `.ai-context/specs/` の 5 件（`107 件`） | 除外。無関係な一致（テスト件数の 107） |

**軸 2（`ADR-0001..` 形のレンジ宣言全般。他の宣言箇所の有無）**:
`git grep -n -E "0001\.\.0?1[01][0-9]|ADR-0001\.\." -- . ':!src/ai-stock-trading' ':!CHANGELOG.md' ':!.ai-context'`

| ヒット | 扱い |
| --- | --- |
| `.claude/rules/traceability.repo.md:7` | 対象（同上） |
| `scripts/check-commit-messages.js:254,667` / `scripts/check-trace-blocks.js:23,219` | 除外。`ADR-0001..NNNN` の書式説明で値を持たない |
| `scripts/check-test-traceability.js:439,455` / `scripts/scripts.repo.test.js:1513,1620` | 除外。合成レンジ（0039 / 0068）のテスト入力 |
| 別紙の過去回の見出し・本文 | 除外。過去の記録 |
| `CHANGELOG.md`・`.ai-context/`（パスで除外） | 除外。生成物・凍結記録（当時の値）。前回 #1497 の軸 2 で同じ集合を確認済み |

**軸 3（計画 ADR 0108 以降の引用。「最新は 0107」と書く箇所の有無）**: `git grep -n -P "(?<!I)ADR-01(0[8-9]|1[0-9])" -- . ':!src/ai-stock-trading'`
—— ヒットは前回の作業仕様書の陰性対照（`ADR-0108 は EXIT=1`）1 行だけであり、上限としての言及は無い。

**規則 10（この変更で新たに誤りになる自分の記述）**: 前回仕様書の「陰性対照 `ADR-0108` は EXIT=1」は本変更の後は
成り立たなくなるが、確定済みの記録（当時の事実）であり書き換えない。別紙に書いた件数（110 件・3 件）は上の実測値。
必読規約（`traceability.repo.md`）は数字 3 桁の置換のみでバイト数は不変。

### 射程外として記録するもの（本 PR では直さない）

- 🔴 **計画側の `NFR` の実物は `[1, 28]`（28 件）である**が、本リポジトリの `traceability.repo.md` は
  「`NFR` の採番は `NFR-01`〜`NFR-27`」と書いている。計画側で `NFR-28`（ログ・監査記録の完全性）は 2026-09-09 に追加済み
  （環流 planning#577）。本リポジトリで `NFR-28` を引く箇所は 0 件（`git grep -n "NFR-28"` が空）。**`NFR` は実在性を検査しない**
  （同節）ため値域検査は落ちないが、宣言は古い。本 issue の受け入れ基準（ADR レンジ）の外であり、別 issue で扱う。

## 受け入れ基準

- [x] 宣言が `` `ADR-0001..0110` `` で、欠番なしの宣言と書式を保つ
- [x] `PR_TITLE="chore(NFR,ADR-0108,ADR-0109,ADR-0110): 試験" node scripts/check-commit-messages.js` が EXIT=0（notice: 宣言レンジ 110 件）
- [x] 陰性対照 `ADR-0111` は違反（「planning の 07_adr/ に実在しない」）
- [x] 別紙の trace ブロックが `ADR-0108`〜`ADR-0110` を引き、`check-trace-blocks.js` が通る
- [x] `check-planning-adr-range.js` が公開 `kg-ranges.json` と突き合わせて `status: ok`・`declaredMax: 110`・`planningMax: 110`・`scanned: 4`
- [x] `node scripts/scripts.test.js` が緑

## 必須チェックへの影響

レンジを広げる変更であり、既存の履歴・文書が引く計画 ADR はすべて 0107 以下（軸 3）。
値域検査が新たに赤くなる入力は存在しない。ワークフロー・ジョブ名は触らない。
